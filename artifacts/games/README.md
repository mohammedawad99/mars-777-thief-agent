# artifacts/games/

**Status: DRAFT (foundation).**

This directory is **tracked**. It will hold **selected, verified game evidence**
(e.g., approved replays / reports) promoted from local runtime output.

- Only evidence **reviewed for secrets and opponent-private data** may be
  promoted here (see `SECURITY.md`).
- Raw runtime logs / state live under `runtime/` and are **git-ignored**.

No evidence exists yet.
