[project]
name = "mars-777-thief-agent"
version = "1.0"
description = "Thief agent (group MaRs-777) for the 2026 Distributed Police-Thief P2P final project."
readme = "README.md"
requires-python = "==3.12.*"
license = { text = "UNLICENSED" }
authors = [{ name = "Mohamed Awad", email = "m7md.awad.1999@gmail.com" }]
dependencies = [
    "fastmcp==3.4.6",
    "pillow==11.3.0",
    "pydantic==2.13.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mars777_thief"]

[dependency-groups]
dev = [
    "pytest>=8,<9",
    "pytest-cov>=5,<7",
    "ruff>=0.6,<1.0",
    "mypy>=1.11,<2.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tools", "research"]
# Resolve `mars777_thief` from source rather than from the installed wheel: the
# distribution ships no `py.typed` marker, so an importer would otherwise be told
# the package is untyped. Adding that marker is a packaging change and belongs to
# the packaging slice, not to a research stage.
mypy_path = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q --cov --cov-report=term-missing --cov-fail-under=90"
markers = [
    "windows_known_limitation: the one native-Windows two-process exact-six stall, run non-gating on Windows and gating everywhere else",
]

[tool.coverage.run]
branch = true
source = ["mars777_thief", "research"]

[tool.coverage.report]
show_missing = true
fail_under = 90
