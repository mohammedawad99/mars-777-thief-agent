# runtime/

**Status: DRAFT (foundation).**

Local, per-repository runtime area. Its `logs/` and `state/` subdirectories are
**git-ignored** - they hold regenerated, game-local data that is never shared
with the sibling agent and never committed.

This `README.md` is tracked so the directory's purpose is documented; the
runtime contents themselves are not tracked.
