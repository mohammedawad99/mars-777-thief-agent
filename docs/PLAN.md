# Project Plan - group MaRs-777 (THIEF)

> **Status: DRAFT.**
> **Purpose:** Sequence the work from foundation to a compliant, competitive
> THIEF agent.
> **Authoritative source:** book v3.0.0 (`.project-spec/police_thief_p2p.pdf`);
> Appendix E (rules/sanctions); Appendix F (numeric values).
> **To extract later:** the concrete milestones and acceptance criteria implied
> by the book once the full 160-page extraction is done.
> **Note:** No requirement is approved merely because this file exists.

## Phases (provisional)

- **Stage 0A - Environment audit.** Complete.
- **Stage 0B - Local foundation.** Complete (incl. 0B.1 evidence audit).
- **Stage 0C - Private remote, reviewed initial commit + push, CI verified.** Complete.
- **Stage 1 - Specification baseline.** **Adopted — REVIEWED / APPROVED / LOCKED.**
  The common Stage-1 baseline (extraction, Appendix-E/F corrections, requirement
  traceability, conflict resolutions, four JSON contracts, cryptographic-authentication
  distinctions, reporting contract) was **synchronized into this repository** from the
  Police locked source commit `691280dc3219452eeff462c997714fd5bcbd9e55` after
  supervising review — **not independently re-extracted here** (see `SOURCES.md` →
  *Synchronization provenance*, and `DECISIONS.md` D15). Lives in `docs/spec/`.
  **This synchronization is itself pending supervising review before it is committed.**
  Implementation: **NOT STARTED**.
- **Stage 2 onwards (PRD/architecture)** begins only **after** this synchronization is
  reviewed. No substantive PRD work has begun.
- **Stage 2A - Architecture freeze + PRD blueprints.** Complete (documentation only): 21 architecture documents in `docs/architecture/` (system topology, dependency layering, state ownership, state machine, error model, 20 ports (18 at 2A; `SeriesLauncherPort` + `CompatibilityProfilePort` added at 2A-R2), data/privacy flow, concurrency, config, artifacts, strategy seam, LLM boundary, observability, 12 test layers, security threat model, four quality gates, traceability, red-team, sync matrix) + seven PRD **blueprints**. All 91 requirements architecture-mapped; 11 blocking red-team findings resolved.
- **Stage 2A-R - Lecturer reference audit.** Complete: read-only audit of `rmisegal/Game-P2P-Cop-Chase` @ `960499fd` (v3.0.0). Its bundled book PDF hash matches ours exactly. Reference classified **NON-BINDING**; its unkeyed `SHA256(terms|nonce)` "signature" does **not** satisfy the book's keyed pre-supplied-key requirement (book wins). Chatbot question pack produced.
- **Stage 2A-R2 - Final reconciliation.** Complete: attachment evidence AE-01…AE-04 recorded as **secondary provenance**; all chatbot items closed (zero pending). Compatibility profiles defined (STRICT / REFERENCE / ATTACHMENT). One authorized Stage-1 **project-contract** correction: **JDEC-014** — the result **references** the declaration instead of duplicating static metadata (four-artifact-set self-containment), so the field matrix moved **77 → 75** (result 13 → 11). No requirement, Appendix E/F value, filename, or conflict changed.
- **Stage 2A-CLOSE - Consistency sweep + commit.** Stale-baseline sweep, non-self-referential `result_sha256` audit, symmetry re-verification, then commit + push + CI.
- **Stage 2B - PRD-01…04 authored in full.** Complete and reviewed (documentation only).
- **Stage 2C - PRD-05…07 authored in full.** Complete and reviewed; all 91 requirements given exactly one primary owner (PRD-01 15 / PRD-02 11 / PRD-03 3 / PRD-04 10 / PRD-05 2 / PRD-06 14 / PRD-07 24 / EXTERNAL-SUBMISSION 12).
- **Stage 2-CLOSE - PASS.** Resolved CLOSE-F1 (series convention is negotiated protocol metadata, **not** a declaration field — matrix stays **75**) and CLOSE-F2 (declared MCP endpoint is a **stable group-level ingress**, 1/team, static whole-series). **All seven PRDs are APPROVED — PHASE 2 LOCKED.** **Implementation: NOT STARTED.**
- **Phase 3 - Deterministic core implementation. STARTED.** Begun after Phase-2 closure review; the phase is **not** complete.
- **Stage 3A - Deterministic domain foundation.** Complete and reviewed (**PASS**), then committed at Stage 3A-CLOSE with CI green. Tests-first: immutable `GridConfig` (the project grid minimum is enforced here, never in `Board`), immutable `Position`, immutable **policy-free** `Board` geometry with blocked cells, `Move` = N/S/E/W/STAY, stable `MOVE_ORDER` = (N, S, E, W, STAY), destination calculation, bounds/blocked legality, deterministic `legal_moves`, and typed `apply_move` failure with no mutation. Role-neutral and byte-identical in both repositories; no opponent truth, no I/O, no dependency added. **Foundation only** - barriers as an action, capture, terminal/survival, scoring and scent are **not** implemented.
- **Stage 3B - Deterministic game semantics.** Complete and reviewed (**PASS**), then committed at Stage 3B-CLOSE with CI green. Tests-first: barrier placement (own/cardinal cell, quota MINIMUM 14, irreversible, no removal API, never moves the actor), the three capture routes (same cell, barrier on the occupied cell, GAME-005 trapped by adjacency), terminal/survival evaluation from configuration, role-keyed scoring (capture 20/5, survival 5/10, technical loss 0/0 via Ch 3 + App E #48 and C-07, tie 2/2 at series scope only) and bounded deterministic scent physics in `Decimal`. Two implementation-discovered source anomalies are recorded with explicit provenance: **JDEC-015** (terminal threshold admissibility - a source *gap*) and **C-10** (scent state bound vs additive update - a source *contradiction*). No Appendix-F value, requirement count or artifact contract changed.
- **Stage 3C - Local application / turn orchestration foundation.** Complete and reviewed (**PASS**), then committed at Stage 3C-CLOSE with CI green. Tests-first: `LocalTruth` owning board, own position and completed steps; typed `MoveAction`/`BarrierAction` making one command structurally one action; a role-specific `LocalTurnService` (police move **or** barrier, thief move only, refused before any domain effect); atomic application delegating to the existing `apply_move`/`place_barrier`; and max-moves exhaustion from the validated `TurnLimits`. **Stage 3C-FIX1** removed a duplicated `barriers_placed` counter so barrier usage has one authoritative representation. The layer declares no terminal outcome, computes no score and runs no scent lifecycle. **PRD-02 moves to IN PROGRESS**; PRD-01 stays IN PROGRESS.
- **Stage 4A - Local protocol state machine foundation.** Complete and reviewed (**PASS**), then committed at Stage 4A-CLOSE with CI green. Tests-first: the frozen 18-phase graph (15 normal + 3 fault) with **31** legal directed edges, verified exhaustively over all **324** ordered pairs (31 legal / 293 illegal); `ProtocolMachine` owns **only** its phase - no step, sub-game or series cursor, no board and no game state; normal bootstrap is `ProtocolMachine.start()` at BOOT, with direct arbitrary-phase construction reserved as a trusted internal snapshot primitive. **Stage 4A-FIX1** reconciled the TECHNICAL_LOSS lifecycle: it ends the current sub-game and rejoins the existing SUBGAME_COMPLETE boundary, while TAMPERED and FAILED remain absorbing. The crypto-named phases carry no cryptography, no message bodies and no transport.
- **Stage 4B - Protocol event / transition evidence foundation.** Complete and reviewed (**PASS**), then committed at Stage 4B-CLOSE with CI green. Tests-first: `ProtocolMachine.advance(target)` now returns one immutable `TransitionResult(machine, evidence)`, where `TransitionEvidence` holds exactly `source_phase` + `target_phase` - the state machine's own share of R7 as fixed by `PROTOCOL_TIMELINE.md` event 10 ("phase transition"; persisted "—", hashed "—"). Richer per-state evidence stays with its owning subsystem and `infra.logger`. **No event enum was invented**: no source or architecture document freezes a transition-signal vocabulary, so the target phase remains the typed request and `_ALLOWED` remains the one graph. There is no evidence-less transition API and no compatibility shim. **Stage 4B-FIX1** made evidence **valid by construction** - the pair must be an edge of `_ALLOWED`, so 31 legal pairs are constructible while all 293 illegal ordered pairs and all 18 self-loops raise `IllegalTransitionError`; the replay corruption boundary moved forward to the constructor and negative replay coverage grew (duplicated and out-of-position records) instead of shrinking. FIX1 also reconciled a reported "153-line" file as a **cross-repo semantic-diff count**, not physical LOC (repository-wide max **150**, **0** over). Structural validity is **not** authenticity: no hash, signature, nonce, MAC, event id, timestamp or sequence number exists, and replay is **phase-path only**. **No** FastMCP, network I/O, tunnel, cryptography, logger/replay persistence, JSON artifact or commit/reveal transport.
- **Stage 4C - Local orchestrator / protocol guard foundation.** Complete and reviewed (**PASS**), then committed at Stage 4C-CLOSE with CI green. Tests-first: `LocalOrchestrator` composes a `ProtocolMachine` and owns the single live fact `STATE_OWNERSHIP.md` assigns it - the **current sub-game index** (lifetime SERIES) - alongside a read-only `SeriesConfig` projection. Phase legality is never re-implemented: every request goes through `ProtocolMachine.advance()`, the module holds **no adjacency table and makes no `TransitionEvidence` call**, and the emitted evidence is passed through unchanged. Exactly one branch is decided locally - `SUBGAME_COMPLETE -> READY` (continue, cursor +1) versus `-> SERIES_COMPLETE` (end, cursor unchanged) - because the cursor plus the fixed series length truthfully determine it, so no caller boolean is accepted. Two ownership conflicts were resolved without touching frozen architecture: `MODULE_BOUNDARIES.md`'s "turn cursor" yields to `STATE_OWNERSHIP.md` (the D17 precedent), and role alternation stays a PRD-05 / `SeriesLauncher` convention (PRD02-FR-011), never derived from an odd/even index. **Stage 4C-FIX1** corrected a binding numeric error caught **before commit**: the series length had been implemented as a floor (`>= 6`) after following stale wording in `CONFIG_CONTRACT.md` and the C-05 impact cell ("6 or the agreed higher"), but Appendix F lists `num_games` among its **FIXED** rows and defines FIXED as "binding, unchangeable; deviation disqualifies" - so 7 is as illegal as 5. `FIXED_NUM_GAMES` now enforces exact equality, the two stale sentences were narrowed to the already-locked App F / C-05 meaning, and no JDEC, conflict, requirement or register entry was added. A counted series is therefore exactly 6 sub-games with exactly five cursor increments and **no representable seventh**; technical loss reaches the same `SUBGAME_COMPLETE` boundary and leaves the cursor sequence identical. Normal bootstrap is `LocalOrchestrator.start()`; direct construction is a trusted snapshot/test primitive with **zero** production callers. **Not** implemented: score recording (still `app.orchestrator`'s per `STATE_OWNERSHIP.md`, pending truthful terminal facts), `LocalTurnService` integration, FastMCP/network I/O, commitment hashing, nonces, signatures, canonical payloads, artifact persistence, replay, strategy, GUI or reporting.
- **Stage 4D - Application port contracts foundation.** **Attempted and blocked before any code** (`PORT-DEPENDENCY-BLOCKER`); zero repository changes. Three internal contradictions made a truthful implementation impossible: `API_BOUNDARIES.md` states in its own header that **no Python signatures are fixed**, and a repository-wide search of every architecture and PRD document confirmed no `def`, `Protocol` or return annotation exists, so every method name would have been invented; the `app.ports` row allowed **stdlib typing only** while the same architecture defines ports over `Board`/`Move`/`Outcome`/`ScoreLine`; and this file still claimed 18 ports where the frozen table holds 20. A thin `ClockPort` exposing `monotonic()` was considered and refused, because that row also owns deadlines and timers whose machinery `CONCURRENCY_MODEL.md` assigns to the Watchdog, the Deadline timers and the executor. Nothing was guessed.
- **Stage 4D-R1 - Application port architecture reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4D-R1-CLOSE with CI green. Documentation only, no Python: the `app.ports` row now allows `stdlib typing` **+ type references to immutable `domain` value types** and still forbids `protocol`, `infra`, adapters, network/FastMCP libraries, `app` implementation modules and domain services with side effects — dependency inversion (D3) is aimed at concrete adapters, not at the domain model, and `app` already imports `domain`, so no edge is added to the layer DAG. `API_BOUNDARIES.md` gained a signature-freeze policy recording that any future Python signature is a **PROJECT-CONTRACT** (never source-, lecturer- or reference-mandated), that the reference tool names `negotiate`/`receive_turn`/`submit_audit`/`receive_control` are **LECTURER_REFERENCE_COMPATIBILITY** only, and that a port row is **not** an automatic instruction to create a Python `Protocol`. The port count is corrected to **20**. Readiness after R1: **0 READY**, 3 NEEDS-SEPARATE-DESIGN (`GameRulesPort`, `ScoringPort`, `ClockPort`), 5 BLOCKED-BY-FUTURE-TYPE, 10 LATER-PRD, 2 EXTERNAL-RUNTIME. See **D22**.
- **Stage 4E - Protocol semantic message contracts foundation.** **Attempted and blocked before any code**; zero repository changes. Two blockers: `MESSAGE-PHASE-DEPENDENCY-BLOCKER` — the single authoritative `ProtocolPhase` lives in `app.state_machine`, which `protocol.messages` (allowed deps `canonical, domain`) may not import, and duplicating or relocating it was forbidden; and `MESSAGE-ENVELOPE-CURSOR-BLOCKER` — PRD02-FR-044's `(sub_game, step, phase)` left step numbering undefined for pre-turn families (a repository-wide search finds no step-numbering statement anywhere), `sub_game` undefined at series-level Step-0, and `phase` undefined outright. The audit additionally corrected the peer-visible timeline inventory from the previously planned **9** to **10**: Event 8 "Move validation" transmits accept/reject and had been omitted. No signature, payload or sentinel was guessed.
- **Stage 4E-R1 - Semantic message architecture / turn-cursor reconciliation.** Documentation only, no Python. `app.peer_messages` is added as an application **contract** module for immutable internal peer-message values — proved rather than preferred, since the Turn Executor that consumes them is `app` and `app` may never import `protocol` — while `protocol.messages` becomes the wire ⇄ semantic mapper and keeps no state. **PRD02-FR-044 is narrowed** to turn-scoped messages carrying `(sub_game, step)`, with the phase check remaining receiver-side under FR-062/STATE-003, because FR-063 (the requirement that names the "cursor guard" and shares FR-044's R8 trace) lists only sub-game and step, and because a lockstep state-machine walk shows a transmitted phase is either redundant or not independently computable. `step` for turn-scoped messages is the **pending turn number** (`completed_steps + 1`; first gameplay step 1), and the cursor is a **projection** of existing owners, never a second state store. Both Stage-4E blockers are resolved and the family inventory is corrected to **10**. See **D23**.
- **Stage 4E-R1-FIX1 - Turn-cursor consistency / sub-game-control identity.** Documentation only, no Python. Supervising review found three current contracts still defining the cursor as `(sub_game, step, phase)` after FR-044 was narrowed — PRD-02 §8, `CONCURRENCY_MODEL.md` §2 guard 2 and `SECURITY_ARCHITECTURE.md` T3 — so all three now read `(sub_game, step)` plus a separate receiver-side phase-admissibility check against the one authoritative `ProtocolMachine` (FR-021/FR-062/STATE-003); FR-062 is untouched and state admissibility is not weakened. The `step` upper bound was reclassified as **contextual** rather than structural, because `max_moves` is per-sub-game locked configuration and is not a cursor field — structural validation is only `int` and `step >= 1`, while `sub_game` may stay `1..6` since `num_games` is globally FIXED. Sub-game-control identity (`Config negotiation`, `Config lock`) is explicit semantic context, never inferred from a `g<NN>` artifact filename. See **D23a**.
- **Stage 4F - Protocol value representation contracts foundation.** **Attempted and blocked before any code** (`VALUE-MODULE-HOME-BLOCKER`); zero repository changes. Nine representations were audited independently: seven were genuinely unresolved and deferred rather than guessed, while the two that were representation-ready — a SHA-256 digest and the final-audit verdict — had **no module home reachable by their consumers**, and duplicating the validator per layer or inventing a `types.py` were both refused.
- **Stage 4F-R1 - Shared protocol value architecture / representation reconciliation.** Documentation only, no Python. The blocker analysis was corrected as **too broad**: `infra.logger`, `infra.artifacts` and `infra.reporter` are mappers that serialize a structured record already reaching them through `app.ports`, so `infra` needs no new dependency and `DEPENDENCY_RULES.md` is unchanged. The genuine consumers — `app.peer_messages`, `protocol.commitment` and `protocol.config_lock` — are served by one inner contract module, **`app.protocol_values`**, with narrow type-reference access for exactly the two protocol producers, reusing the outer→inner pattern Stage 4E-R1 authorized for `protocol.messages`. `domain` was rejected (protocol semantics, not game concepts) and a generic `types.py` outright. The digest representation resolved to canonical **lowercase 64-hex as PROJECT-CONTRACT**: SHA-256 is SOURCE, hex is project-locked, 64 is entailed, and case was free because the sole `.hexdigest()` occurrence is inside a block labelled "the reference" — uppercase is **rejected, never normalized**, with any wire compatibility handled at `protocol.messages`. The final-audit verdict is the closed pair `Verified OK` / `TAMPERED`. `RESULT_CONTRACT.md`'s `mutual_agreement` scalar/object contradiction was corrected in favour of the bool its field table, scoring rule and example already define, preserving the approval-core exclusion semantics and the locked 11-field result count. See **D24**. Closed at Stage 4F-R1-CLOSE with CI green, together with **Stage 4F-R1-FIX1**, which made the two protocol producers' dependency a **runtime use** rather than annotation-only, widened `app.protocol_values` to the pure stdlib value-definition primitives (`typing`, `dataclasses`, `enum`) it actually needs, and corrected the category accounting to **2 ready / 6 deferred / 1 reconciled = 9**. A follow-up debt is tracked: `app.peer_messages` carries the same narrow "stdlib typing" wording and must be re-audited before Stage 4E resumes. See **D24a**.
- **Stage 4F — RESUME (two approved values).** Complete and reviewed (**PASS**), then committed at Stage 4F-CLOSE with CI green. Tests-first in both repositories: the first RED was `ModuleNotFoundError` for `app.protocol_values` and closed at 55 targeted tests; supervising review then produced **Stage 4F-RESUME-FIX1**, whose RED was 6 failures caused by `InvalidDigestError` subclassing bare `Exception` and being absent from the otherwise exhaustive app public surface, closing at 58. The module holds exactly three classes — two semantic values and one supporting error. `Sha256Digest` is a frozen, slotted single-field value that accepts only a strict `str` of exactly 64 characters over `[0-9a-f]`: SHA-256 is SOURCE-EXPLICIT, hex is PROJECT-LOCKED, 64 is entailed, and **lowercase is a PROJECT-CONTRACT**, so uppercase is refused and never normalised, with any future wire compatibility belonging to `protocol.messages`. It performs **no hashing** and knows nothing of which field a digest belongs to. `FinalAuditVerdict` is the closed `Verified OK` / `TAMPERED` pair, kept distinct from the Event-8 move verdict, the Event-9 `verified` record, intent classification and technical loss. `InvalidDigestError(ValueError)` makes malformed construction catchable both narrowly and generically, matching `FinalAuditVerdict("OK")`. Six value categories stay deferred and result agreement still needs no dedicated type. See **D25**.
- **Stage 4E-R2 — Peer message runtime contract readiness reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R2-CLOSE with CI green on Ubuntu + Windows, together with **Stage 4E-R2-FIX1** and **Stage 4E-R2-FIX2**. Documentation/design only, no Python. Two `app.peer_messages` dependency defects are paid off before any Stage-4E code exists: the row allowed only "stdlib typing" (which cannot define an immutable validated value — the identical finding Stage 4F-R1-FIX1 made for `app.protocol_values`) and it could not name `app.protocol_values` at all, so a message could not carry the `Sha256Digest` that reconciliation was built to supply. The row now allows the pure stdlib value-definition primitives (`typing`, `dataclasses`), immutable `domain` value types and `app.protocol_values` at **runtime use**; `enum` is **withheld** because no peer message defines a vocabulary of its own — `Move` and `FinalAuditVerdict` already exist and are reused. Acyclicity is **structural**: `app.protocol_values` admits only pure stdlib, so the back edge is impossible under its own allow-list and D2 needs no new rule. All ten peer-visible families were re-audited against ten readiness criteria, and only representations already frozen were counted: **1 READY** (Commitment) and **9 blocked**, each with a named blocker. `TurnCursor` is **TURN-CURSOR-READY** with `app.peer_messages` as its home — `app.protocol_values` exists for primitives that *outer protocol implementations* also need, and neither `protocol.commitment` nor `protocol.config_lock` needs a cursor; it remains a projection of `app.orchestrator`'s sub-game index and `domain.truth`, never a second owner. Three findings contradicted the stage's own hypotheses and were reported rather than smoothed over: **Reveal** cannot be `(cursor, Move, hint)` because no contract says how a police barrier placement — a legal one-action turn under PRD01-FR-010 that BAR-001/FR-035 require to be declared with its exact cell — reaches the peer; **Result agreement** is blocked because `INTEROPERABILITY_NEGOTIATION.md` NDEC-006 still carries the `mutual_agreement.confirmed` object form Stage 4F-R1 withdrew from `RESULT_CONTRACT.md`, and because the agreement bool is set *after* the exchange it cannot be truthful in a single message; and **Acknowledgement** got two of three sub-questions resolved by PRD06-FR-082 (the digest *is* carried, `ack_of_step` *is* the cursor step) but stays blocked on whether the acking role is transmitted or attributed at persistence time. Selected Stage-4E subset: **`TurnCursor` + `Commitment` only**. See **D26**.
- **Stage 4E-R2-FIX1 - Current result-agreement contract consistency + peer-message construction error semantics.** Documentation only, no Python, **not committed**. Supervising review approved the R2 readiness result and required two close-critical corrections. **(1) NDEC-006.** The withdrawn `mutual_agreement.sha256` / `.confirmed` nested object form was still a live project contract there; NDEC-006 now states the approval **procedure** (both peers build the core, each computes `result_sha256`, the digests are exchanged and compared, and only once equal does each record carry the flag) and the **record shape** (`result_sha256` and `mutual_agreement` as separate top-level fields), with no replacement nested object. An explicit scope limit was added: NDEC-006 freezes a *record* shape, never a *message* shape, so **Family 14 stays BLOCKED-BY-PAYLOAD-SHAPE** on message multiplicity, disagreement signalling and message-layer identity. A mechanical sweep then found the residue is wider than R2 reported: **PRD06-FR-142**, **PRD07-FR-085** and **PRD07-FR-190** still assert the nested form, and FR-085 contradicts FR-080 inside the same PRD-07 table. FIX1 was not authorized to edit PRDs, so those three are reported and tracked rather than changed — which is why this stage closes **PARTIAL**, not PASS. Provenance recorded: the nesting comes from the lecturer's extracted attachment example (secondary provenance, AE-03), which PRD07-FR-087 already declares is not a verified parser schema, so no source requirement is discarded by removing it. **(2) Construction errors.** The future static-construction contract for `TurnCursor` and `Commitment` is **built-in `ValueError` for every malformed construction, with zero supporting error types** — `FinalAuditVerdict` is the in-project precedent for a new semantic value raising native `ValueError`, `InvalidDigestError` is a `ValueError` subclass so one catch covers both contract modules, and a narrow subclass remains a non-breaking later addition. `Commitment` **never** auto-wraps: a raw `str` is not coerced to `Sha256Digest` and a tuple/dict is not coerced to `TurnCursor`, preserving one authoritative constructor per value. Readiness is unchanged at **1 READY / 9 BLOCKED**; the Reveal barrier gap, the Acknowledgement `by_role` gap, the Final-audit association gap and the `protocol.messages` preflight debt are all preserved. See **D26a**.
- **Stage 4E-R2-FIX2 - PRD result-agreement propagation + TurnCursor fixed-constant dependency reconciliation.** Documentation only, no Python, **not committed**. It closes the two items that made FIX1 correctly return PARTIAL. **(1) PRD propagation.** The three active requirements still asserting the withdrawn nested form were corrected narrowly and in place: **PRD06-FR-142** excludes "`result_sha256` itself, the `mutual_agreement` flag"; **PRD07-FR-085** stores the digest as a separate top-level result field beside the separate top-level bool, which resolves its contradiction with **PRD07-FR-080** four rows above it in the same table; and **PRD07-FR-190** requires both reports to carry the same `result_sha256` and record `mutual_agreement = true` as a separate field once the comparison establishes equality. Each keeps its ID, modality, ownership, ordering and provenance — nothing added, deleted or renumbered — and each carries an explicit note that it is a **record** requirement, so **no Event-14 message multiplicity is frozen** and Family 14 stays `BLOCKED-BY-PAYLOAD-SHAPE` on message flow, disagreement signalling and message-layer identity. **Zero current project contracts** now assert the nested form; it survives only as historical annotation and as the lecturer attachment evidence, both left intact because PRD07-FR-087 already records that the example is not a verified 1:1 parser-exact schema. **(2) Constant dependency.** The `app.peer_messages` row permitted immutable `domain` value **types** — which names classes and therefore did **not** authorize the two module-level `Final[int]` constants `TurnCursor`'s frozen bound needs. Reading "value types" generously would have repeated exactly the mistake corrected at 4D-R1 and 4F-R1-FIX1, so one narrow widening now names `FIRST_SUB_GAME` and `FIXED_NUM_GAMES` explicitly, read-only. A literal `6`, a copied constant and a `SeriesConfig`-derived bound were each rejected on evidence: the first two would create a **second** numeric authority for an Appendix-F FIXED value, and the third would either add a third field to a two-field cursor or make a transmitted cursor appear to depend on locked configuration it does not carry. Single authority is mechanically proved — `FIXED_NUM_GAMES: Final[int] = 6` is the only `= 6` series length in the source tree — and `app.orchestrator` already imports `FIRST_SUB_GAME` at runtime, so **no new layer edge**. Readiness is unchanged at **1 READY / 9 BLOCKED**; the Reveal, Acknowledgement, Final-audit and `protocol.messages` gaps are all preserved. See **D26b**.
- **Stage 4E — RESUME: TurnCursor + Commitment Foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-RESUME-CLOSE with CI green on Ubuntu + Windows. **A slice close, not a stage close:** Commitment was the only one of the ten peer-visible families that Stage 4E-R2 could prove ready, so nine remain BLOCKED and **Stage 4E as a whole is not complete**. Tests-first in both repositories: RED #1 was `ModuleNotFoundError` for `app.peer_messages`; RED #2, after the module existed, was the missing `TurnCursor` export on the exhaustive app surface; GREEN at **73** targeted tests, with Police at 721 and Thief at 716 overall and 100% coverage. The module holds exactly two classes and **no** supporting error type. `TurnCursor` is the frozen, slotted `(sub_game, step)` turn identity — no phase, because the receiver already owns the single authoritative `ProtocolMachine` — validating structure only: strict `int` with `bool` refused, sub-game bounded by the one authoritative `FIRST_SUB_GAME`/`FIXED_NUM_GAMES` contract rather than a restated literal, and `step >= 1` with no ceiling, since `max_moves` is per-sub-game locked configuration that a transmitted cursor deliberately does not carry. `Commitment` is exactly `cursor` + `h_commit: Sha256Digest`, matching `PROTOCOL_TIMELINE.md` event 5's "`H_commit` only" plus the cursor required independently by PRD02-FR-044 and PRD06-FR-086. Composition uses **exact type identity**, so even a `Sha256Digest` subclass is refused, and it **never coerces** — a valid lowercase 64-hex string raises rather than being wrapped, keeping one authoritative constructor per value. No hashing, no phase, no message-kind enum and no privacy-leaking field. See **D27**.
- **Stage 4E-R3 - Acknowledgement + Reveal contract reconciliation.** Complete and reviewed (**PASS**, together with its supplemental evidence report), then committed at Stage 4E-R3-CLOSE with CI green on Ubuntu + Windows. Documentation/contract only, no Python. **A reconciliation close, not a stage close: eight families remain blocked and Stage 4E as a whole is not complete.** Both families were re-derived from the **authoritative book** instead of from our own derived contracts, and the two questions came apart. **Acknowledgement became READY**: Ch 5 §5.3.2 (p.51) gives its entire content as *the opponent confirms it received the commitment and is locked onto it*, which PRD06-FR-082 already renders as a specific `H_commit` for a specific `(sub_game, step)`, so the message is exactly `cursor` + `h_commit` and needs no new dependency. `by_role` resolved to **LOCAL-LOG-ATTRIBUTION**: the source names an acknowledging party but never a payload field, Figure 6 carries it as message direction, and a local writer always knows it — one opponent process (ARCH-001/002), roles immutable from `CONFIG_LOCKED` (PRD06-FR-048), and it knows whether it sent or received. Since role is never transmitted, a hostile peer has nothing to forge. **Reveal stayed BLOCKED, but for a different and much smaller reason than before.** The Stage-4E-R2 finding — that no contract said how a police barrier reaches the peer — was true of our *derived* documents but not of the source: Ch 5 p.51 defines the sealed `Move` as *the physical action (movement, barrier placement, etc.)*, Reveal sends *the action (Move)*, and Ch 3 p.37/38 make the exact location a truthful, never-hidden declaration. So a barrier is an ordinary action bound by the same commitment and **no eleventh family is required**. What is missing is only *representation and home*: no shared action semantic type exists that both the future commitment producer and `app.peer_messages` can reach — the clean home is a domain-owned immutable action type, which needs new Python plus a `turn_service` refactor — and the sealed `move` value's canonical representation is unfrozen, where `NDEC-002` freezes `state`'s. `LOG_CONTRACT.md`, `NDEC-001` and the timeline now record both facts. Reclassified from `BLOCKED-BY-PAYLOAD-SHAPE` to the more exact **`BLOCKED-BY-VALUE-REPRESENTATION`**. Next subset: **Acknowledgement only**. See **D28**.
- **Stage 4E — RESUME 2: Acknowledgement Foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-RESUME2-CLOSE with CI green on Ubuntu + Windows. **A slice close, not a stage close:** `Acknowledgement` is the **second** implemented peer-message family, eight remain BLOCKED and Stage 4E as a whole is **not complete**. Exactly one new type in `app.peer_messages`, `Acknowledgement(cursor, h_commit)`, reusing the frozen `TurnCursor` + `Sha256Digest` contract, built-in `ValueError`, **0** supporting errors, no coercion, no hashing, no phase and no new dependency. Its first attempt was interrupted and produced an unclean preflight and a reconstructed RED; review rejected that provenance, and **Stage 4E-RESUME2-FIX1** replayed the slice from the exact clean committed parents with a genuine tests-first sequence, which is the authoritative history. Reveal stayed excluded.
- **Stage 4E-R4 - Shared physical action + canonical move representation reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R4-CLOSE with CI green on Ubuntu + Windows. Documentation / architecture / contract only, **0 Python**. Re-verified from the authoritative PDF rather than our derived docs. Freezes the two things that were blocking Reveal: (a) the **semantic home** of the chosen physical action — a new domain-owned `domain.actions` holding `MoveAction`, `BarrierAction`, the `PhysicalAction` alias and the `ActionKind` tag vocabulary moved down from `app.turn_service`, reachable by local execution, the future commitment producer and Reveal while `domain` imports none of them; and (b) the **canonical representation** of that action under the unchanged sealed `move` key — a tagged, structurally-exclusive object `{kind,value}` reusing the FIXED `move_set` tokens and the already-locked `[row,col]` coordinate array. The sealed field set stays exactly eight and `domain.rules.Move` stays the five-token movement vocabulary. **Reveal is reclassified BLOCKED-BY-VALUE-REPRESENTATION → BLOCKED-BY-FUTURE-SEMANTIC-TYPE.**
- **Stage 4E-R5 - Shared physical-action foundation + `LocalTurnService` migration.** Complete and reviewed (**PASS**), then committed at Stage 4E-R5-CLOSE with CI green on Ubuntu + Windows. **A slice close, not a stage close.** One atomic migration: `domain.actions` now owns the only `ActionKind`/`MoveAction`/`BarrierAction` definitions and the `PhysicalAction` union, `LocalTurnService` consumes them, and the app-level names are identity-equal re-exports rather than a second representation. Structural validation only, with `InvalidPhysicalActionError(DomainError)` for malformed construction and the LIVE turn-service errors unchanged. 800 / 795 tests at 100% coverage.
- **Stage 4E — RESUME 3: Reveal Foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-RESUME3-CLOSE with CI green on Ubuntu + Windows. **A slice close, not a stage close:** `Reveal` is the **third** implemented peer-message family - exactly `cursor: TurnCursor` + `action: PhysicalAction` + `hint: str`, with exact `(MoveAction, BarrierAction)` membership, no coercion, built-in `ValueError` on wrong composition, and no nonce, state, intent, role, phase or `H_commit`. 858 / 853 tests at 100% coverage. `app/peer_messages.py` finished at exactly **150/150** LOC after prose-only compression whose behavioural neutrality was proved by executable-AST equivalence.
- **Stage 4E-R6 - Remaining turn-protocol readiness + peer-message module organization reconciliation.** Complete and reviewed (**PASS**, with corrections R6-FIX1 and R6-FIX2), then committed at Stage 4E-R6-CLOSE with CI green on Ubuntu + Windows. Documentation / architecture / contract only, **0 Python**. Freezes a four-module organization chosen by measured LOC rather than taste - `app.turn_cursor` + `app.peer_turn_messages` + `app.peer_final_messages` behind an `app.peer_messages` **façade** re-exporting identity-equal classes, with no message hierarchy, enum or registry. Re-audits the three remaining turn/finalization families together against Figure 6, which draws only four arrows: **#11 Final nonce reveal becomes READY-TO-IMPLEMENT** (batched, own steps only, with the nonce representation frozen in NDEC-001 in place), while **#8** and **#12** are reclassified to **BLOCKED-BY-PAYLOAD-SHAPE** because the source never fixes whether either is transmitted at all.
- **Stage 4E-R7 - Peer-message module reorganization.** Complete and reviewed (**PASS**), then committed at Stage 4E-R7-CLOSE with CI green on Ubuntu + Windows. A pure, behaviour-preserving migration: `peer_messages.py` went from a **150-LOC implementation module to a 20-LOC façade**, `TurnCursor` moved to `app.turn_cursor` and the three turn families to `app.peer_turn_messages`, with `app.peer_final_messages` created as an empty boundary. One definition per class, identity-equal re-exports, natural `__module__` relocation with nothing forged, executable-AST equivalence for all five moved definitions, and **zero edits to the existing behaviour tests**. 881 / 876 tests at 100% coverage; the capacity blocker is removed with real headroom.
- **Stage 4E-R8 - Final nonce reveal foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R8-CLOSE with CI green on Ubuntu + Windows. **A slice close, not a stage close:** `FinalNonceReveal` is the **fourth** implemented peer-message family, and it filled the finalization boundary R7 created. Delivered tests-first from clean parents: `InvalidNonceError` + `NonceValue` (exactly `[0-9a-f]{32}`, never normalised) in `app.protocol_values`; `NonceRevealEntry` + `FinalNonceReveal` in `app.peer_final_messages`; façade and app re-exports identity-equal. Representation is separated from generation, freshness and secrecy, so a low-entropy-looking value is structurally valid; empty, duplicate, out-of-order and mixed sub-game batches construct because those rules are LIVE. 983/978 tests, 100% coverage. No recomputation, `FinalAudit`, canonicalization runtime, wire mapper, FastMCP or transport.
- **Stage 4E-R9 - Canonical commitment codec + recompute foundation.** Attempted and **stopped `BLOCKED-BEFORE-CODE`** at the mandatory pre-code dependency audit; the supervising review accepted the stop. Five of eight sealed members were implementation-ready (`move`, `hint`, `step`, `sub_game`, `nonce`); `role`, `state` and `intent` were not. **0 code, 0 tests, 0 docs** were produced - no partial codec, no `protocol/` package, no invented semantic representation. Reconciled by 4E-R9-R1; the codec resumes after 4E-R9-R2.
- **Stage 4E-R9-R1 - Sealed commitment semantic prerequisites reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R9-R1-CLOSE with CI green on Ubuntu + Windows. Documentation / contract / architecture only, **0 Python**. Froze the canonical sealed role vocabulary `police`/`thief` with an explicit runtime mapping and three separated vocabularies; froze `intent` to the source-explicit `truth`/`lie`; froze complete future contracts for `ActorRole`, `Intent` and `SealedState` in `app.sealed_record_values`; pinned sorted duplicate-free barriers and the two state/top-level equality invariants; repaired the stale `LOG_CONTRACT.md` state text and the `[row,col]` JDEC citation; propagated `ensure_ascii=False` into the central canonical contract; and separated pure digest comparison from `E-HASH-MISMATCH`/TAMPERED ownership. **8/8 sealed members are now contract-ready** - contract-ready, not implemented.
- **Stage 4E-R9-R2 - Sealed record semantic values foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R9-R2-CLOSE with CI green on Ubuntu + Windows. Tests-first from clean parents: `ActorRole` (`police`/`thief`), `Intent` (`truth`/`lie`) and `SealedState` implemented together in `app.sealed_record_values`, the home R9-R1 chose on dependency grounds. Exact composed types, `step >= 1` with `bool` rejected, barriers already sorted and duplicate-free and never silently repaired, no opponent truth representable, and no serialization, canonicalization or hashing. Runtime `ROLE`/`VALID_ROLES` and the PRD-01 score keys are untouched and provably distinct from the sealed vocabulary. 1116/1111 tests, 100% coverage, and **0 historical test modifications**. **A slice close, not a stage close:** the codec itself is still unwritten.
- **Stage 4E-R9-RESUME - Canonical commitment codec + recompute foundation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R9-RESUME-CLOSE with CI green on Ubuntu + Windows. The deterministic path exists for the first time: semantic inputs → the exact eight-field sealed record → canonical JSON → UTF-8 bytes → unkeyed SHA-256 → `Sha256Digest`, in the architecture-frozen `protocol.canonical` and `protocol.commitment`. Three supervisor-supplied known-answer vectors - NFC, Hebrew UTF-8/STAY and barrier - were verified **independently before production existed** and match exactly in both repositories. One `compute_commitment` primitive serves both commit and later recomputation; digest inequality is a plain `bool`, never a verdict. 1258/1253 tests, 100% coverage, 0 committed-test edits. **A slice close, not a stage close:** no peer-visible family was added.
- **Stage 4E-R10 - Final audit + move validation readiness reconciliation.** Attempted and **stopped `INVENTORY-CONTRADICTION` before any documentation change; 0 files changed**, and the supervising review accepted the stop. Neither payload could be frozen: the source does not specify a transmitted `FinalAudit` verdict, and `MoveValidation` stays `BLOCKED-BY-PAYLOAD-SHAPE`. Resolved by 4E-R10-R1.
- **Stage 4E-R10-R1 - Final-audit inventory + audit-material exchange reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R10-R1-CLOSE with CI green on Ubuntu + Windows. Documentation / architecture only, **0 Python**. Separates the implemented `FinalNonceReveal`, the source-required audit-material/full-log disclosure and the local `FinalAuditVerdict`; rules out a peer-visible `FinalAudit` verdict family on source minimality without claiming prohibition; keeps `ProtocolPhase.FINAL_AUDIT`, timeline event 12 and the 15-event count; opens `AUDIT-EXCHANGE-PAYLOAD: BLOCKED-BY-INTEROPERABILITY-SHAPE` as an artifact/transport blocker; corrects the peer-visible inventory **10 → 9** with matrix **4/0/5**; and records official **Conflict-Register C-11**.
- **Stage 4E-R10-R2 - MoveValidation existence + payload reconciliation.** Attempted and **stopped `BLOCKED-BY-EXISTENCE-EVIDENCE` at the existence gate; 0 files changed**, with the stop accepted by supervising review. Opponent rejection of an illegal move is source-entailed, but the source does not say whether it is a standalone message, a same-call response or a local refusal. No payload was invented and no second inventory contradiction was declared. Resolved by 4E-R10-R3.
- **Stage 4E-R10-R3 - Move-rejection inventory + transport-response reconciliation.** Reviewed **PARTIAL** and committed at Stage 4E-R10-R3-CLOSE with CI green on Ubuntu + Windows. Documentation / architecture only, **0 Python**. Supervising decision places the peer-facing legality rejection at the **transport/port response boundary** rather than in a peer-message family; `domain.rules`/`LocalTurnService` remain the sole legality authority via the existing `GameRulesPort`, and `E-PROTO-ILLEGAL-MOVE` already owns the outcome. Freezes the four-way separation of delivery, authentication, protocol-order and game-legality acceptance. Inventory corrected **9 → 8**, matrix **4/0/4**, **C-12** recorded. **Intentionally still blocked:** `MOVE-REJECTION-TRANSPORT-SHAPE`, because concrete operation signatures are deferred to Stage 2B-2C and both peer ports are async.
- **Stage 4E-R11 - Peer operation + transport contract reconciliation.** Reviewed **PARTIAL** and committed at Stage 4E-R11-CLOSE with CI green on Ubuntu + Windows. Documentation / architecture only, **0 Python**. Freezes the logical peer operation layer as O1-O7: request/response semantics (async is I/O, not message shape), operation result/error separation, the PRD02-FR-033 operation inventory with the reference tool names as compatibility aliases, stable group ingress with local role routing, the move-rejection `bool` contract, the finalized-log / no-second-audit-schema rule, and the operation-vs-FastMCP binding boundary. **`MOVE-REJECTION-TRANSPORT-SHAPE` is RESOLVED and READY-TO-IMPLEMENT**; **`AUDIT-EXCHANGE-PAYLOAD` is narrowed to `BLOCKED-BY-LOG-ARTIFACT-SHAPE`**, its one remaining cause being the LOCAL-ONLY classification of the persistent log document and its unfrozen ack/reveal nesting.
- **Stage 4E-R11-R1 - Log artifact interoperability + audit exchange reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4E-R11-R1-CLOSE with CI green on Ubuntu + Windows. Documentation / contract only, **0 Python**. Amends JDEC-007 in place so the finalized per-sub-game log's audit-disclosure core is interoperable at final audit while logger mechanics, artifact metadata and locally-derived verification annotations stay local; freezes the one-schema lifecycle, the shared/local field boundary, the separate-event ack/reveal representation and the JSON-native `submit_audit` payload; proves audit completeness without a tenth log field. A CLOSE-time guard tightened the payload boundary so local artifact metadata creates no optional wire semantics. **Both integration blockers are resolved and `INTEROPERABILITY_BLOCKERS` reads Blocking items: None.**
- **Stage 4E-R12 - Step-0 + config negotiation + config lock readiness reconciliation.** Complete across six stages (R12, R12-FIX, R12-R1, R12-R2, R12-R3) and **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R12-CLOSE; documentation only, **0 Python, 0 tests**, 20 paths per repo. **Step-0, Config negotiation and Config lock are READY-TO-IMPLEMENT**; peer families **8 = 4/3/1**. The route was not straight: R12 marked Step-0 READY too early and edited unauthorized tracking paths; R12-FIX restored them and withdrew the claim; R12-R1 proved `token_usage_locked` a misplaced runtime datum and recomputed **FIELD_MATRIX to 74 = 15/39/9/11**; R12-R2 corrected an overclaim that `result_sha256` satisfied the runtime token-locking MUST, leaving **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`** explicitly open; R12-R3 closed the token-cap chronology with MODEL A (**PRE-STEP0-AGREED / SERIES-WIDE / IMMUTABLE-AFTER-STEP0**, still source-NEGOTIABLE) and repaired a stale NDEC-005 exclusion. Integration blockers remain **None**.
- **Stage 4E-R13 - ResultAgreement payload + result artifact reconciliation.** Complete across four stages (R13, R13-R1, R13-R2) and **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R13-CLOSE; documentation only, **0 Python, 0 tests**, 14 paths per repo. **ResultAgreement is READY-TO-IMPLEMENT; peer families 8 = 4 implemented / 4 ready / 0 blocked.** R13 resolved the payload shape and repaired the stale `result (13)` prose, but stopped PARTIAL on the joint-derivability defect; R13-R1 made the participant-owned core members `{group_a, group_b}` objects and introduced `ResultContribution` + the `Sha256Digest` operation response; R13-R2 carried the common `UtcTimestamp` in the request and fixed the deterministic two-request order, correcting the proposer rule to the byte-wise lower `group_id` **value** rather than the `group_a` slot. Integration blockers remain **None**; FIELD_MATRIX **74 = 15/39/9/11**.
- **Stage 4E-R14 - Pregame peer semantic types implementation.** Complete across five stages (R14, R14-R1, R14-R1-FIX, R14-R1-FIX2, R14-R2) and **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R14-CLOSE; 35 paths per repo. R14 stopped **BLOCKED-BEFORE-CODE with 0 files** on two missing contract dependencies; R14-R1 and its two fixes froze the `Declaration` lifecycle, the ten closed profile vocabularies, the shared cross-artifact primitives and the final hardware types; **R14-R2 implemented the pregame semantic value layer** with 9 new modules, 313 new tests per repo and 100.00% statement and branch coverage, every production file ≤150 LOC. **The protocol runtime was deliberately not implemented** - no FastMCP, transport, key loading, HMAC/Ed25519 computation or orchestration - so Step-0, config negotiation and config lock remain READY rather than operationally implemented.
- **Stage 4E-R15 - ResultAgreement semantic types implementation.** Complete in a single pass and **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R15-CLOSE; 18 paths per repo. Implements the result **semantic value layer** - the two participant-scoped shapes, the contribution entry and aggregate, and `ResultAgreement` itself - with the six-entry, game-fixed-commit and declaration-join invariants enforced at construction. 96 new tests per repo, totals 1667 / 1662 at 100.00% statement and branch coverage. **The protocol runtime was deliberately not implemented** - no proposer, cadence, approval-core assembly, `result_sha256`, digest comparison, transport or reporting - so `ResultAgreement` remains operationally READY.
- **Stage 4E-R16 - Peer application protocol runtime implementation.** Next authorized; **not started**. Turns the semantic values of the four READY families into executable operations and state-machine behaviour: Step-0 core projection and keyed auth, config proposal/lock cadence and `config_sha256`, the ResultAgreement two-request cadence, `RESULT_APPROVAL_CORE` assembly, `result_sha256` and `mutual_agreement` gating. No FastMCP adapter, tunnel, network transport, Gmail, GUI or token-accounting crypto. That mandatory security slice remains required before Stage 4 can close.
- **Stage 2 - Game logic & movement legality.** Pending implementation (PRD-01, locked).
- **Stage 3 - Local FastMCP protocol.** Pending (PRD-02).
- **Stage 4 - Baseline strategy.** Pending (PRD-03).
- **Stage 5 - Language & scent.** Pending (PRD-04).
- **Stage 6 - Public network.** Pending (PRD-05).
- **Stage 7 - Security & cryptography.** Pending (PRD-06).
- **Stage 8 - Reporting, GUI, replay.** Pending (PRD-07).
- **Stage 9 - Submission delivery.** Pending.
