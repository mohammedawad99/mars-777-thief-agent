# Project Plan - group MaRs-777 (THIEF)

> **Status: DRAFT.**
> **Purpose:** Sequence the work from foundation to a compliant, competitive
> THIEF agent.
> **Authoritative source:** book v3.0.0 (`.project-spec/police_thief_p2p.pdf`);
> Appendix E (rules/sanctions); Appendix F (numeric values).
> **To extract later:** the concrete milestones and acceptance criteria implied
> by the book once the full 160-page extraction is done.
> **Note:** No requirement is approved merely because this file exists.

## Phases (provisional)

- **Stage 0A - Environment audit.** Complete.
- **Stage 0B - Local foundation.** Complete (incl. 0B.1 evidence audit).
- **Stage 0C - Private remote, reviewed initial commit + push, CI verified.** Complete.
- **Stage 1 - Specification baseline.** **Adopted — REVIEWED / APPROVED / LOCKED.**
  The common Stage-1 baseline (extraction, Appendix-E/F corrections, requirement
  traceability, conflict resolutions, four JSON contracts, cryptographic-authentication
  distinctions, reporting contract) was **synchronized into this repository** from the
  Police locked source commit `691280dc3219452eeff462c997714fd5bcbd9e55` after
  supervising review — **not independently re-extracted here** (see `SOURCES.md` →
  *Synchronization provenance*, and `DECISIONS.md` D15). Lives in `docs/spec/`.
  **This synchronization is itself pending supervising review before it is committed.**
  Implementation: **NOT STARTED**.
- **Stage 2 onwards (PRD/architecture)** begins only **after** this synchronization is
  reviewed. No substantive PRD work has begun.
- **Stage 2A - Architecture freeze + PRD blueprints.** Complete (documentation only): 21 architecture documents in `docs/architecture/` (system topology, dependency layering, state ownership, state machine, error model, 20 ports (18 at 2A; `SeriesLauncherPort` + `CompatibilityProfilePort` added at 2A-R2), data/privacy flow, concurrency, config, artifacts, strategy seam, LLM boundary, observability, 12 test layers, security threat model, four quality gates, traceability, red-team, sync matrix) + seven PRD **blueprints**. All 91 requirements architecture-mapped; 11 blocking red-team findings resolved.
- **Stage 2A-R - Lecturer reference audit.** Complete: read-only audit of `rmisegal/Game-P2P-Cop-Chase` @ `960499fd` (v3.0.0). Its bundled book PDF hash matches ours exactly. Reference classified **NON-BINDING**; its unkeyed `SHA256(terms|nonce)` "signature" does **not** satisfy the book's keyed pre-supplied-key requirement (book wins). Chatbot question pack produced.
- **Stage 2A-R2 - Final reconciliation.** Complete: attachment evidence AE-01…AE-04 recorded as **secondary provenance**; all chatbot items closed (zero pending). Compatibility profiles defined (STRICT / REFERENCE / ATTACHMENT). One authorized Stage-1 **project-contract** correction: **JDEC-014** — the result **references** the declaration instead of duplicating static metadata (four-artifact-set self-containment), so the field matrix moved **77 → 75** (result 13 → 11). No requirement, Appendix E/F value, filename, or conflict changed.
- **Stage 2A-CLOSE - Consistency sweep + commit.** Stale-baseline sweep, non-self-referential `result_sha256` audit, symmetry re-verification, then commit + push + CI.
- **Stage 2B - PRD-01…04 authored in full.** Complete and reviewed (documentation only).
- **Stage 2C - PRD-05…07 authored in full.** Complete and reviewed; all 91 requirements given exactly one primary owner (PRD-01 15 / PRD-02 11 / PRD-03 3 / PRD-04 10 / PRD-05 2 / PRD-06 14 / PRD-07 24 / EXTERNAL-SUBMISSION 12).
- **Stage 2-CLOSE - PASS.** Resolved CLOSE-F1 (series convention is negotiated protocol metadata, **not** a declaration field — matrix stays **75**) and CLOSE-F2 (declared MCP endpoint is a **stable group-level ingress**, 1/team, static whole-series). **All seven PRDs are APPROVED — PHASE 2 LOCKED.** **Implementation: NOT STARTED.**
- **Phase 3 - Deterministic core implementation. STARTED.** Begun after Phase-2 closure review; the phase is **not** complete.
- **Stage 3A - Deterministic domain foundation.** Complete and reviewed (**PASS**), then committed at Stage 3A-CLOSE with CI green. Tests-first: immutable `GridConfig` (the project grid minimum is enforced here, never in `Board`), immutable `Position`, immutable **policy-free** `Board` geometry with blocked cells, `Move` = N/S/E/W/STAY, stable `MOVE_ORDER` = (N, S, E, W, STAY), destination calculation, bounds/blocked legality, deterministic `legal_moves`, and typed `apply_move` failure with no mutation. Role-neutral and byte-identical in both repositories; no opponent truth, no I/O, no dependency added. **Foundation only** - barriers as an action, capture, terminal/survival, scoring and scent are **not** implemented.
- **Stage 3B - Deterministic game semantics.** Complete and reviewed (**PASS**), then committed at Stage 3B-CLOSE with CI green. Tests-first: barrier placement (own/cardinal cell, quota MINIMUM 14, irreversible, no removal API, never moves the actor), the three capture routes (same cell, barrier on the occupied cell, GAME-005 trapped by adjacency), terminal/survival evaluation from configuration, role-keyed scoring (capture 20/5, survival 5/10, technical loss 0/0 via Ch 3 + App E #48 and C-07, tie 2/2 at series scope only) and bounded deterministic scent physics in `Decimal`. Two implementation-discovered source anomalies are recorded with explicit provenance: **JDEC-015** (terminal threshold admissibility - a source *gap*) and **C-10** (scent state bound vs additive update - a source *contradiction*). No Appendix-F value, requirement count or artifact contract changed.
- **Stage 3C - Local application / turn orchestration foundation.** Complete and reviewed (**PASS**), then committed at Stage 3C-CLOSE with CI green. Tests-first: `LocalTruth` owning board, own position and completed steps; typed `MoveAction`/`BarrierAction` making one command structurally one action; a role-specific `LocalTurnService` (police move **or** barrier, thief move only, refused before any domain effect); atomic application delegating to the existing `apply_move`/`place_barrier`; and max-moves exhaustion from the validated `TurnLimits`. **Stage 3C-FIX1** removed a duplicated `barriers_placed` counter so barrier usage has one authoritative representation. The layer declares no terminal outcome, computes no score and runs no scent lifecycle. **PRD-02 moves to IN PROGRESS**; PRD-01 stays IN PROGRESS.
- **Stage 4A - Local protocol state machine foundation.** Complete and reviewed (**PASS**), then committed at Stage 4A-CLOSE with CI green. Tests-first: the frozen 18-phase graph (15 normal + 3 fault) with **31** legal directed edges, verified exhaustively over all **324** ordered pairs (31 legal / 293 illegal); `ProtocolMachine` owns **only** its phase - no step, sub-game or series cursor, no board and no game state; normal bootstrap is `ProtocolMachine.start()` at BOOT, with direct arbitrary-phase construction reserved as a trusted internal snapshot primitive. **Stage 4A-FIX1** reconciled the TECHNICAL_LOSS lifecycle: it ends the current sub-game and rejoins the existing SUBGAME_COMPLETE boundary, while TAMPERED and FAILED remain absorbing. The crypto-named phases carry no cryptography, no message bodies and no transport.
- **Stage 4B - Protocol event / transition evidence foundation.** Complete and reviewed (**PASS**), then committed at Stage 4B-CLOSE with CI green. Tests-first: `ProtocolMachine.advance(target)` now returns one immutable `TransitionResult(machine, evidence)`, where `TransitionEvidence` holds exactly `source_phase` + `target_phase` - the state machine's own share of R7 as fixed by `PROTOCOL_TIMELINE.md` event 10 ("phase transition"; persisted "—", hashed "—"). Richer per-state evidence stays with its owning subsystem and `infra.logger`. **No event enum was invented**: no source or architecture document freezes a transition-signal vocabulary, so the target phase remains the typed request and `_ALLOWED` remains the one graph. There is no evidence-less transition API and no compatibility shim. **Stage 4B-FIX1** made evidence **valid by construction** - the pair must be an edge of `_ALLOWED`, so 31 legal pairs are constructible while all 293 illegal ordered pairs and all 18 self-loops raise `IllegalTransitionError`; the replay corruption boundary moved forward to the constructor and negative replay coverage grew (duplicated and out-of-position records) instead of shrinking. FIX1 also reconciled a reported "153-line" file as a **cross-repo semantic-diff count**, not physical LOC (repository-wide max **150**, **0** over). Structural validity is **not** authenticity: no hash, signature, nonce, MAC, event id, timestamp or sequence number exists, and replay is **phase-path only**. **No** FastMCP, network I/O, tunnel, cryptography, logger/replay persistence, JSON artifact or commit/reveal transport.
- **Stage 4C - Local orchestrator / protocol guard foundation.** Complete and reviewed (**PASS**), then committed at Stage 4C-CLOSE with CI green. Tests-first: `LocalOrchestrator` composes a `ProtocolMachine` and owns the single live fact `STATE_OWNERSHIP.md` assigns it - the **current sub-game index** (lifetime SERIES) - alongside a read-only `SeriesConfig` projection. Phase legality is never re-implemented: every request goes through `ProtocolMachine.advance()`, the module holds **no adjacency table and makes no `TransitionEvidence` call**, and the emitted evidence is passed through unchanged. Exactly one branch is decided locally - `SUBGAME_COMPLETE -> READY` (continue, cursor +1) versus `-> SERIES_COMPLETE` (end, cursor unchanged) - because the cursor plus the fixed series length truthfully determine it, so no caller boolean is accepted. Two ownership conflicts were resolved without touching frozen architecture: `MODULE_BOUNDARIES.md`'s "turn cursor" yields to `STATE_OWNERSHIP.md` (the D17 precedent), and role alternation stays a PRD-05 / `SeriesLauncher` convention (PRD02-FR-011), never derived from an odd/even index. **Stage 4C-FIX1** corrected a binding numeric error caught **before commit**: the series length had been implemented as a floor (`>= 6`) after following stale wording in `CONFIG_CONTRACT.md` and the C-05 impact cell ("6 or the agreed higher"), but Appendix F lists `num_games` among its **FIXED** rows and defines FIXED as "binding, unchangeable; deviation disqualifies" - so 7 is as illegal as 5. `FIXED_NUM_GAMES` now enforces exact equality, the two stale sentences were narrowed to the already-locked App F / C-05 meaning, and no JDEC, conflict, requirement or register entry was added. A counted series is therefore exactly 6 sub-games with exactly five cursor increments and **no representable seventh**; technical loss reaches the same `SUBGAME_COMPLETE` boundary and leaves the cursor sequence identical. Normal bootstrap is `LocalOrchestrator.start()`; direct construction is a trusted snapshot/test primitive with **zero** production callers. **Not** implemented: score recording (still `app.orchestrator`'s per `STATE_OWNERSHIP.md`, pending truthful terminal facts), `LocalTurnService` integration, FastMCP/network I/O, commitment hashing, nonces, signatures, canonical payloads, artifact persistence, replay, strategy, GUI or reporting.
- **Stage 4D - Application port contracts foundation.** **Attempted and blocked before any code** (`PORT-DEPENDENCY-BLOCKER`); zero repository changes. Three internal contradictions made a truthful implementation impossible: `API_BOUNDARIES.md` states in its own header that **no Python signatures are fixed**, and a repository-wide search of every architecture and PRD document confirmed no `def`, `Protocol` or return annotation exists, so every method name would have been invented; the `app.ports` row allowed **stdlib typing only** while the same architecture defines ports over `Board`/`Move`/`Outcome`/`ScoreLine`; and this file still claimed 18 ports where the frozen table holds 20. A thin `ClockPort` exposing `monotonic()` was considered and refused, because that row also owns deadlines and timers whose machinery `CONCURRENCY_MODEL.md` assigns to the Watchdog, the Deadline timers and the executor. Nothing was guessed.
- **Stage 4D-R1 - Application port architecture reconciliation.** Complete and reviewed (**PASS**), then committed at Stage 4D-R1-CLOSE with CI green. Documentation only, no Python: the `app.ports` row now allows `stdlib typing` **+ type references to immutable `domain` value types** and still forbids `protocol`, `infra`, adapters, network/FastMCP libraries, `app` implementation modules and domain services with side effects — dependency inversion (D3) is aimed at concrete adapters, not at the domain model, and `app` already imports `domain`, so no edge is added to the layer DAG. `API_BOUNDARIES.md` gained a signature-freeze policy recording that any future Python signature is a **PROJECT-CONTRACT** (never source-, lecturer- or reference-mandated), that the reference tool names `negotiate`/`receive_turn`/`submit_audit`/`receive_control` are **LECTURER_REFERENCE_COMPATIBILITY** only, and that a port row is **not** an automatic instruction to create a Python `Protocol`. The port count is corrected to **20**. Readiness after R1: **0 READY**, 3 NEEDS-SEPARATE-DESIGN (`GameRulesPort`, `ScoringPort`, `ClockPort`), 5 BLOCKED-BY-FUTURE-TYPE, 10 LATER-PRD, 2 EXTERNAL-RUNTIME. See **D22**.
- **Stage 4E - Protocol semantic message contracts foundation.** **Attempted and blocked before any code**; zero repository changes. Two blockers: `MESSAGE-PHASE-DEPENDENCY-BLOCKER` — the single authoritative `ProtocolPhase` lives in `app.state_machine`, which `protocol.messages` (allowed deps `canonical, domain`) may not import, and duplicating or relocating it was forbidden; and `MESSAGE-ENVELOPE-CURSOR-BLOCKER` — PRD02-FR-044's `(sub_game, step, phase)` left step numbering undefined for pre-turn families (a repository-wide search finds no step-numbering statement anywhere), `sub_game` undefined at series-level Step-0, and `phase` undefined outright. The audit additionally corrected the peer-visible timeline inventory from the previously planned **9** to **10**: Event 8 "Move validation" transmits accept/reject and had been omitted. No signature, payload or sentinel was guessed.
- **Stage 4E-R1 - Semantic message architecture / turn-cursor reconciliation.** Documentation only, no Python. `app.peer_messages` is added as an application **contract** module for immutable internal peer-message values — proved rather than preferred, since the Turn Executor that consumes them is `app` and `app` may never import `protocol` — while `protocol.messages` becomes the wire ⇄ semantic mapper and keeps no state. **PRD02-FR-044 is narrowed** to turn-scoped messages carrying `(sub_game, step)`, with the phase check remaining receiver-side under FR-062/STATE-003, because FR-063 (the requirement that names the "cursor guard" and shares FR-044's R8 trace) lists only sub-game and step, and because a lockstep state-machine walk shows a transmitted phase is either redundant or not independently computable. `step` for turn-scoped messages is the **pending turn number** (`completed_steps + 1`; first gameplay step 1), and the cursor is a **projection** of existing owners, never a second state store. Both Stage-4E blockers are resolved and the family inventory is corrected to **10**. See **D23**.
- **Stage 4E-R1-FIX1 - Turn-cursor consistency / sub-game-control identity.** Documentation only, no Python. Supervising review found three current contracts still defining the cursor as `(sub_game, step, phase)` after FR-044 was narrowed — PRD-02 §8, `CONCURRENCY_MODEL.md` §2 guard 2 and `SECURITY_ARCHITECTURE.md` T3 — so all three now read `(sub_game, step)` plus a separate receiver-side phase-admissibility check against the one authoritative `ProtocolMachine` (FR-021/FR-062/STATE-003); FR-062 is untouched and state admissibility is not weakened. The `step` upper bound was reclassified as **contextual** rather than structural, because `max_moves` is per-sub-game locked configuration and is not a cursor field — structural validation is only `int` and `step >= 1`, while `sub_game` may stay `1..6` since `num_games` is globally FIXED. Sub-game-control identity (`Config negotiation`, `Config lock`) is explicit semantic context, never inferred from a `g<NN>` artifact filename. See **D23a**.
- **Stage 4F - Protocol value representation contracts foundation.** **Attempted and blocked before any code** (`VALUE-MODULE-HOME-BLOCKER`); zero repository changes. Nine representations were audited independently: seven were genuinely unresolved and deferred rather than guessed, while the two that were representation-ready — a SHA-256 digest and the final-audit verdict — had **no module home reachable by their consumers**, and duplicating the validator per layer or inventing a `types.py` were both refused.
- **Stage 4F-R1 - Shared protocol value architecture / representation reconciliation.** Documentation only, no Python. The blocker analysis was corrected as **too broad**: `infra.logger`, `infra.artifacts` and `infra.reporter` are mappers that serialize a structured record already reaching them through `app.ports`, so `infra` needs no new dependency and `DEPENDENCY_RULES.md` is unchanged. The genuine consumers — `app.peer_messages`, `protocol.commitment` and `protocol.config_lock` — are served by one inner contract module, **`app.protocol_values`**, with narrow type-reference access for exactly the two protocol producers, reusing the outer→inner pattern Stage 4E-R1 authorized for `protocol.messages`. `domain` was rejected (protocol semantics, not game concepts) and a generic `types.py` outright. The digest representation resolved to canonical **lowercase 64-hex as PROJECT-CONTRACT**: SHA-256 is SOURCE, hex is project-locked, 64 is entailed, and case was free because the sole `.hexdigest()` occurrence is inside a block labelled "the reference" — uppercase is **rejected, never normalized**, with any wire compatibility handled at `protocol.messages`. The final-audit verdict is the closed pair `Verified OK` / `TAMPERED`. `RESULT_CONTRACT.md`'s `mutual_agreement` scalar/object contradiction was corrected in favour of the bool its field table, scoring rule and example already define, preserving the approval-core exclusion semantics and the locked 11-field result count. See **D24**. Closed at Stage 4F-R1-CLOSE with CI green, together with **Stage 4F-R1-FIX1**, which made the two protocol producers' dependency a **runtime use** rather than annotation-only, widened `app.protocol_values` to the pure stdlib value-definition primitives (`typing`, `dataclasses`, `enum`) it actually needs, and corrected the category accounting to **2 ready / 6 deferred / 1 reconciled = 9**. A follow-up debt is tracked: `app.peer_messages` carries the same narrow "stdlib typing" wording and must be re-audited before Stage 4E resumes. See **D24a**.
- **Stage 4F — RESUME (two approved values).** Next authorized; **not started**. Implement only the SHA-256 digest representation and the final-audit verdict in `app.protocol_values`. The nine original value-blocker **categories** account exactly as **2 ready · 6 deferred · 1 reconciled without a dedicated value type** (result agreement, whose `mutual_agreement` is a bool beside the separate `result_sha256`); the six deferred categories expand to nine unresolved **sub-items**, since the auth category holds `auth_alg`/`key_id`/`auth_tag` and the nonce category holds atom and association. **No** hashing, signing, nonce generation, canonical-byte generation, wire serialization, FastMCP, transport or message classes.
- **Stage 2 - Game logic & movement legality.** Pending implementation (PRD-01, locked).
- **Stage 3 - Local FastMCP protocol.** Pending (PRD-02).
- **Stage 4 - Baseline strategy.** Pending (PRD-03).
- **Stage 5 - Language & scent.** Pending (PRD-04).
- **Stage 6 - Public network.** Pending (PRD-05).
- **Stage 7 - Security & cryptography.** Pending (PRD-06).
- **Stage 8 - Reporting, GUI, replay.** Pending (PRD-07).
- **Stage 9 - Submission delivery.** Pending.
