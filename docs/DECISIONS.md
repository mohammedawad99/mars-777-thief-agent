# Decisions Log - group MaRs-777

> **Status: DRAFT.** Records approved decisions with rationale.

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | Develop on WSL2 native ext4 | Correct LF handling + performance; avoids /mnt/c and cloud-sync issues |
| D2 | Parent directory is NOT a Git repository | Prevents a super-repo nesting both agents |
| D3 | Two fully independent repositories | Enforces role isolation; no shared live state |
| D4 | Lowercase repository slugs | Filesystem-safe; group code `MaRs-777` preserved in metadata/docs |
| D5 | Exact group code `MaRs-777` preserved | Required identity; case-sensitive |
| D6 | Per-repository uv environments (`.venv`) | No shared interpreter or dependencies between agents |
| D7 | LF normalization via `.gitattributes` | Byte-identical canonical JSON hashes across OSes |
| D8 | Static local PDF copy under `.project-spec/` is git-ignored | Authoritative reference without committing a binary |
| D9 | No remote creation before review | Controlled delivery |
| D10 | No sibling access during implementation | Prevents strategy / state leakage |
| D11 | Both repositories created as PRIVATE under mohammedawad99 | Controlled academic delivery |
| D12 | Git transport: **initially HTTPS** (gh-authenticated) during foundation setup; **migrated to SSH in Stage 0C.2** after the OAuth token proved unable to obtain the `workflow` scope. **Current transport = SSH** (`git@github.com:mohammedawad99/mars-777-thief-agent.git`); authentication verified as **mohammedawad99** | HTTPS matched the originally verified auth and avoided embedded tokens; the SSH migration was required to push workflow files under the available scopes. No credential or key material is stored in the repository under either transport. |
| D13 | Public visibility / lecturer access deferred | To be handled in a later reviewed stage |
| D14 | Branch protection / rulesets deferred | Until CI check names are known from the first run |
| D15 | Adopt the reviewed COMMON Stage-1 specification baseline by **synchronization** from the Police repository (`mohammedawad99/mars-777-police-agent`), locked source commit `691280dc3219452eeff462c997714fd5bcbd9e55`, rather than re-extracting it here | Guarantees the common contractual facts (requirements, Appendix E/F counts, JSON contracts, field matrix, JDEC/NDEC/INV registers, conflicts, crypto taxonomy) **cannot drift** between the two agents. Stages 1A–1D.1 were executed and reviewed in Police; this repository adopted the reviewed result. Documentation only — no code, runtime, dependency, CI, credential, or Git history was shared. The **book remains authoritative**; the Police commit is the reviewed extraction baseline, not a replacement for it. See `SOURCES.md` → *Synchronization provenance*. |
| D16 | **JDEC-015 — terminal threshold admissibility.** A counted configuration must satisfy `survival_threshold <= max_moves`; a violating configuration is refused before `CONFIG_LOCKED` | Appendix F Table 15 fixes only two independent MINIMUM-35 floors, and Chapter 3 Table 2 defines no end event for a survival threshold the step ceiling can never reach. Discovered while implementing Stage 3B. Refusing the configuration preserves every source-defined terminal instead of inventing a fourth outcome. PROJECT-CONTRACT only: no Appendix-F row, no requirement, no numeric minimum and no scoring/tie/technical-loss rule changes. |
| D17 | **C-10 — scent state bound wins over the literal additive update.** Implemented evolution saturates: `τ_next = min(0.9, max(0, (1−ρ)·τ + ∆τ))`, and every `ScentField` cell is validated into `[0, 0.9]` at construction | Ch 4 §4.3 defines τ as a continuous value in [0, 0.9] while writing the update with a lower clamp only, so literal re-emission (0.81 + 0.9 = 1.71) breaks the stated domain. Discovered while implementing Stage 3B. The explicit state domain wins; Figure 5 only corroborates. SOURCE-CONFLICT resolved by documented project interpretation — no Appendix-F value, requirement, JDEC or config field changes. |
| D18 | **"Own barrier budget" is a derived read-model, not stored state.** `LocalTruth` holds exactly `board`, `own_position`, `completed_steps`; any remaining barrier budget is computed as `max_barriers - len(board.blocked)` | `MODULE_BOUNDARIES.md` names "own barrier budget" as a `domain.truth` *responsibility*, but `STATE_OWNERSHIP.md` - the authority for mutable-state ownership - lists only two rows owned by `domain.truth` (turn/step and own position) and assigns the barrier set to `domain.barriers` as PUBLIC, with anti-duplication rule 2 requiring barriers to exist **once**. No static-obstacle category exists in the locked model, so `len(board.blocked)` is exactly the police placement count and `is_placeable` already enforces the quota against it. A stored counter would be a second live representation that could drift. Found at Stage 3C review, corrected at Stage 3C-FIX1. No architecture document was modified. |
| D19 | **TECHNICAL_LOSS ends the current sub-game, not the machine** (`STATE_MACHINE.md` §4). One edge added: `TECHNICAL_LOSS -> SUBGAME_COMPLETE`. Normal state-machine bootstrap is `ProtocolMachine.start()` at BOOT; direct arbitrary-phase construction is a trusted internal snapshot primitive and must never be driven by untrusted input | The frozen table simultaneously made "technical loss" an entry condition of SUBGAME_COMPLETE, told TECHNICAL_LOSS to "proceed per series rules", excluded it from R5 (which names only TAMPERED and FAILED as never returning to play), and yet listed no successor. Ch 3 Table 2 scores technical loss 0/0 as a **sub-game** end event beside capture and survival, and a series is 6 FIXED sub-games. Implementation-discovered architecture correction found at Stage 4A review; TAMPERED and FAILED stay absorbing and are unchanged. No requirement, PRD, JDEC, NDEC, INV or Conflict-Register entry was added. |
| D20 | **Transition evidence = phase pair only, valid by construction.** `TransitionEvidence` holds exactly `source_phase` + `target_phase`, and construction requires `target_phase in _ALLOWED[source_phase]`; `ProtocolMachine.advance()` is the only transition path and returns one `TransitionResult(machine, evidence)`. **No event enum, no second adjacency table, no persistence and no cryptography.** Structural validity is explicitly **not** authenticity | `STATE_MACHINE.md` R7 requires an evidence record per transition, and `PROTOCOL_TIMELINE.md` event 10 fixes the *state machine's* share as literally a "phase transition" with persistence and hashing both "—" — the richer per-state Evidence entries (artifacts, hashes, log lines) belong to their owning subsystems and `infra.logger` (`API_BOUNDARIES.md` LoggerPort). No source or architecture document freezes a transition-signal vocabulary, so a 31-value event enum would only duplicate `_ALLOWED`. R7 records a **successful** transition and R1/R5 forbid the skipped and post-terminal steps, so a value naming a non-edge could never be truthful: 31 legal pairs construct, 293 illegal pairs and 18 self-loops raise `IllegalTransitionError`. Found at Stage 4B review, corrected at Stage 4B-FIX1. Replay is **phase-path only** — not game-state, movement, barrier, score, scent, commitment, nonce, network-message, artifact or complete-game replay. No requirement, PRD, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry was added or changed. |
| D21 | **The counted series is exactly 6 sub-games (`num_games` FIXED), and the orchestrator owns only the current sub-game cursor in this slice.** `FIXED_NUM_GAMES = 6` with equality validation in `SeriesConfig`; `LocalOrchestrator` holds `machine` + read-only `series` + `sub_game`, delegates all phase legality to `ProtocolMachine.advance()` and passes its evidence through unchanged; the cursor advances only on `SUBGAME_COMPLETE -> READY` | Appendix F lists `num_games` among its **FIXED** rows and defines FIXED as "binding, unchangeable; deviation disqualifies", so a value **above** 6 is refused just like one below — Stage 4C had wrongly implemented a floor (`>= 6`) by following stale derived wording in `CONFIG_CONTRACT.md` and the C-05 impact cell ("6 or the agreed higher"); both sentences were narrowed at Stage 4C-FIX1 to the meaning App F and C-05 already locked, before any commit. Ownership follows `STATE_OWNERSHIP.md` ("Current sub-game index | app.orchestrator"), which outranks `MODULE_BOUNDARIES.md`'s "turn cursor" wording under the D17 precedent; role alternation stays a PRD-05/`SeriesLauncher` convention (PRD02-FR-011) and is never derived from the index. The same table also assigns the **recorded score** to `app.orchestrator`; that is **deferred**, not denied, because it needs a truthful terminal `Outcome`. No requirement, PRD, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry was added or changed. |
| D22 | **`app.ports` may name immutable `domain` value types; port rows are not automatic Protocol mandates; any future Python signature is PROJECT-CONTRACT.** Implementation-discovered internal architecture reconciliation (Stage 4D-R1): the `app.ports` row in `MODULE_BOUNDARIES.md` now allows `stdlib typing` **+ type references to immutable domain value types**, and still forbids `protocol`, `infra`, adapters, network/FastMCP libraries, `app` implementation modules and domain services with side effects. `API_BOUNDARIES.md` gains a signature-freeze policy note. The frozen inventory is **20 ports** (18 at Stage 2A + `SeriesLauncherPort` and `CompatibilityProfilePort` at 2A-R2); the stale "18 ports" count in `PLAN.md` is corrected | Stage 4D attempted to implement the port contracts and was stopped before writing code, because three internal contradictions made a truthful implementation impossible: (a) `API_BOUNDARIES.md` states plainly "**No Python signatures are fixed here**", and a repository-wide search of `docs/architecture/` and `docs/prd/` finds no `def`, `Protocol` or return annotation, so every method name would have been invented; (b) the `app.ports` row allowed **stdlib typing only** while the same architecture defines `GameRulesPort` over board/action/config and `ScoringPort` over outcome → per-role scores, so those ports were unrepresentable in the module assigned to them without duplicating domain types or erasing them behind `Any`/`object`; (c) `PLAN.md` still claimed 18 ports. Only (b) needed a rule change, and only one cell of one row: dependency inversion (D3) is aimed at **concrete adapters**, not at the domain model, and `app` may already import `domain` (`app.orchestrator`, `app.turn_service`), so a type-only reference adds no edge to the layer DAG. The rejected alternatives were generics over type parameters (hides the exact semantics a port exists to fix, and defeats the per-port contract tests of `TEST_ARCHITECTURE.md`) and duplicate structural shapes inside `app.ports` (a second semantic representation of `Board`/`Outcome`/`ScoreLine`, breaking `STATE_OWNERSHIP.md`'s one-owner rule). **No lecturer-source conflict exists here** — this is internal project architecture. No requirement, PRD, Appendix-F row, JDEC, NDEC, INV or Conflict-Register entry was added or changed, and the 20-row port table itself was not rewritten. |
| D23 | **Internal semantic peer-messages live in the application layer (`app.peer_messages`); `protocol.messages` is the wire mapper; the transmitted turn cursor is `(sub_game, step)` and `phase` stays a receiver-side check.** Implementation-discovered internal reconciliation (Stage 4E-R1) after Stage 4E was blocked before code. Also corrected: the peer-visible timeline inventory is **10**, not the 9 previously planned | Three findings. **(1) Home.** Stage 4E targeted `protocol.messages`, but that row is a wire boundary ("wire schema validation", failure "malformed JSON") while `CONCURRENCY_MODEL.md` has the MCP server "validate, convert to an **event**, and submit it to the executor queue" — and the Turn Executor is `app`. Because `app` may never import `protocol`, a semantic value the executor consumes **cannot** live in `protocol`. That is a proof, so `app.peer_messages` was added as an application **contract** module beside `app.ports` / `app.strategy_api`, and `protocol.messages` now maps wire bytes ⇄ those values. **(2) Cursor.** PRD02-FR-044 said every inbound message carries `(sub_game, step, phase)`, which left `step` undefined for pre-turn families (no document anywhere defines step numbering) and `phase` undefined entirely. Internal evidence resolves it: **FR-063**, the requirement that names the "cursor guard" and traces to R8 exactly as FR-044 does, lists only **sub-game and step**; the phase rejection is **FR-062** under **STATE-003**, a receiver-side check against the receiver's own machine. A state-machine walk confirms a transmitted phase would be either redundant with the message family (sender phase is constant per family) or not independently computable (turns are lockstep, so peers occupy different phases simultaneously). FR-044 was therefore narrowed to turn-scoped messages carrying `(sub_game, step)`. This also dissolves the `MESSAGE-PHASE-DEPENDENCY-BLOCKER`: no phase value is transmitted, so no message type needs `ProtocolPhase`, and the single authoritative representation in `app.state_machine` is untouched — no second enum, no `str`/`Literal`/`int` phase. **(3) Inventory.** Event 8 "Move validation" transmits `accept/reject` peer-to-peer and had been omitted, so the corrected count is 10. `step` for turn-scoped messages is defined as the **pending turn number** (`completed_steps + 1`, first gameplay step 1), a PROJECT-CONTRACT: commitment precedes effect application (R4), so the whole commit/ack/reveal/validate cycle shares one step, retries reuse it (R8), and the turn cursor remains a **projection** of `app.orchestrator`'s sub-game index and `domain.truth.completed_steps`, never a second owner. No requirement count, PRD ID, Appendix-F value, JDEC, NDEC, INV or Conflict-Register entry changes. |
| D23a *(4E-R1-FIX1)* | **Turn-cursor consistency addendum to D23.** (a) The turn cursor is `(sub_game, step)` in **every** current contract: PRD-02 §8, `CONCURRENCY_MODEL.md` §2 guard 2 and `SECURITY_ARCHITECTURE.md` T3 previously still said `(sub_game, step, phase)` and were corrected; phase admissibility is receiver-side (FR-021/FR-062/STATE-003) against the one authoritative `ProtocolMachine`. (b) **`step` bounds are split**: *structural* is a strict `int` with `step >= 1` (bool rejected); the upper bound `step <= max_moves` is **contextual**, because `max_moves` is per-sub-game locked configuration, is not globally FIXED, and is not a cursor field — a future Gatekeeper/application check evaluates it against the authoritative locked config together with expected-step equality and duplicate/stale comparison. `sub_game` may stay structurally `1..6` because `num_games` **is** globally FIXED. Nothing hard-codes 35 and no second max-moves value exists. (c) **Sub-game-control identity is semantic, never artifact-derived**: `Config negotiation` and `Config lock` carry an explicit sub-game identity in the semantic message; the R1 matrix wording "implied by artifact `g<NN>`" wrongly coupled internal values to a filename and is withdrawn. Semantic identity is never inferred from a path, filename, wire route or FastMCP method name. (d) **Data association is not a live cursor**: `Final nonce reveal` may associate nonces with sub-games/steps without carrying a turn cursor; the collection shape stays unfrozen. (e) `app.peer_messages` responsibility is worded as values application control flow consumes **and produces**, since Step-0, config-control and finalization families are not solely Turn-Executor inputs; state owned remains **none**. No Python, no requirement ID, no register entry and no source rule changed. |
| D24 | **Shared protocol semantic values live in `app.protocol_values`; the internal SHA-256 digest representation is canonical lowercase 64-hex (PROJECT-CONTRACT); the final-audit verdict is the closed pair `Verified OK` / `TAMPERED`; and `mutual_agreement` is a bool beside a separate `result_sha256`.** Implementation-discovered internal reconciliation (Stage 4F-R1) after Stage 4F was blocked before code by `VALUE-MODULE-HOME-BLOCKER` | **(a) Home.** Stage 4F's blocker analysis was too broad: `infra.logger`, `infra.artifacts` and `infra.reporter` are **mappers**, not semantic consumers — they serialize a structured record that already reaches them through `app.ports`, so `infra` needs no new dependency and `DEPENDENCY_RULES.md` is unchanged. The genuine consumers are `app.peer_messages` (messages carry the digests), `protocol.commitment` (produces `H_commit`) and `protocol.config_lock` (produces `config_sha256`). One inner contract module, `app.protocol_values`, plus a narrow type-reference dependency for exactly those two protocol producers, satisfies all three — the same outer→inner pattern Stage 4E-R1 authorized for `protocol.messages` → `app.peer_messages`. `domain` was rejected as a home: a SHA-256 digest and a protocol audit verdict are protocol semantics, not game-domain concepts, and `protocol.config_lock` may import only `domain.config_model` anyway. A generic `types.py`/`common.py` was rejected outright. **(b) Digest.** SHA-256 is SOURCE (App E #17); hexadecimal is PROJECT-LOCKED (FIELD_MATRIX types the auth tags and the approval hash `string(hex)`); 64 characters is entailed by SHA-256 in hex, not invented. **Case is not fixed by any source or current project contract** — the single repository-wide `.hexdigest()` occurrence sits inside a block the canonicalization contract itself labels "The reference (Ch 5 p.53)", and JDEC-002 adopts the reference **serialization parameters**, not the digest's spelling. Because the source leaves that freedom, canonical **lowercase 64-hex** is adopted as an internal PROJECT-CONTRACT so one digest never has two spellings; uppercase is **rejected, never silently normalized**, and any future wire-compatibility normalization belongs at `protocol.messages`, not inside the value. **(c) Audit verdict.** `LOG_CONTRACT.md` freezes `audit.result` as a Required "string enum (`Verified OK`/`TAMPERED`)" (Ch 7 p.72–74) with the example `"result": "Verified OK"` — a closed two-state vocabulary, kept distinct from the Event-8 move-validation verdict, the Event-9 local verification record and `intent` classification. **(d) Result agreement.** `RESULT_CONTRACT.md` defined `mutual_agreement` as a **bool** in its field table, scoring rule and JSON example, while the approval-core prose alone treated it as an object with `.sha256`/`.confirmed` — an object form that also duplicated the separate top-level `result_sha256` inside itself and would have broken the locked 11-field result count. The stale prose was corrected; the exclusion semantics (neither the approval hash nor the agreement state may sit inside the bytes it approves) are unchanged. No new JDEC, NDEC, INV, Conflict-Register entry or source requirement; FIELD_MATRIX stays 75 = 16/39/9/11. |
| D24a *(4F-R1-FIX1)* | **Runtime-usability and readiness-accounting addendum to D24.** (a) `protocol.commitment` and `protocol.config_lock` **produce** the digest, so their `app.protocol_values` permission is **runtime use** (construct and return), not the annotation-only "type references" wording first copied from `app.ports`; no other application module becomes reachable and the outer→inner direction is unchanged. (b) `app.protocol_values` first allowed only "stdlib typing", which cannot define an immutable validated value at all; it now allows the pure stdlib **value-definition and validation primitives** (`typing`, `dataclasses`, `enum`) and nothing more, with `hashlib`, `hmac`, `cryptography`, `secrets`, `random`, I/O and all serialization explicitly forbidden. (c) The nine original Stage-4E value-blocker **categories** account as **2 READY** (digest, final-audit verdict), **6 DEFERRED** (declaration, full config, auth representations, acknowledgement, move-validation verdict, nonce representation + association) and **1 RECONCILED with no dedicated value type** (result agreement) = **9**; the earlier "2 ready + 7 deferred" double-counted result agreement as still deferred after D24 had already reconciled it, and mixed sub-fields into a category count. Sub-item detail is reported separately: the six deferred categories cover nine unresolved sub-items. Reported but not changed: `app.peer_messages` carries the same too-narrow "stdlib typing" phrasing and will need the same widening when Stage 4E resumes. No requirement, PRD, register, FIELD_MATRIX or source rule changes. |
| D25 | **Stage 4F implements exactly two shared protocol semantic values — `Sha256Digest` and `FinalAuditVerdict` — plus one supporting error, `InvalidDigestError(ValueError)`.** All three live in `app.protocol_values`, which imports only `dataclasses`, `enum` and `typing` and owns no state | `Sha256Digest` is a frozen, slotted, single-field value accepting a strict `str` (so `bool` and `str` subclasses are refused) of exactly 64 characters over `[0-9a-f]`. The provenance is kept in four separate layers: **SHA-256** is SOURCE-EXPLICIT (App E #17); **hex** is PROJECT-LOCKED (`FIELD_MATRIX.md` types the auth tags and approval hash `string(hex)`); **64 characters** is *entailed* by SHA-256 written in hex; and **lowercase is a PROJECT-CONTRACT only** — no source fixes the case, and the single repository `.hexdigest()` sits in a block `CANONICALIZATION_CONTRACT.md` itself labels "the reference". Uppercase is therefore **rejected, never normalised**, so one digest never has two spellings; any future peer-compatibility normalisation belongs at `protocol.messages`. It performs no hashing and carries no notion of which field it belongs to, so `"0" * 64` is structurally valid. `FinalAuditVerdict` is the closed `Verified OK` / `TAMPERED` pair `LOG_CONTRACT.md` freezes for `audit.result` (Ch 7 p.72-74), deliberately distinct from the Event-8 move-validation verdict, the Event-9 per-step `verified` record, intent classification, technical loss and `ProtocolPhase`. `InvalidDigestError` subclasses **`ValueError`** — malformed value construction, never stale input, a hash mismatch, a replay mismatch or a tampering verdict — so a caller may catch either the narrow type or the built-in category, matching `FinalAuditVerdict("OK")`; it is publicly exported but is **not** a third semantic value. The six deferred categories (declaration, full negotiated config, auth representations, acknowledgement shape, move-validation verdict, nonce representation + association) remain unimplemented, and result agreement still needs no dedicated type. No requirement, PRD, Appendix-F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry changes. |
| D26 | **`app.peer_messages` may use the pure stdlib value-definition primitives and `app.protocol_values` at runtime, but not `enum`; the turn cursor's home is `app.peer_messages`; and after re-auditing all ten peer-visible families exactly one — Commitment — is implementation-ready.** Implementation-discovered internal reconciliation (Stage 4E-R2), paying off the tracked Stage-4E preflight debt before any Stage-4E Python exists | **(a) Capability.** The `app.peer_messages` row allowed only "stdlib typing", which cannot define an immutable validated value at all — the identical defect Stage 4F-R1-FIX1 corrected for `app.protocol_values`. It now allows `typing` and `dataclasses` and nothing more of the stdlib. **`enum` is deliberately withheld**: no peer message defines a vocabulary of its own, and the two vocabularies messages will reference already exist elsewhere (`domain.rules.Move`, `app.protocol_values.FinalAuditVerdict`); granting `enum` now would invite exactly the duplicate vocabularies the anti-duplication rule forbids. It is added later only with evidence that a family needs a genuinely new closed set. **(b) Dependency.** The row could not name `app.protocol_values` at all, so a message could not carry `Sha256Digest` — the value Stage 4F-R1 created for it. That permission is **runtime use** (constructors accept and store the value), not the annotation-only wording borrowed from `app.ports`, because a frozen message dataclass really holds the object. It is intra-layer (both are `app` contract modules), so no edge is added to the layer DAG and D1 is untouched. **Acyclicity is structural, not conventional:** `app.protocol_values` admits only pure stdlib primitives, so it cannot import `app.peer_messages` under its own allow-list; D2 holds without a new rule and no shared layer is created. `protocol.messages` → `app.peer_messages` is unchanged; its own "type references only" phrasing carries the same defect and is **reported and tracked**, not fixed, because `protocol.messages` is not resuming — the same reason Stage 4F-R1-FIX1 gave for deferring this row. **(c) Turn cursor.** Home is `app.peer_messages`, not `app.protocol_values`: the latter exists for primitives *outer protocol implementations* must also agree on, and neither `protocol.commitment` (whose sealed record carries `step`, `role`, `sub_game` as separate fields per NDEC-001) nor `protocol.config_lock` (per sub-game, no step) needs a cursor, while PRD-02 §8 defines it precisely as "the transmitted identity of a **turn-scoped** semantic message". `app.orchestrator` may import `app.peer_messages` under the existing cross-cutting rule, so the expected-cursor comparison needs no new edge either. It owns **no state** and stays a projection of `app.orchestrator`'s sub-game index and `domain.truth`; structural validation is strict `int` with `bool` rejected, `1 <= sub_game <= 6` (`num_games` is globally FIXED) and `step >= 1`, with `step <= max_moves`, expected-step equality and duplicate/stale comparison all **contextual** (D23a(b)). No phase field. **(d) Readiness.** Against ten criteria, **1 of 10 families is READY** — Commitment, exactly `TurnCursor` + `Sha256Digest`, because PROTOCOL_TIMELINE event 5 transmits "`H_commit` **only**" and the cursor is independently required by PRD02-FR-044 and PRD06-FR-086 (which rejects a second commitment for an already-committed `(sub_game, step)`). Three blockers contradict this stage's own hypotheses and are recorded rather than smoothed over. **Reveal** is blocked by payload shape: PRD-01 defines a legal action as "a move in `move_set` **or** a police barrier placement" with one action per turn (FR-010), and BAR-001/BAR-002/FR-035 require every placement and its **exact location** to be declared openly and truthfully — yet event 7 and `LOG_CONTRACT.md` §D describe the reveal as `move` + `hint` only, so no contract says how the placement cell reaches the peer and a `(cursor, Move, hint)` record could not express a legal police turn. **Result agreement** is blocked because `INTEROPERABILITY_NEGOTIATION.md` NDEC-006 still carries the `mutual_agreement.sha256` / `.confirmed` object form Stage 4F-R1 withdrew from `RESULT_CONTRACT.md` (a residue that stage did not reach), and because that contract sets the bool **after** the exchange, so a single message cannot truthfully carry both its own `result_sha256` and an agreement verdict; the multiplicity of event 14 is therefore unfrozen. **Acknowledgement** had two of three sub-questions resolved — PRD06-FR-082 binds receipt of a *specific* `H_commit` for a *specific* `(sub_game, step)`, so the digest **is** carried and `ack_of_step` **is** the cursor's step rather than a second field — but stays blocked on whether the acking role (`LOG_CONTRACT.md` §C `by_role`, Required, SOURCE-SEMANTIC) is transmitted or attributed at persistence time. Step-0, config negotiation and config lock stay blocked on deferred semantic types; move validation on the deferred verdict representation; final nonce reveal on a nonce atom whose length PRD06-FR-066 leaves negotiable and an association shape marked project-contract/review-required; final audit on association shape, since "audit verdicts" has no frozen multiplicity or binding to the audited material. **`Sha256Digest` is explicitly NOT reused for `auth_tag`**: NDEC-005/007 permit an agreed asymmetric signature, so an auth tag is not a 64-hex digest. No message-kind enum is authorized — distinct dataclasses already discriminate. No new error type is needed. No Python, no requirement, PRD, Appendix-F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry changes; `docs/spec/**` was not edited. |
| D26a *(4E-R2-FIX1)* | **Current-contract and construction-error addendum to D26.** (a) NDEC-006 corrected to the live result model; (b) the same withdrawn object form found alive in **three PRD requirements**, reported not edited; (c) `TurnCursor` / `Commitment` static construction raises built-in `ValueError` with **zero** supporting error types | **(a) NDEC-006.** The cell still wrote `mutual_agreement.sha256` in the core-exclusion list and required "the identical `result_sha256` + `mutual_agreement.confirmed = true`" — the nested object form `RESULT_CONTRACT.md` withdrew at Stage 4F-R1 in favour of the bool its field table, scoring rule and JSON example define. It now states the **procedure** (both peers build the same `RESULT_APPROVAL_CORE`, each computes `result_sha256`, the digests are exchanged and compared, and **only once equal** does each local record carry the flag) and the **record shape** (`result_sha256` and `mutual_agreement` as **separate top-level fields**). No replacement nested object was invented; NDEC-006's purpose as the pre-match interoperability agreement for result approval is preserved; NDEC count stays **7**, FIELD_MATRIX **75**, result fields **11**, and C-09/INV-11 are untouched. **(b) Scope limit, added explicitly.** NDEC-006 freezes a *record* shape, never a *message* shape. It does **not** decide whether Event 14 is one message, request/response, offer + confirm, a digest echo or another pattern; it does not put `game_id`/`game_uid` in the semantic message; and it does not choose between `false` and absence-plus-`E-REPORT-DISAGREE`/C-09 for disagreement. **Family 14 therefore remains `BLOCKED-BY-PAYLOAD-SHAPE`**, now on message flow alone rather than on the stale data shape. **(c) The residue is wider than Stage 4E-R2 reported.** A mechanical sweep found the object form alive in three further **current** contracts: **PRD06-FR-142** (core exclusion list names `mutual_agreement.sha256`, `mutual_agreement.confirmed`), **PRD07-FR-085** ("`result_sha256` … is **stored in** `mutual_agreement.sha256`, with `confirmed` recording agreement state"), and **PRD07-FR-190** ("same `result_sha256` with `mutual_agreement.confirmed = true`"). FR-085 additionally contradicts **PRD07-FR-080** in the same table, which lists `mutual_agreement` and `result_sha256` as two separate top-level result fields. Stage 4E-R2-FIX1 was not authorized to modify PRDs, so all three are **reported and tracked**, not edited — which is why the stage closes **PARTIAL**: the object form is no longer live in `docs/spec/**`, but it is not yet extinct project-wide. Provenance, recorded so the future correction is safe: the nesting originates in `docs/reference/ATTACHMENT_EVIDENCE.md` ("`mutual_agreement` (as reported): `sha256`, `confirmed`") — the lecturer's extracted attachment example, **secondary provenance (AE-03)**, which **PRD07-FR-087** already states is "not a verified 1:1 parser schema". Ch 9 p.94 requires a SHA-256-backed mutual approval, never a nesting, so removing it discards no source requirement; `LECTURER_ATTACHMENT_COMPATIBILITY` remains the correct place for any grader-parser alignment. **(d) Construction errors.** `TurnCursor` and `Commitment` raise **built-in `ValueError`** for every malformed static construction, with **0 supporting error types**. Four options were audited. `TypeError`-for-wrong-type plus `ValueError`-for-bad-value was rejected: no value object in this project raises `TypeError` (`Position` raises `InvalidBoardError` through `require_int`, `Sha256Digest` raises `InvalidDigestError` for a non-`str`), it would force callers to catch two categories for one concept, and it makes `bool` ambiguous — `True` is an `int` by type yet a forbidden value. Reusing an existing error was rejected: `InvalidDigestError` would be a semantic lie for a malformed cursor, `ApplicationError`/`InvalidActionError`/`IllegalTransitionError`/`IllegalSubGameBranchError` are turn-service and lifecycle **behaviours** whose base is a bare `Exception` (so `except ValueError` would miss them, reintroducing exactly the inconsistency Stage 4F-RESUME-FIX1 removed), and `require_int` types its error parameter as `type[DomainError]`, which would force a domain error onto an application value. A new narrow module-local `ValueError` subclass was rejected as unnecessary public API: **`FinalAuditVerdict` is the in-project precedent** — a semantic value in the sibling contract module that raises **native** `ValueError`, asserted by its own test — and because `InvalidDigestError` is itself a `ValueError` **subclass**, a single `except ValueError` already catches malformed construction uniformly across `app.protocol_values` and `app.peer_messages`. The choice is also forward-safe in one direction only: adding a narrow subclass later is **non-breaking** (every raise is already a `ValueError`), whereas removing one would not be. No `ProtocolMessageError` is created merely because the module is named `peer_messages`, and no live-protocol outcome — `E-PROTO-STALE`, `E-HASH-MISMATCH`, `E-REPLAY-MISMATCH`, `TAMPERED`, `TECHNICAL_LOSS` — is used for malformed value construction. **(e) Composition never coerces.** `Commitment` requires already-validated semantic objects and rejects wrong composed types: a raw `str` (even a valid 64-hex one) is **not** auto-wrapped into `Sha256Digest`, and a tuple/dict/2-int pair is **not** coerced into `TurnCursor`. Each value keeps exactly one authoritative constructor, so a caller can never be unsure which layer validated a field. Readiness is unchanged at **1 READY / 9 BLOCKED**, the Stage-4E subset is unchanged at `TurnCursor` + `Commitment`, and the Reveal barrier-transmission gap, the Acknowledgement `by_role` gap, the Final-audit association gap and the `protocol.messages` "type references only" preflight debt are all preserved unmodified. No Python, no requirement, PRD, Appendix-F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry changes. |
| D26b *(4E-R2-FIX2)* | **Propagation and constant-dependency addendum to D26/D26a.** (a) The three active PRD requirements still asserting the withdrawn nested result-agreement form are corrected, so **no current project contract** uses it; (b) `app.peer_messages` is narrowly widened to read the two globally-FIXED `domain` constants `TurnCursor`'s structural bound needs | **(a) PRD propagation.** Stage 4E-R2-FIX1 corrected NDEC-006 and then found the same withdrawn shape alive in three further **current** contracts, which it was not authorized to edit — the reason it correctly returned PARTIAL. All three are now corrected narrowly and in place. **PRD06-FR-142** excludes "`result_sha256` itself, the `mutual_agreement` flag, `reported_by`, and any reporter-local presentation metadata"; the exclusion **semantics** are unchanged (neither the approval digest nor the agreement state may sit inside the bytes it approves), and the algorithm, canonicalization and approval-core field set are untouched. **PRD07-FR-085** now says the digest "is stored as a **separate top-level result field**, beside the **separate top-level** `mutual_agreement` bool", which resolves a contradiction **internal to PRD-07**: **FR-080**, four rows above in the same table, already listed `mutual_agreement` and `result_sha256` as two separate top-level result fields. **PRD07-FR-190** now requires both reports to carry the same `result_sha256` and record `mutual_agreement = true` as a separate top-level field **once the digest comparison establishes equality**. Every one of the three keeps its exact ID, modality, ownership, ordering and provenance line; no requirement was added, deleted, renumbered or reclassified, no acceptance criterion was invented to carry the correction, and no JDEC/NDEC/INV/Conflict entry was created. Each carries an explicit note that it is a **record** requirement: none of them defines an offer, confirm, response, echo, RPC method, wire key or FastMCP method. **A record shape is not a message shape**, so **Family 14 remains `BLOCKED-BY-PAYLOAD-SHAPE`** — on message multiplicity, on disagreement signalling (`false` versus absence plus `E-REPORT-DISAGREE`/C-09) and on message-layer identity — and no `ResultAgreement` value type is reintroduced. A one-message protocol is **not** inferred from the fact that a *record* contains `mutual_agreement = true`. **(b) Provenance, preserved not rewritten.** The nested form is not a project invention: `docs/reference/ATTACHMENT_EVIDENCE.md` records it as the shape of the lecturer's extracted attachment example ("`mutual_agreement` (as reported): `sha256`, `confirmed`"), secondary provenance (AE-03). That file is left untouched, and the claim that the attachment never used the nesting is **not** made. The existing compatibility conclusion stands — **PRD07-FR-087** already states the attachment example "is not a verified 1:1 parser schema" — so the project may use the internally coherent separate-field model without promoting attachment nesting to a source MUST. No PRD provenance annotation overstated the nesting as SOURCE-EXACT: FR-142 traces to JDEC-010/014 and FR-085 to PRD06-FR-140…144 (both internal), while FR-190's `SOURCE-MANDATORY (LEAGUE-002)` labels the *requirement to agree the result*, never a nesting — so no provenance annotation needed narrowing and no source modality changed. **(c) Constant dependency.** The `app.peer_messages` row permitted "immutable `domain` value **types**", which names classes. `TurnCursor`'s frozen bound `FIRST_SUB_GAME <= sub_game <= FIXED_NUM_GAMES` needs two module-level `Final[int]` **constants**, and reading "value types" to cover them would repeat exactly the generous reading refused at Stage 4D-R1 (`app.ports`) and Stage 4F-R1-FIX1 (`app.protocol_values`). The row therefore names them explicitly, read-only, and nothing else; the module may never redefine, shadow or re-export them. Alternatives rejected on evidence rather than taste: a **literal `6`** inside `app.peer_messages`, or **moving/copying** the constants, would create a **second numeric authority** for a value Appendix F marks FIXED with "deviation disqualifies" (C-05) — the precise duplication the anti-duplication rule exists to prevent; and deriving the bound from a **`SeriesConfig`** instance, though `SeriesConfig` is an already-permitted immutable value type, would either add a third field to a cursor frozen at exactly two or make a *transmitted* cursor appear to depend on locked configuration it does not carry, when the bound it needs is the global FIXED constant, not a config value. **Single authority is mechanically verified:** `FIXED_NUM_GAMES: Final[int] = 6` in `domain.config_model` is the **only** `= 6` series length anywhere in the source tree, `FIRST_SUB_GAME: Final[int] = 1` sits beside it, both are exported from `domain.__init__`, and `SeriesConfig` validates *against* `FIXED_NUM_GAMES` rather than restating it. **No new layer edge:** `app.orchestrator` already imports `FIRST_SUB_GAME` from `domain.config_model` at runtime, and `app` depends inward on `domain` under D1; no domain service, mutable state, truth ownership or rule execution becomes reachable, and nothing broadens to "all domain". **(d) Regression.** `TurnCursor` stays design-only with exactly `sub_game` and `step`, no phase, strict `int` with `bool` rejected, and every live check (max_moves, expected cursor, staleness, phase admissibility) outside the constructor; `Commitment` stays design-only and READY with exactly `cursor: TurnCursor` and one `Sha256Digest` digest field, no coercion of a raw `str` or a tuple/dict; both raise built-in **`ValueError`** with **0** supporting error types. Readiness stays **1 READY / 9 BLOCKED**, the subset stays `TurnCursor` + `Commitment`, and the Reveal barrier-transmission gap, the Acknowledgement `by_role` gap, the Final-audit association gap and the `protocol.messages` "type references only" debt are preserved unmodified. No Python; no requirement count, modality, PRD status, Appendix-E/F value, FIELD_MATRIX, JDEC, NDEC, INV or Conflict-Register change. |
| D27 | **Stage 4E's resumed slice implements exactly two types in `app.peer_messages` — the `TurnCursor` support contract and the `Commitment` peer-message family — with zero supporting error classes; nine families and Stage 4E as a whole remain incomplete** | **(a) Scope.** Stage 4E-R2 proved only one of the ten peer-visible families implementable, so exactly that one was built. `app.peer_messages` holds exactly `TurnCursor` and `Commitment` plus one module-private `_require_int` helper; there is no third class, no placeholder for a blocked family, and **no supporting error type** — the Stage 4E-R2-FIX1 decision that malformed static construction raises the built-in `ValueError` is honoured, so one `except ValueError` still covers every value in the application contract modules. **(b) `TurnCursor`.** Frozen, slotted, fields exactly `(sub_game, step)`, owning no state: it is a projection of `app.orchestrator`'s sub-game index and `domain.truth`, never a second owner. It carries **no phase** — the receiver already owns the single authoritative `ProtocolMachine` and checks admissibility against it (FR-021/FR-062/STATE-003) — and no `game_id`, `role`, `kind` or `max_moves`. Validation is **structural only**: `type(x) is int` exactly, which refuses `bool` because accepting `True` as sub-game 1 would be the silent coercion the project forbids; `FIRST_SUB_GAME <= sub_game <= FIXED_NUM_GAMES` **read from** `domain.config_model` under the Stage 4E-R2-FIX2 permission, so no second exact-six authority exists anywhere (the module contains no literal `6`); and `step >= 1` with **no upper bound**, proved by accepting `step = 10**9`, because `max_moves` is per-sub-game locked configuration that a transmitted cursor deliberately does not carry. Expected-cursor equality, staleness, duplicate detection, timeouts and config lock all stay LIVE and outside the constructor, and no `ProtocolMachine`, `LocalOrchestrator`, `SeriesConfig` instance or `LocalTruth` is imported. **(c) `Commitment`.** Fields exactly `(cursor, h_commit)` — `h_commit` being the PROJECT-CONTRACT Python spelling of the source field `H_commit`, with no uppercase duplicate and no alias. This matches `PROTOCOL_TIMELINE.md` event 5, which transmits "`H_commit` **only**", plus the cursor required independently by PRD02-FR-044 and PRD06-FR-086. Fifteen fields were tested absent — `game_id`, `game_uid`, `role`, `state`, `move`, `intent`, `hint`, `nonce`, `sealed_record`, `timestamp`, `message_id`, `phase`, `kind`, `accepted`, `verified` — because the sealed record's contents stay inside the digest and the hiding property of PRD06-FR-067 is exactly why none of them may travel beside it. **(d) Strict composition, never coercion.** Type checks use **exact identity** (`type(x) is …`) rather than `isinstance`, which is materially stronger and was proved adversarially: purpose-built `TurnCursor` and `Sha256Digest` subclasses are both **refused**, where `isinstance` would have accepted them and allowed a subclass to weaken validation. Nothing is converted: a **valid** lowercase 64-hex `str` raises rather than being wrapped, and a tuple/dict/list is never turned into a cursor, so each value keeps exactly one authoritative constructor and a caller can always tell which contract validated a field. `Commitment` never calls `Sha256Digest(...)`. **(e) No computation.** The module performs no hashing: an AST scan with docstrings stripped finds no executable `hashlib`, `hmac`, `cryptography`, `secrets`, `random`, `json`, `re`, `enum`, `sha256`, `digest` or `hexdigest` identifier, and its only imports are `dataclasses`, the two `domain.config_model` constants and `Sha256Digest`. It knows nothing of what was hashed, canonical bytes, the nonce or the sealed record. No message-kind enum exists — distinct dataclass types already discriminate the family — and the single `ProtocolPhase` definition is untouched. **(f) Surface.** `app/__init__.py` gains exactly `TurnCursor` and `Commitment` (22 → 24 exports, sorted, explicit, no wildcard); `FIRST_SUB_GAME` and `FIXED_NUM_GAMES` are **not** re-exported from `app`, the private helper is not exported, and `Sha256Digest` gains no second alias. **(g) What this does *not* claim.** Only one of ten families is implemented. The Acknowledgement `by_role` question, the Reveal barrier-placement transmission gap, the move-validation verdict, the nonce representation and association, the final-audit multiplicity and association, and the result-agreement message flow are all untouched and still blocking; `protocol.messages`' "type references only" debt remains tracked; and **Stage 4E as a whole is NOT complete**. No requirement, PRD, Appendix-F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX entry changes. |
| D28 *(4E-R3)* | **`by_role` is local log attribution, not transmitted content, so Acknowledgement is READY as exactly `cursor` + `h_commit`; and the sealed `move` is the generic physical *action* (barrier placement included), which resolves Reveal's source ambiguity while leaving it BLOCKED on the action's representation and home** | **(a) Method.** Both questions were re-derived from the **authoritative PDF**, not from our derived documents, because Stage 4E-R2 had recorded a Reveal blocker that turned out to be an artefact of our own wording. **(b) Acknowledgement content.** Ch 5 §5.3.2 (p.51, printed 35) gives the acknowledgement in full: *"היריב מאשר כי קיבל את ההתחייבות וכי הוא נעול עליה"* — the opponent confirms it **received the commitment** and is **locked onto it**. To confirm receipt *of a commitment* the message must identify which one, which is exactly what **PRD06-FR-082** already states (binds receipt of a specific `H_commit` for a specific `(sub_game, step)`), what `PROTOCOL_TIMELINE.md` event 6 transmits ("ack of step + commit") and what `LOG_CONTRACT.md` §C persists (`ack_of_step`, `ack_commit`). Figure 6 (p.52) shows the step as "Acknowledge (locked)" with no further payload. Nothing source-level adds a field, and none was invented — no `accepted`/`ok` bool, no `message_id`, no timestamp, no phase. **(c) `by_role` — the primary question — resolves to LOCAL-LOG-ATTRIBUTION.** The source names an acknowledging party (*"the opponent confirms"*), which is why `LOG_CONTRACT.md` §C marks `by_role` Required/SOURCE-SEMANTIC, but it nowhere states that the party is carried *inside* the payload; Figure 6 carries it as the message's **direction between two lifelines**. The premise that the sender identity is independently known was **proved, not assumed**: there is exactly one opponent process (ARCH-001/002 — two completely separate processes, no shared state), the role each side plays in a sub-game is **immutable from `CONFIG_LOCKED`** (PRD06-FR-048 freezes the agreed series convention), and the local writer trivially knows whether it *sent* or *received* a given acknowledgement. A security consequence worth recording: because role is never transmitted, **there is nothing for a hostile peer to forge** — the attribution is unforgeable by construction. The reference FastMCP snippet's `{"accepted": …}` return (p.28) is **NON-BINDING** example code for a different tool and was not promoted. **(d) Acknowledgement is READY-TO-IMPLEMENT** as exactly `cursor: TurnCursor` + `h_commit: Sha256Digest` — the same two already-frozen values `Commitment` uses, so **no new dependency, no new supporting error and no new module permission** is required. Structural: frozen/slotted, exact type identity, no coercion, built-in `ValueError`. LIVE and outside the constructor: expected phase, expected cursor, equality against the outstanding commitment, duplicate/stale detection, sender authentication, deadlines. **(e) `move` is the generic physical action.** Ch 5 §5.3.1 (p.51) defines the sealed `Move` as *"הפעולה הפיזית. המהלך הנבחר (תנועה, **הצבת חסם** וכדומה)"* — the physical **action**: movement, **barrier placement**, etc. — *"the core that is to be locked against change"*; §5.3.2 has Reveal send *"את הפעולה (Move) ואת המשפט המילולי"*, the **action** and the verbal sentence. Ch 3 §3.4 (p.37, printed 21) supplies the rest: a turn is a **single action**, the police may place a barrier only *"in a turn in which he forgoes movement"*, and the **declaration duty** is explicit — every placement **and its exact location** must be declared truthfully and never hidden (repeated in the Iron Rules, p.38). **Therefore a police barrier placement is an ordinary action carried by the same `move` slot, bound by the same `H_commit`, and no eleventh peer-visible family is needed or created** — the peer-visible inventory stays **10**. The reference `commit(state, move: str, intent)` snippet (p.53) is **EXAMPLE-ONLY**, already recorded as a simplification, and does not narrow `move` to a movement string. This corrects a real under-specification in our own derived contracts, which had modelled `move` as movement only. **(f) Reveal remains BLOCKED — on representation, not on semantics.** Two blockers, both proved. **(1) No shared action semantic type exists in a home reachable by both consumers.** The action is *produced* by the future `protocol.commitment` (it is sealed) and *carried* by `app.peer_messages` (it is revealed). Option A (reuse `app.turn_service.MoveAction`/`BarrierAction`) is rejected: `app.peer_messages` explicitly forbids `app.turn_service`, and those types raise `ApplicationError`, a bare `Exception`, contradicting the peer-message `ValueError` contract. Option C (`app.protocol_values`) is rejected: an action is a **game-domain** concept — PRD-01 defines the legal action space — and D24 rejected `domain` for digests on the mirror-image ground that digests are protocol semantics; putting an action there makes that module the junk drawer. Option D (a bare `Move | Position` union over existing primitives) is expressible with zero new types but is semantically weaker than the project's own structurally-exclusive named-action idiom and still leaves the canonical representation unfrozen. Option E (define it inside `app.peer_messages`) is rejected outright: `protocol.commitment` may not import `app.peer_messages`, so the producer could not reach it. Option F (optional `move` + optional `barrier_target` with XOR) is rejected: the project deliberately uses exclusive types and does not put cross-field rules in semantic constructors. **Option B — a domain-owned immutable action type — is the correct home**, reachable by `app.peer_messages` (already permitted "immutable `domain` value types"), by `protocol.commitment` (allowed `domain`) and by `app.turn_service`; but creating it requires new Python **and** refactoring `turn_service` onto it to avoid a second representation, and Stage 4E-R3 is documentation-only, so it is **recorded as a future requirement, not performed**. **(2) The sealed `move` representation is not frozen.** NDEC-001 fixes the field *set* and NDEC-002 fixes `state`'s representation, but nothing fixes how a movement token or a barrier target is written into the canonical bytes — so a revealed barrier cell could not be recomputed byte-identically against `H_commit`. NDEC-001 is **amended narrowly** to record both the generic-action semantics and the unfrozen representation; **no NDEC-008 was created and the count stays 7**. Reveal is therefore reclassified from `BLOCKED-BY-PAYLOAD-SHAPE` to the more exact **`BLOCKED-BY-VALUE-REPRESENTATION`**. **(g) Untouched.** The implemented `Commitment(cursor, h_commit)` peer message is **not** the sealed record and gains nothing — no move, barrier, action, target, hint or nonce. Hint semantics were audited and left as they stand: the source gives a verbal sentence with the negotiated `hint_max_words` cap, so no non-empty rule, minimum, character cap, alphabet or normalization was invented. The other eight families keep their classifications. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changes. |
| D29 *(4E-RESUME2)* | **`Acknowledgement` is implemented as the second peer-message family — exactly `cursor: TurnCursor` + `h_commit: Sha256Digest`, with zero supporting error classes and zero new dependencies — while eight families and Stage 4E as a whole remain incomplete** | **(a) Scope.** D28 (4E-R3) had already proved Acknowledgement READY-TO-IMPLEMENT and its content complete at two members; this decision records only the implementation of that frozen contract. Reveal was not touched, no shared action type was created, `app.turn_service` was not refactored, and no `protocol/`, wire schema, FastMCP integration or cryptographic computation was added. **(b) Contract.** Frozen, slotted, no `__dict__`; composition validated by **exact type identity**, so a `TurnCursor` subclass and a `Sha256Digest` subclass are both refused; **no coercion**, so a valid lowercase 64-hex `str` raises rather than being wrapped, keeping one authoritative constructor per value; malformed composition raises the built-in `ValueError`, the same category `Commitment` already uses, so no supporting error type was defined. **(c) The three absences, all inherited from D27 and mechanically tested.** `by_role` is **local log attribution** — the local writer derives it from the emitted/received direction plus the role mapping frozen at `CONFIG_LOCKED`, so it is never transmitted and therefore never forgeable, and duplicating that local truth inside the payload was refused. `ack_of_step` is the **persisted name** of `cursor.step` in `LOG_CONTRACT.md`, not a second message field. There is no `accepted`/`ok`/`valid`/`verified`/`success` flag: an acknowledgement is positive **by existing**, and a stale, duplicate or mismatched one is a LIVE rejection path, not an `Acknowledgement(..., accepted=False)`. **(d) Family discrimination without a tag.** Acknowledgement and Commitment carry the *same two component types* and are still different families. It is deliberately **not** an alias, not a subclass and not a parent of `Commitment`; the class identity alone discriminates them, so **no `MessageKind` enum was introduced** and the single `ProtocolPhase` authority stays in `app.state_machine`. Cross-class equality was tested so the two semantic kinds cannot collapse into one value. **(e) Structural vs LIVE.** The constructor validates composition only. Expected phase, expected cursor, equality against the outstanding `H_commit`, stale/duplicate detection, authenticated sender identity, local role attribution and deadlines are all LIVE and stay outside it; the module imports no protocol machine, orchestrator, clock, gatekeeper, transport or config. **(f) TDD provenance — recorded, not sanitized.** The first attempt was interrupted, leaving an unclean worktree that its own preflight should have stopped on, and its RED was *reconstructed* by restoring production to HEAD around a test file that attempt had already written. Supervising review refused that as sufficient evidence. **Stage 4E-RESUME2-FIX1 replayed the slice from the exact clean committed parents**: clean baseline 721/716, only the new test file created, production SHA-confirmed equal to HEAD at the moment of a genuine `ImportError` RED, production written only afterwards, 39 Ack tests GREEN, then the committed `test_peer_messages.py` observed failing **solely** on its now-stale blocked-family list, then one approved maintenance line replacing `Acknowledgement` with the still-blocked `ResultAgreement`, 73 GREEN, 112 targeted. The replay reproduced both production files byte-identically from the contract. That replay is the authoritative history. **(g) Unchanged.** `Commitment` and `TurnCursor` are byte-unchanged; Reveal remains **BLOCKED-BY-VALUE-REPRESENTATION**; peer-visible families remain **10**, of which **2** are implemented and **8** blocked; and no requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changed. |
| D30 *(4E-R4)* | **The chosen physical action gets one authoritative home — a new domain-owned `domain.actions` — and one frozen canonical encoding under the unchanged sealed `move` key: a tagged, structurally-exclusive `{kind,value}` object** | **(a) Method.** Both questions were re-derived from the **authoritative PDF**, not from our derived documents, for the reason Stage 4E-R3 recorded: a derived narrowing had already propagated once. Ch 5 §5.3.1 (p.51) defines the sealed `Move` as *"the physical action; the chosen action (movement, **barrier placement**, etc.)"* and calls it *"the core to be locked against change"*; §5.3.2 has Reveal send *"the action (Move) and the verbal sentence"* with the nonce withheld; Ch 3 §3.4 (p.37) gives **one single action per turn** — one orthogonal cell **or stay**, diagonals forbidden — and permits a police barrier only *"in a turn in which he forgoes movement"*, in a cell within one step, with the duty to declare **the exact location** truthfully and never in hiding; Ch 5 p.50 states the sealed record is *richer than the four fields shown*. **(b) The problem.** `move` means the generic action, but `domain.rules.Move` is only the five-token movement vocabulary, and `app.turn_service` holds a *second* action model (`ActionKind`/`MoveAction`/`BarrierAction`) that `app.peer_messages` and the future `protocol.commitment` are both forbidden to import. Two representations of one chosen action would be a duplicate truth the moment a commitment producer exists. **(c) Home — domain, confirmed with evidence.** The action is game-domain semantics (PRD-01 owns the legal action space), it exists before any transport, and `domain` is the only layer all three consumers may reach: `app.turn_service` (deps `domain, ports`), `app.peer_messages` (deps allow immutable `domain` value types) and `protocol.commitment` (deps `canonical, domain, app.protocol_values`), while `domain` imports none of them — so the graph stays acyclic by construction, not by convention. **(d) Module.** A **new** `domain.actions`, not `domain.rules`: `rules.py` is already 118 LOC and the added value types would breach the 150-line rule, and legality is a different responsibility from the action value. **(e) Type model.** Two frozen, slotted, structurally-exclusive dataclasses — `MoveAction(move: Move)` and `BarrierAction(target: Position)` — plus the alias `PhysicalAction = MoveAction | BarrierAction`. The existing names are **kept**, so the migration is a move rather than a rename; an optional-field model (`move` + `barrier_target` with an XOR rule) was rejected because the project uses exclusive types and does not put cross-field rules in semantic constructors. **`ActionKind` moves down to `domain.actions`** so the canonical tag has exactly one authority — a new vocabulary would have duplicated an existing concept. **(f) Structural vs LIVE.** The constructor checks only exact composed types with no coercion, raising `InvalidPhysicalActionError(DomainError)` — the established `Invalid<Value>Error` convention, named to avoid colliding with `app.turn_service.InvalidActionError`, which keeps its distinct meaning of a LIVE domain rejection. Role, the thief barrier prohibition, board extent, the within-one-step rule (Ch 3 p.37), occupancy, quota, current position, phase, cursor and locked config all stay **LIVE**: a domain value must not need a `Board` to be constructed. **(g) STAY.** `Move.STAY` is an ordinary movement action — the source lists staying put beside the four directions and treats a barrier turn as one where the police *forgoes movement*, so a barrier turn is **not** STAY, and the distinction stays explicit in the type. **(h) Canonical encoding.** The sealed key stays `move` and the sealed set stays **exactly eight fields** — no `action`, `barrier_target` or `action_kind` was added. Its *value* is now frozen as `{"kind":"MOVE","value":"<move_set token>"}` or `{"kind":"BARRIER","value":[row,col]}`. Movement reuses the FIXED `move_set` tokens verbatim; the barrier carries **the exact placed cell** as the `[row,col]` array already locked by JDEC-006/JDEC-012, so no new coordinate convention was invented. An untagged union distinguished only by JSON type (string vs array) would have worked today but relies on type-sniffing and leaves no room for the source's *"etc."*; a string form such as `"BARRIER:3,4"` was rejected as a second ad-hoc parser inside canonical JSON. The inner key is `value` rather than `move` deliberately, so the overloaded word does not appear at two nesting levels. **(i) Provenance, stated honestly.** The action *semantics* are SOURCE-REQUIRED; byte-identical recomputation is SOURCE-ENTAILED (p.50); the *shape* is **PROJECT-CONTRACT** — Ch 5 p.53's `commit(state, move: str, …)` is EXAMPLE-ONLY and the book labels that core a simplification — carried pre-match by the existing **NDEC-001** echo exactly as NDEC-002 carries `state`. **NDEC-001 is amended in place; the NDEC count stays 7** and no JDEC-016, INV-16 or C-11 was created. **(j) No codec identifier.** An `action_codec` field was considered and **rejected**: only one representation is permitted, both peers must use it, and it is already fixed by the NDEC-001 agreement referenced through `config_sha256` — a second identifier would be a second authority for the same fact. **(k) Commit/Reveal identity — stated semantically, not by object identity.** The invariant is that the **same semantic `PhysicalAction` maps to the same canonical action JSON value** under `sealed_record["move"]` when `H_commit` is computed and when Reveal is serialized — e.g. `MoveAction(Move.N)` → `{"kind":"MOVE","value":"N"}` and `BarrierAction(Position(r,c))` → `{"kind":"BARRIER","value":[r,c]}` — for STAY, for all four cardinal moves and for a barrier with its exact cell. Commit and Reveal **must not have separate action codecs**. Nothing here claims that two peer processes, or even the commit and reveal steps within one process, share a Python object: they share a semantics and a codec. **(l) Consequence.** Reveal moves from `BLOCKED-BY-VALUE-REPRESENTATION` to **`BLOCKED-BY-FUTURE-SEMANTIC-TYPE`** — its design is frozen and it waits only on the shared value existing in Python. The other nine families are deliberately left unchanged. Implemented peer-message families remain **2**; Stage 4E as a whole remains **NOT COMPLETE**. |
| D31 *(4E-R5)* | **The physical action now has exactly one definition, in `domain.actions`; `app.turn_service` consumes it through identity-equal re-exports; and malformed *domain construction* versus malformed *message composition* are deliberately owned by different layers — which makes Reveal READY-TO-IMPLEMENT** | **(a) Scope.** D29 (4E-R4) froze the design; this records its implementation and one supervising ruling. No canonicalization, no Reveal code, no `protocol/`, no wire schema, no FastMCP, no crypto. **(b) One authority.** `class ActionKind`, `class MoveAction` and `class BarrierAction` each appear **exactly once** per package, in `domain/actions.py`, which is **byte-identical between Police and Thief** (sha256 `c78776a3…`). The names remain importable from `app.turn_service` because the impact scan proved that path is pinned by a subprocess probe in `tests/app/test_turn_boundaries.py`, but they are the *same class objects* — proven by identity assertions — and `LocalAction is PhysicalAction` rather than a second union with its own member list. `app/__init__.py` needed **no** change. **(c) Structure only.** Frozen, slotted, one payload field each, exact composed-type identity, no coercion (`MoveAction("N")` raises rather than parsing), `kind` a derived property and never a stored field that could disagree with the class. A `BarrierAction` accepts an off-board `Position` **on purpose**: bounds, role, the thief prohibition, adjacency, occupancy, quota, position, phase and config are LIVE, and a value that needed a `Board` to exist would not be a value. `Move.STAY` stays an ordinary movement, distinct from a barrier turn. **(d) Error ownership — the supervising ruling.** Constructing a malformed *domain value* raises `InvalidPhysicalActionError(DomainError)`; handing a future `Reveal` a component whose exact type is neither `MoveAction` nor `BarrierAction` is a **message-composition** failure and raises the built-in `ValueError` that `app.peer_messages` already uses. These do not conflict because they are different failures at different layers: Reveal consumes an **already-constructed** action and must never wrap a `Move`, wrap a `Position`, construct either action, or catch and translate a `DomainError`. No new error class was created, and the LIVE `InvalidActionError`/`UnsupportedActionError`/`ActionsExhaustedError` keep their prior semantics — a rule violation never becomes a construction error. **(e) Consequence.** With `TurnCursor` and `PhysicalAction` both existing, `app.peer_messages` already permitted to consume immutable `domain` value types, `hint` structurally a `str` and `hint_max_words` still LIVE, **Reveal moves from BLOCKED-BY-FUTURE-SEMANTIC-TYPE to READY-TO-IMPLEMENT**. Because `PhysicalAction` is a union alias, its future runtime check is exactly `type(action) in (MoveAction, BarrierAction)` — never `type(action) is PhysicalAction` — with subclass rejection and no coercion. **(f) Process, recorded rather than smoothed.** The scope came from a mechanical impact scan run **before** any edit, not from R4's predicted file list, and the scan corrected that prediction in both directions. During the Thief migration an initial attempt mirrored Police's **whole** `turn_service.py` across; that was wrong — the two services carry legitimately different role policy — and it broke **6 Thief role tests**. The regression suite caught it, Thief's file was restored from its own HEAD, and the same narrow ownership delta was reapplied to its own content. The mirrored version was never an accepted design, and final Thief role behaviour is unchanged: it still refuses a `BarrierAction` through the existing LIVE capability policy, and role policy was **not** moved into `domain.actions`. **(g) Unchanged.** `domain.rules.Move` is still exactly N/S/E/W/STAY with no `BARRIER`; `app/peer_messages.py` has **zero diff**; `LocalActionResult` keeps `(truth, kind, completed_step)`; peer-visible families remain **10** — now 2 implemented, 1 ready, 7 blocked — and **Stage 4E as a whole is NOT COMPLETE**. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changed. |
| D32 *(4E-RESUME3)* | **`Reveal` is implemented as the third peer-message family - exactly `cursor` + `action` + `hint` - consuming an already-constructed domain action and never building one; and `app.peer_messages` reaches its 150-LOC ceiling, which bounds future family growth without blocking Reveal** | **(a) Scope.** R4 froze the shape and R5 supplied the value type; this records the implementation only. No canonicalization, wire mapping, commitment producer, verification, `protocol/`, FastMCP, transport or crypto. `domain.actions` and `app.turn_service` are **byte-unchanged**. **(b) Contract.** Frozen, slotted, fields exactly `('cursor','action','hint')` in that order; **0** supporting error classes and **0** new dependencies. Ch 5 §5.3.2 (p.51) sends *the action and the verbal sentence* with the nonce withheld until final audit, so ordinary reveal is deliberately incomplete beside audit material: no `nonce`, `state`, `intent`, `role`, `by_role`, `sealed_record`, `phase` or `h_commit`, and the 21-name absence sweep passes. The member is `action` because it holds the domain value; the **sealed key stays `move`**, and that mapping is the later canonical layer's job, not this slice's. **(c) The union-alias subtlety, made explicit.** `PhysicalAction` is a *type alias*, not a class, so neither `type(x) is PhysicalAction` nor `isinstance(x, PhysicalAction)` is meaningful. The frozen structural rule is exact membership - `type(action) in (MoveAction, BarrierAction)` - which also rejects subclasses of both. Nothing is coerced: a bare `Move`, a `Position`, a `"N"` token and a canonical `{"kind": …}` mapping each raise rather than being wrapped. **(d) Error boundary, as ruled.** Building a malformed *domain value* is `InvalidPhysicalActionError(DomainError)`; handing `Reveal` a component of the wrong exact type is a **message-composition** fault and raises the built-in `ValueError` this module already uses. Reveal never constructs, wraps, parses, repairs or translates an action, and never catches a `DomainError`. **(e) `hint` stays structural.** Exactly a `str`, with a `str` subclass refused; `""`, `"   "` and very long multi-word strings are **accepted**, because `hint_max_words` is locked configuration and stays LIVE. Inventing a non-empty, trimming, word-count or alphabet rule here would have been a rule no contract freezes. **(f) STAY and the exact cell.** `MoveAction(Move.STAY)` is a valid revealed movement, not a null or skip; a `BarrierAction` is retained as **the same object** with its exact `Position`, with no row/col extraction and no adjacency, bounds, role or quota check. **(g) Historical regression, observed before it was fixed.** The committed `test_peer_messages.py` was run **unmodified** after Reveal existed and failed on exactly one assertion, its blocked-family list still naming `Reveal`; only then was that entry replaced by the genuinely still-blocked `FinalNonceReveal`, keeping the same five assertions and all coverage. **(h) Module capacity - the honest cost.** The parent module was 132 LOC and a minimal Reveal needs about 32, so it did not fit. Roughly 20 lines were reclaimed by **prose-only** compression of four docstrings, and behavioural neutrality was **proved** by stripping docstrings from both revisions and comparing ASTs: `TurnCursor`, `Commitment`, `Acknowledgement` and `_require_int` are executable-AST identical, with `Reveal` the only added name. The file now sits at **exactly 150/150** - compliant, but with zero headroom. **No further semantic family may be added there** until Stage 4E-R6 reconciles module organization; the docstrings must not be compressed again, and no base class, mixin, registry or module split may be improvised to make room. Reveal itself is not blocked by this. **(i) Unchanged.** `domain.rules.Move` is still exactly N/S/E/W/STAY; `Commitment` and `Acknowledgement` keep `(cursor, h_commit)`; peer-visible families remain **10**, now **3 implemented and 7 blocked**, and **Stage 4E as a whole is NOT COMPLETE**. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changed. |
| D33 *(4E-R6)* | **The peer-message module organization is fixed by measured LOC into three defining modules behind an `app.peer_messages` façade; and re-auditing #8/#11/#12 against Figure 6 makes the final nonce reveal READY while reclassifying the other two to BLOCKED-BY-PAYLOAD-SHAPE** | **(a) Method.** Both halves were re-derived from the **authoritative PDF**. **(b) Capacity, settled arithmetically.** `app/peer_messages.py` is 150/150: `TurnCursor` 27, `Commitment` 25, `Acknowledgement` 28, `Reveal` 31, private guard 8, docstring 16, imports 4. Relocating all four into one new module lands at **~145** and reproduces the wall one family later, so that option is **rejected on measurement, not aesthetics**. Frozen: `app.turn_cursor` (~54) + `app.peer_turn_messages` (~107) + `app.peer_final_messages` (new) + `app.peer_messages` as a **pure façade** (~36). **(c) Why `TurnCursor` moves.** Both message modules need it and neither should import the other; extracting the shared value keeps the split symmetric and the graph acyclic (`turn_cursor` <- turn/final <- façade). That is ownership evidence - §H's bar - not 'the file is full'. **(d) Façade, not wrappers.** The public paths must keep working with **identity-equal** classes, because three test modules import `app.peer_messages` directly and one imports the module object to assert blocked families are absent. A façade defining wrapper or subclass copies is forbidden; `peer_messages.X is defining_module.X` must hold. Explicitly **not** used: a `PeerMessage` base class, a `MessageKind` enum for filing, a registry, a factory, a generic payload dict, dynamic imports or reflection. **(e) Figure 6 is the pivot for part two.** Ch 5 p.52 draws exactly four arrows - Commit, Acknowledge, Reveal, *Final Reveal: all Nonces* - captioned as the four stages Commit→Acknowledge→Reveal→**Audit**. There is **no move-validation arrow and no audit-verdict arrow**, which is why two of the three families move sideways rather than forward. **(f) #8 Move validation → BLOCKED-BY-PAYLOAD-SHAPE.** The *value-representation* half is genuinely solved (R4 froze the action encoding, R5 built `PhysicalAction`, `Reveal` carries it), so keeping that label would have been misleading. What remains unfrozen is whether validation is transmitted at all: App E #14 requires the opponent to reject an illegal move and Ch 6.5 requires local rejection of an illegal model proposal, but no source says a payload travels, and the only `{"accepted": …}` text is the minimal FastMCP example (p.28) - REFERENCE-EXAMPLE, the same snippet R3 refused to promote. Same category as #14, whose flow NDEC-006 also declines to freeze. **(g) #11 Final nonce reveal → READY-TO-IMPLEMENT.** p.55: each agent submits his full log *including the nonce reveals of all his steps*, and Figure 6 draws the arrow both ways - so **one batched message per side, covering that side's own steps**, never one per turn and never carrying the opponent's material. Role is **not** a payload field: a side reveals only its own nonces and the receiver knows the direction, exactly the `by_role` result from R3. Association is `TurnCursor`, which already carries `(sub_game, step)`. The last gap was the nonce's own representation, whose default stays **`[0-9a-f]{32}`, lowercase, exactly 32 characters** - **PROJECT-CONTRACT**, since the book requires only a *fresh cryptographic* nonce and `secrets.token_hex(16)` is REFERENCE-EXAMPLE. *(Stage 4E-R6-FIX2 removed a contradiction FIX1 introduced: NDEC-001 was worded as though another nonce encoding could already be echoed and accepted, while `NonceValue` accepts exactly `[0-9a-f]{32}` — both could not be true. Current **v1 supports exactly one representation**; NEGOTIATED-PRE-MATCH means both peers echo **that** profile before `CONFIG_LOCKED`, and a differing profile **refuses counted play** as a LIVE compatibility check — not an `InvalidNonceError`, not a TAMPERED verdict, not an invented sanction. A second representation is a future-version change requiring contract extension, value support and both repos implementing it. Width is also not entropy: 32 hex characters fix a 16-byte *width*, while freshness and randomness remain the producer's CSPRNG duty.)* *(Rationale corrected at Stage 4E-R6-FIX1: R6 justified the fixed form by claiming peers disagreeing on case or length could not recompute identical bytes. That was **wrong** - the receiver rebuilds the record from the **exact string the sender revealed**, so any stable representation recomputes identically. The real reasons are parser strictness, invariance under NDEC-003's NFC step, and a 128-bit entropy floor. Correspondingly, **CSPRNG production and secrecy-until-reveal are producer/runtime duties, never structural constructor checks** - a value built from a received string cannot prove either.)* **Frozen type inventory (4E-R6-FIX1, design only):** `NonceValue(value: str)` in `app.protocol_values` with `InvalidNonceError(ValueError)` mirroring `InvalidDigestError`; `NonceRevealEntry(cursor, nonce)` and `FinalNonceReveal(entries: tuple[NonceRevealEntry, ...])` in `app.peer_final_messages`, exact-`tuple` with no iterable coercion, `entries == ()` structurally accepted, and completeness/uniqueness/ordering left LIVE. **Batch boundary: one message per peer per sub-game** - a PROJECT-CONTRACT reading, because Appendix F names the counted unit *משחקון* while §5.4 says *end of the game*, and the project's own locked artifact `log_<game_id>_g<NN>.json` is one file per sub-game carrying its own `final_reveal` and `audit.result`. **NDEC-001 is amended in place; the NDEC count stays 7** and no JDEC-016, INV-16 or C-11 was created. **(h) #12 Final audit → BLOCKED-BY-PAYLOAD-SHAPE.** The *association* question is answered - §5.4 rebuilds the opponent's record from State/Move/Intent/Nonce against the declared commitment, and `LOG_CONTRACT.md` §E already freezes `entries[].verified`, `audit.result` (`Verified OK`/`TAMPERED`) and `audit.tampered_step` - so that label was also stale. Unfrozen is whether a verdict is **transmitted**: Ch 2.2.1 makes independent verification the whole reason there is no central server, and Ch 7 §7.5 + Figure 10 put the recomputation in the **Replay Viewer** over the persisted log. A later stage may conclude this family is not transmitted at all; R6 does not force that. **(i) Sanction boundary, kept distinct.** p.55 says tampering *causes* a heavy technical loss - so `TAMPERED` is the cryptographic **verdict** and technical loss is the **sanction**, not synonyms; `FAILED` remains the separate protocol terminal. **(j) Unchanged.** No Python; `Commitment`, `Acknowledgement`, `Reveal`, `domain.actions` and `turn_service` untouched; implemented families remain **3**; peer-visible families remain **10**; and no requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changed. |
| D34 *(4E-R7)* | **The peer-message values are physically re-homed into `app.turn_cursor` and `app.peer_turn_messages` behind a 20-line `app.peer_messages` façade, with an empty `app.peer_final_messages` boundary - a pure migration proved by executable-AST equivalence and zero edits to the existing behaviour tests** | **(a) Scope.** D32 (4E-R6) chose the architecture by measurement; this records only its execution. No new family, no canonicalization, no `protocol/`, no wire schema, no crypto, no transport, and no game or domain semantics touched. **(b) Result.** `peer_messages.py` **150 → 20 LOC**; `turn_cursor.py` 51 (`TurnCursor` + private `_require_int`); `peer_turn_messages.py` 108 (`Commitment`, `Acknowledgement`, `Reveal`); `peer_final_messages.py` 10, a docstring-only boundary with **zero** semantic classes - a placeholder there would have been a claim the module has not earned. **(c) Proof, not assertion.** Ownership and emptiness were checked by **AST**, not grep, precisely because the façade docstring contains a line beginning "class objects" that a naive `grep '^class '` reports as a definition: one definition per class in its owning module, zero in the façade and the boundary. All five moved definitions - including the private helper - are **executable-AST identical** to the committed parent with docstrings, imports, file location and `__module__` ignored, in **both** repositories. **(d) Compatibility is import compatibility.** `peer_messages.X is <defining module>.X` and `app.X is <defining module>.X` for all four; the façade re-exports the same class objects with explicit `X as X` and defines no wrapper, subclass or copied validation. `app/__init__.py` has **zero diff** - the façade was designed so that stayed true rather than the package being adjusted to fit. The three committed behaviour test modules (73 + 39 + 58 assertions) passed **untouched**, which is the migration's real acceptance test; no existing test was edited, because no stale-ownership failure occurred. **(e) Metadata, decided by evidence.** A pre-edit scan for `__module__`, `__qualname__`, `repr`, pickle, `inspect` and source-location dependencies across both repos found **none**, so the defining-module metadata was allowed to follow the new physical ownership (`TurnCursor` → `app.turn_cursor`; the three families → `app.peer_turn_messages`). **Nothing was assigned, monkeypatched or wrapped** to fake the old location. **(f) Graph.** `turn_cursor` imports no sibling; `peer_turn_messages` imports `{protocol_values, turn_cursor}`; `peer_final_messages` imports none; only the façade imports the defining modules - acyclic, with no back edge and no `domain → app` edge. **(g) Process defect, preserved.** The first migration helper sliced class blocks by hardcoded line numbers; an assertion caught an off-by-one **after** `peer_messages.py` had been overwritten. The partial state was discarded - the file restored from HEAD and its hash `23cf3b03…` re-verified, the partial modules deleted - and the migration redone with **AST-derived ranges**, every extracted block re-parsed and proved identical to the original definition before being written. The line-slicing approach was never accepted; recording it matters more than the tidiness of omitting it. **(h) Outcome.** 881 / 876 tests at 100% coverage; every file ≤150 with the façade ~130 lines of headroom and `peer_turn_messages` ~42, so the capacity blocker is removed rather than relocated. `FinalNonceReveal` stays **READY-TO-IMPLEMENT in contract and absent from Python**; implemented families remain **3**, blocked **6**, and **Stage 4E is NOT COMPLETE**. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changed. |
| D35 *(4E-R8)* | **The commitment nonce is implemented as a representation-only semantic value, and the final nonce reveal as a structurally-permissive batch: `NonceValue` accepts exactly `[0-9a-f]{32}` and refuses everything else without normalising, while entropy, freshness, CSPRNG provenance, secrecy, completeness, uniqueness, ordering and same-sub-game agreement all stay outside the constructors** | **(a) Scope.** R6/FIX1/FIX2 froze the contract; this decision records how it was implemented and, more importantly, the two lines that were deliberately *not* drawn inside a constructor. **(b) One error, one meaning.** `InvalidNonceError(ValueError)` mirrors `InvalidDigestError` and covers exactly one thing - malformed `NonceValue` construction under the already-selected local profile. It is **not** the pre-match profile-mismatch refusal (a LIVE compatibility check before `CONFIG_LOCKED`, NDEC-001), not a freshness, entropy, CSPRNG-provenance or secrecy failure, not a duplicate-message or phase/order error, not a commitment mismatch and not a tampering verdict. It is deliberately **not** a subclass of `InvalidDigestError`: a 32-character nonce and a 64-character digest are disjoint domains that merely share an alphabet, and one `except` clause catching both would be a latent bug. Message composition errors stay built-in `ValueError`, as `Commitment`, `Acknowledgement` and `Reveal` already do. **(c) Representation is not generation.** The book requires a *fresh cryptographic* nonce (Ch 5 p.50-51) and fixes no encoding; `secrets.token_hex(16)` is REFERENCE-EXAMPLE. Our 32-lowercase-hex profile is PROJECT-CONTRACT, adopted for strict deterministic parsing and NFC stability - **not** because recomputation needs it, since a receiver rebuilds the sealed record from the exact revealed string. It follows that a value built from a *received* string can never prove how it was produced, so `NonceValue("0" * 32)` is **accepted and asserted valid**. CRYPTO-010 remains a runtime obligation on the producer and secrecy-until-final-reveal remains a protocol invariant; neither is a structural check, and pretending otherwise would advertise a guarantee the type cannot make. Nothing is normalised: uppercase, whitespace, an `0x` prefix, separators, Unicode hex lookalikes and `str` subclasses are all refused rather than repaired, because silently repairing input is how two peers end up hashing different bytes. **(d) The batch is structural only.** `FinalNonceReveal` is exactly `entries: tuple[NonceRevealEntry, ...]` - an exact `tuple`, every member an exact `NonceRevealEntry`, no list, set, generator or iterable consumed or converted, subclasses refused. An **empty** batch, **duplicate** cursors, **out-of-order** steps, step gaps and **mixed sub-games** all construct successfully, and each is asserted on purpose: deciding they are wrong requires knowing which steps were actually played, which sub-game is current and which nonces have already been seen. That is game state, and a semantic value that reaches for game state stops being a value. Completeness, uniqueness, expected ordering, same-sub-game consistency, expected sender, phase, deadline, duplicate/stale handling, commitment lookup and cryptographic recomputation are all **LIVE**. **(e) What the payload does not carry.** No `role`/`by_role` - D27 settled that attribution is local, derived from sender direction plus the role mapping frozen at `CONFIG_LOCKED`, and because it is never transmitted there is nothing to forge. No `sub_game` field either: the one-message-per-peer-per-sub-game contract is carried by the protocol, and association comes from each entry's `TurnCursor`, which is precisely why a mixed-sub-game tuple constructs here. No repeated `h_commit`, action, move, hint, state, intent or verdict - each already has an authority, and a second copy is a second thing that can disagree. **(f) Placement follows ownership, not convenience.** The nonce is a **protocol value** shared by the sealed record and the reveal, so it lives in `app.protocol_values` beside `Sha256Digest`; the two message types are the **finalization family**, so they live in `app.peer_final_messages`, the boundary DD34 created and left empty. `app.peer_messages` re-exports `FinalNonceReveal` as the same class object and defines nothing; `NonceRevealEntry` stays **off** the façade because it is a supporting composition value, not a message a peer sends. **(g) Nothing cryptographic was added.** No sealed-record rebuild, canonical JSON, UTF-8 encoding, SHA-256, `H_commit` comparison, `FinalAuditVerdict` production or TAMPERED transition; no `hashlib`, `json`, `secrets` or `random` reaches either module, and neither class grew a `verify`, `recompute`, `serialize` or `to_json`. That work is Stage 4E-R9. **(h) Unchanged.** `TurnCursor`, `Commitment`, `Acknowledgement`, `Reveal`, `Sha256Digest`, `InvalidDigestError`, `FinalAuditVerdict`, the domain action types and `LocalTurnService` are untouched, and the three committed behaviour test modules for the earlier families passed with **zero edits**. `FinalNonceReveal` becomes the fourth implemented family; `MoveValidation`, `FinalAudit` and the mutual result agreement remain `BLOCKED-BY-PAYLOAD-SHAPE`, families stay **10**, and **Stage 4E is NOT COMPLETE**. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changes. |
| D36 *(4E-R9-R1)* | **The three sealed members that had no exact representation are resolved as contracts, not code: the canonical sealed role is `"police"`/`"thief"` with an explicit runtime mapping and three deliberately separate vocabularies; `intent` is the source-explicit `truth`/`lie`; and `SealedState` is frozen over sorted, duplicate-free barriers with two cross-component equality invariants - all homed in `app.sealed_record_values`, chosen on boundary grounds rather than convenience** | **(a) Why this decision exists.** Stage 4E-R9 stopped `BLOCKED-BEFORE-CODE` after auditing all eight sealed members: `move`, `hint`, `step`, `sub_game` and `nonce` were exact; `role`, `state` and `intent` were not. That stop is preserved as a correct result, not rewritten. **(b) `role` - four spellings, one sealed vocabulary.** The book never fixes a byte string: Figure 6 (p.52) labels its two lifelines `Cop` and `Thief`, which is explanatory terminology. Meanwhile `LOG_CONTRACT.md` said `police`/`thief` in a **Type column**, the repositories carry `ROLE = "POLICE"` / `VALID_ROLES = {"POLICE","THIEF"}`, and PRD01-FR-070/071/072 key scores as `{cop, thief}`. The lowercase form only *looked* frozen; the same Type column describes `state` as `string/object`, which is demonstrably not a frozen representation, so that column cannot be serialization law. The canonical sealed vocabulary is therefore fixed **by decision** as exactly `"police"` / `"thief"`, classified **PROJECT-CONTRACT** and explicitly *not* source law. The runtime mapping is **explicit** - `"POLICE"` → `"police"`, `"THIEF"` → `"thief"` - never `lower()`, case-folding, normalisation, alias or synonym, and an unrecognised runtime role is refused at the owning boundary rather than mapped to a guess. Three vocabularies now coexist on purpose: source `Cop`/`Thief` (never serialized), sealed `police`/`thief`, and score keys `cop`/`thief`. **The `cop` score key is not the sealed role for police**, and PRD-01 was deliberately **not** re-spelled: two contracts may legitimately use two words for one side, and rewriting a locked scoring contract for cosmetic uniformity would be a real change disguised as tidying. **(c) `intent` - the vocabulary was always in the source.** Ch 5 §5.3.1 (p.51) defines the intent flag as stating whether the accompanying hint is true *(truth)* or misleading *(lie)*, printing both English words inside the Hebrew prose. So the vocabulary is **SOURCE-REQUIRED**, and only the Python type carrying it is a project choice. No bool, no `unknown`/`neutral`/`honest`/`deceptive`, no empty string, no normalisation, and never inferred from the hint's wording. `intent` and `hint` remain **separate sealed members that both always travel**: Reveal sends `action` + `hint` on the wire while the sealed record carries all eight members including `intent`, and that asymmetry is the entire point of a commitment. **(d) `state` - the shape was locked, the representation was not.** JDEC-012 / NDEC-002 / PRD06-FR-068 already froze `{config_sha256, self_pos, barriers, step, role}`, but `LOG_CONTRACT.md` contradicted its own §B by still listing `state` REVIEW-REQUIRED in two places - repaired here. Two things are tightened. `barriers` must **already** be lexicographically sorted by `(row, col)` and duplicate-free **in the semantic value**, so the canonical mapper never sorts, deduplicates or coerces a `set`/`list`; this gives a logically set-like collection one deterministic order before serialization, so canonical bytes cannot depend on producer iteration order and equality does not silently become order-sensitive. And because `state` repeats `step` and `role`, the future builder **must refuse** `state.step != cursor.step` or `state.role != role` **before hashing** - a composition error, emphatically **not** a hash mismatch and not TAMPERED, since nothing has been hashed and no peer has done anything wrong. Hashing a self-contradictory record would be the actual defect. This duplication is deliberate and is **not** a ninth field. **(e) Where the new values live - decided by boundary, not by space.** `app.protocol_values` was the obvious shelf and was rejected because it is contractually stdlib-pure while `SealedState` must hold a `domain.board.Position`; admitting `domain` there would widen a boundary drawn deliberately at 4F-R1. (It is also at 137/150 LOC, but that is the secondary argument - the answer would be the same in an empty file.) `domain` was rejected because `SealedState` carries a `Sha256Digest` and a `domain → app` edge is forbidden, the mirror image of D23. `protocol.commitment` was rejected because `app.turn_service` would then import outward. The peer-message modules were rejected on ownership: **the sealed record is never transmitted**, so it is not a peer-message family. The answer is one new application-layer module, **`app.sealed_record_values`**, holding all three together because they are one contract (`SealedState.role` *is* an `ActorRole`). Its outward permission for `protocol.canonical`/`protocol.commitment` is the **same outer→inner pattern already authorized twice** (4E-R1, 4F-R1), so `DEPENDENCY_RULES.md` is unchanged - no new layer, no new edge kind, no cycle. **(f) Error contract, deliberately small.** `ActorRole` and `Intent` are `StrEnum`s, so invalid construction raises the ordinary `ValueError`; `SealedState` composition failures raise the built-in `ValueError`, the convention `NonceRevealEntry` and `FinalNonceReveal` already use. **No `InvalidRoleError`, no `InvalidIntentError`, no new hierarchy**; `InvalidDigestError` and `InvalidNonceError` stay with the representation values they belong to. **(g) Two related ambiguities closed.** `ensure_ascii=False` existed only in PRD06-FR-005 while `CANONICALIZATION_CONTRACT.md`, NDEC-003 and JDEC-002 said merely "fixed & agreed" - a value living in one artifact nobody reads at implementation time is in practice an implementation-time choice, so it is now stated centrally. And digest comparison is split into three layers: the **pure codec** returns a `bool` and a `False` is a correct successful result, never an exception; the **audit consumer** interprets that as `E-HASH-MISMATCH` and/or `FinalAuditVerdict.TAMPERED`; the **sanction layer** applies the consequence. `API_BOUNDARIES.md`'s terminal-mismatch language describes the port outcome, not the primitive. **(h) Scope.** 0 Python, 0 tests, 0 dependencies. `ActorRole`, `Intent` and `SealedState` are **not implemented** and are **not peer-message families**; the peer-visible inventory stays **10 = 4 implemented / 0 ready / 6 blocked** and **Stage 4E is NOT COMPLETE**. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changes; every clarification landed inside NDEC-001/002/003 and JDEC-002/012. |
| D37 *(4E-R9-R2)* | **The three sealed-record semantic prerequisites are implemented exactly as R9-R1 froze them - two closed `StrEnum` vocabularies and one structural snapshot that refuses rather than repairs - and every judgement that could have quietly widened them was resolved by exact type rather than by value** | **(a) Scope.** R9-R1 froze `ActorRole`, `Intent` and `SealedState` at contract level; this decision records their implementation in `app.sealed_record_values` and the choices the contract left to code. No contract was altered. **(b) Closed vocabularies, not validated strings.** `ActorRole` and `Intent` are `StrEnum`s with exactly two members each, no alias, no `_missing_` hook and no normalisation. That matters more than it looks: three different words denote the police side across the project - `"POLICE"` (our runtime constant), `"Cop"` (the book's Figure 6 display word) and `"cop"` (a PRD-01 score key) - and every one of them is **refused** here, as is every case variant and whitespace-padded form. A `_missing_` hook would have re-admitted all of them silently, which is why its absence is asserted behaviourally rather than assumed. The runtime→sealed mapping stays a future producer duty: no `from_runtime_role`, no mapping dict, no `lower()`. `Intent` likewise admits no bool substitute and no inference from the hint's wording. **(c) Exact type, not truthy value.** `SealedState` validates `type(x) is T` for every member. The case that justifies the strictness is `step`: `True` is an `int` in Python, so a `step >= 1` check alone would have accepted `True` as step 1. Rejecting bools by exact type closes that before it becomes a step-numbering bug. The same rule refuses a raw 64-hex digest string, a bare `(row, col)` tuple, a role string and - where Python permits the subclass - `Sha256Digest` and `Position` subclasses. An `Enum` with members cannot be subclassed at all; the test records that Python fact rather than inventing a proxy. **(d) Barriers refuse, never repair.** `barriers` must arrive an exact `tuple`, every member an exact `Position`, already sorted by `(row, col)` and duplicate-free. Nothing sorts, deduplicates, copies-and-fixes, converts a `list`/`set`, or consumes a generator - a generator is refused rather than exhausted. The proof is stronger than equality: `state.barriers is` the supplied tuple object. Barriers are logically a *set*, and serializing a set without a fixed order is precisely where two honest peers diverge into a false TAMPERED, so the ordering decision lives in the value where it can be enforced once. **(e) Structural only.** Board bounds, barrier legality on the current board, whether `self_pos` is blocked, quotas, reachability, capture, movement legality, phase and the current cursor are all absent, and `Board`/`LocalTruth`/`LocalTurnService` are not imported. Proved rather than asserted: `Position(9_999, 9_999)` is structurally valid to `Position` itself and `SealedState` accepts it, so the value demonstrably adds no geometry. The two builder invariants R9-R1 froze - `state.step == cursor.step` and `state.role == role` - are deliberately **not** here, because no cursor and no top-level role are in scope; they belong to `protocol.commitment`'s future builder. **(f) No opponent truth.** Sixteen absence assertions plus the exact field inventory: under partial observation there is structurally nowhere to put the opponent, and there is no metadata dict, no `extra` slot and no `**kwargs` capture to smuggle one in. **(g) Errors stay small.** `StrEnum` construction failures raise the ordinary `ValueError`; every `SealedState` composition failure raises the built-in `ValueError`, the convention `NonceRevealEntry` and `FinalNonceReveal` already use. No `InvalidRoleError`, no `InvalidIntentError`, no new hierarchy, and no child construction error is caught and translated, because the value accepts already-valid children. **(h) Nothing serializes.** No `to_dict`, `to_json`, `canonical`, `serialize`, `encode`, `digest`, `commit`, `recompute` or `verify`; no `json`, `hashlib`, `hmac`, `secrets`, `random`, `os`, `uuid` or `time` reaches the module; no NFC, no UTF-8, no SHA-256, no `protocol/` package. The AST import audit shows the module imports exactly `dataclasses`, `enum`, `itertools`, `domain.board.Position` and `app.protocol_values.Sha256Digest`, and the reverse edges are clean, so the graph stays acyclic and inward. **(i) Method note.** The `SealedState` test design measured 169 LOC against the 150-line rule and was **split by responsibility while still tests-first** into `test_sealed_state.py` and `test_sealed_state_barriers.py`; no assertion was merged away, thinned or deleted to fit. **(j) Unchanged.** `protocol_values`, the four peer-message modules, `turn_service` and the domain are byte-unchanged; the repository-root `ROLE`/`VALID_ROLES` constants are untouched; every committed test passed with **zero edits**. These three are internal prerequisites, **not** peer-message families - they are absent from the `peer_messages` façade - so the inventory stays **10 = 4 implemented / 0 ready / 6 blocked** and **Stage 4E is NOT COMPLETE**. The original R9 codec scope is now implementation-unblocked. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changes. |
| D38 *(4E-R9-RESUME)* | **The deterministic commitment path is implemented across two architecture-frozen modules, validated against an external known-answer oracle that predates the code, with one hashing primitive serving both commitment and recomputation and digest inequality returning a plain `bool`** | **(a) Why this could be written now.** The original Stage 4E-R9 stopped `BLOCKED-BEFORE-CODE` with `role`, `intent` and `state` unrepresented; 4E-R9-R1 reconciled them as contracts and 4E-R9-R2 implemented `ActorRole`, `Intent` and `SealedState`. That stop is preserved as correct: writing the codec earlier would have serialized `role` as whichever of four coexisting spellings seemed natural, and the consequence of that error is a false TAMPERED, which voids a match with no appeal. **(b) The oracle preceded the implementation.** Three supervisor-supplied canonical strings and SHA-256 values were reconstructed independently, before any production module existed, by a one-off script importing only `json`, `hashlib` and `unicodedata`, importing no project code and writing no repository file. All three matched, and the expected values were hard-coded as test literals afterwards. This matters because the ordinary failure mode of codec tests is to compute the expectation from the implementation, which proves only that the code agrees with itself - useless for a scheme whose whole purpose is that two independent parties agree. **(c) Two modules, two responsibilities.** `protocol.canonical` owns bytes and knows no semantic type: it imports exactly `json` and `unicodedata`, and its runtime domain is deliberately narrow - exact `str`, `int`, `list`, `dict`, with exact-`str` keys. It refuses `None`, `bool`, `float`, `bytes`, tuples, sets, generators, arbitrary objects and even subclasses of the accepted builtins. `float`/`null`/`bool` are refused **on purpose**: a sealed record contains none of them, so implementing the wider contract's numeric rules here would ship untested branches for a payload that never exercises them; the domain is extendable when something needs it. It also refuses a **non-NFC** string rather than normalising it, because repairing that one step before hashing would hide the producer defect the NFC rule exists to catch. `protocol.commitment` owns meaning: explicit `PhysicalAction` and `SealedState` mappings, the eight-field builder, the hash and the comparison. Nothing reflects - no `__dict__`, `asdict`, `repr`, `pickle`, custom encoder or `default=` callback - because a reflective encoder silently follows whatever field a value grows next, and the point of this key set is that it cannot grow. **(d) The mapper does not repair.** Barriers are emitted in the order `SealedState` already established; re-sorting here would hide a producer defect behind bytes that happen to agree. **(e) Contradictions are refused before hashing.** `state.step != cursor.step` and `state.role != role` raise `ValueError` in the builder. These are local composition defects, emphatically not tampering: nothing has been hashed and no peer has sent anything. Hashing a self-contradictory record would be the real bug, because the digest would be perfectly valid and permanently unreproducible for the right reasons. **(f) One primitive, not two.** There is no `recompute` function. `compute_commitment` is called once at commit time and again with the revealed nonce, and a test asserts the equality explicitly. Two implementations of one hash can drift, and a drifting verifier accuses an honest peer. **(g) The comparison does not judge.** `commitment_matches` returns a plain `bool`; `False` is a correct successful result. `E-HASH-MISMATCH` and `FinalAuditVerdict.TAMPERED` stay with the audit consumer above this layer, exactly as 4E-R9-R1 froze, and `hmac` is deliberately not imported for the comparison because these digests are not authentication secrets. **(h) Unchanged.** All thirteen committed semantic/application/domain files are byte-identical, `app/__init__.py` gained nothing - protocol helpers are not app exports - the peer façade is untouched, no peer message is constructed, no game legality is checked, and every committed test passed with **zero edits**. **(i) One documentation reconciliation.** `MODULE_BOUNDARIES.md`'s `protocol.commitment` dependency cell omitted `app.turn_cursor`, which the builder input frozen at 4E-R9-R1 always required; the row was incomplete, not contradicted, and no other permission was broadened. `DEPENDENCY_RULES.md` needed no change - `protocol → app` was never forbidden. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV, Conflict-Register or FIELD_MATRIX count changes, and the peer-visible inventory stays **10 = 4 implemented / 0 ready / 6 blocked**. |
| D39 *(4E-R10-R1)* | **There is no peer-visible `FinalAudit` verdict family - not because the source forbids one, but because it never requires one - and removing it exposes a source-required audit-material obligation that had been hiding behind it, so the correction is a three-way separation rather than a deletion** | **(a) Why this decision exists.** Stage 4E-R10 was asked to freeze the `FinalAudit` and `MoveValidation` payloads and stopped `INVENTORY-CONTRADICTION` with 0 files changed, because the source audit undermined the premise of the first one. That stop is preserved as correct. **(b) Three convergent source lines.** Ch 5 §5.4 has each side **submit its full log including every nonce reveal** and then **independently recompute** the opponent's commitments, concluding that *"the cryptography, and not human judgement, decides"*. Ch 7 §7.5 and Figure 10 place `Verified OK`/`TAMPERED` in the **Replay Viewer**, a local submission-requirement tool reading a persisted log file, and PRD-07 REPLAY-001/002 owns that output. Figure 6 (p.52) draws four arrows - Commit, Acknowledge, Reveal, Final Reveal - and no audit-verdict arrow. App E and PRD06-FR-100 make the mutual audit a **precondition to agreeing the shared result**, and PRD06-FR-104 requires evidence to be *preserved*, not transmitted. A transmitted verdict would be the other side's assertion about its own conduct, which unforgeable local recomputation exists to make unnecessary. **(c) Classification, stated precisely.** The correct label is **NOT SOURCE-REQUIRED**, never *forbidden*. Source silence is not prohibition, and the difference decides whether a future stage may legitimately add such a message; every reconciled document says so explicitly. The project declines to invent one under source minimality. **(d) The obligation that was hiding behind it.** This is the part a plain deletion would have lost. The sealed record has eight members; ordinary Reveal discloses the action and hint, `FinalNonceReveal` discloses the nonce batch, and the peer therefore still lacks the disclosed `state` and `intent` needed to rebuild the record and recompute the digest. Ch 5 §5.4 closes that with the **full-log disclosure**. So removing the family makes a real source requirement **visible** rather than removing it - and nothing in the project was carrying it. It is **not** an `app.peer_messages` family: it belongs to the artifact/transport boundary (`infra.logger` produces, `infra.artifacts` stores, `PeerTransportPort` carries, the replay/audit path consumes), and its interchange shape is recorded as **`AUDIT-EXCHANGE-PAYLOAD: BLOCKED-BY-INTEROPERABILITY-SHAPE`** with raw bytes, the whole JSON log, a sealed-record tuple, partial material, an artifact URL and a filesystem path all deliberately left undecided, and `submit_audit` left a REFERENCE-CONVENTION. Deciding those bytes in the same stage that discovered the gap would repeat the original error. **(e) What survives untouched.** `FinalAuditVerdict` keeps its exact `"Verified OK"`/`"TAMPERED"` vocabulary as the **local** audit/log/replay result - source-explicit through Ch 7, consumed by `infra.replay` and the log, never by a peer. `ProtocolPhase.FINAL_AUDIT` remains a workflow phase, because a phase is not a message family. Timeline **event 12** survives with the event count still **15**, rewritten to distinguish **transmitted** (audit material) / **local** (recomputation, comparison, verdict) / **persisted** (audit evidence). The TAMPERED sanction is unchanged. **0 Python** changed anywhere. **(f) A wording correction to my own prior stage.** R10 said the peer "learns the verdict through `ResultAgreement`". That is stronger than the evidence: the supported claim is only that a **completed mutual audit is a precondition to** mutual result agreement. `ResultAgreement` is downstream of audit, is **not** the transport of `FinalAuditVerdict`, and remains `BLOCKED-BY-PAYLOAD-SHAPE`. **(g) Counts.** The derived peer-visible inventory is corrected **10 → 9** - Step-0, config negotiation, config lock, Commitment, Acknowledgement, Reveal, MoveValidation, FinalNonceReveal, ResultAgreement - with the matrix **4 implemented / 0 ready / 5 blocked**. `FinalAuditVerdict`, the FINAL_AUDIT phase, the audit-material exchange and the Replay Viewer result are **not** counted as families. The official Conflict Register grows **10 → 11** with **C-11**, disambiguated from the unrelated review-local label of the same name in `docs/prd/PRD_05_07_REVIEW.md`, which was not edited. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV or FIELD_MATRIX count changes, and no C-12. **(h) Out of scope.** `MoveValidation` stays `BLOCKED-BY-PAYLOAD-SHAPE` with every byte-affecting dimension open, and **Stage 4E is NOT COMPLETE**. |
| D40 *(4E-R10-R3)* | **The source-required rejection of an illegal move is placed at the transport/port response boundary rather than modelled as a standalone peer-message family - a PROJECT-CONTRACT choice in a source-unspecified area - and the response shape is deliberately left unfrozen because the operation contract it would attach to does not exist yet** | **(a) Why this decision exists.** Stage 4E-R10-R2 stopped `BLOCKED-BY-EXISTENCE-EVIDENCE` with 0 files changed: App E #14 makes opponent rejection source-entailed, but no passage says whether it is a standalone message, a same-call response or a local refusal. That stop is preserved as correct, and it was deliberately **not** turned into a second inventory contradiction - unlike `FinalAudit`, where the audit mechanism is demonstrably complete without the message, here the source positively places an act on the opponent, so there was no positive evidence of over-classification. Supervising review then chose the mechanism. **(b) The ruling.** No standalone `MoveValidation` family, and none of `ValidationAck`, `ValidationResultMessage`, `MoveAcceptedMessage` or `MoveRejectedMessage`; the peer-facing legality outcome belongs to the **transport / port response** of the operation that received the turn. Classified **PROJECT-CONTRACT under source minimality**, and stated everywhere as *the source does not **require** such a message*, never as a prohibition - the same discipline C-11 established. **(c) The obligation relocates, it does not vanish.** Three committed contracts already carry it: **`GameRulesPort`** decides legality and *"never raises for legality - returns a verdict"*; **`E-PROTO-ILLEGAL-MOVE`** already owns "opponent sent an illegal move" with *"received message + validator verdict"* as its evidence; and **`PeerServerPort`** is the ingress whose response would surface it. So **no new port, semantic concept or error ID** was created - the instruction not to invent a concept an existing contract already owns is satisfied by pointing at these. **(d) Four acceptances, never one flag.** Delivery/parsing, authentication, protocol phase/cursor/order, and **game legality** are now separated explicitly in `API_BOUNDARIES.md` principle P2b. App E #14 concerns only the fourth. The FastMCP `receive_move` example returns `{"accepted": is_valid, …}` where `is_valid = verify_signature(...)` - the **second** - so promoting it would not merely adopt example code, it would mislabel an authentication result as a game verdict, which is a worse error than the one Stage 4E-R3 already refused for the acknowledgement. Transport never re-derives legality, applies a move, mutates `LocalTruth`, computes scoring or chooses technical loss. **(e) Why the shape is not frozen.** The preferred candidate - one exact `bool` where `True` means the locally validated revealed `PhysicalAction` is game-legal and accepted at the currently expected turn - is documented as a **candidate, not a contract**. `API_BOUNDARIES.md` states in its own header that concrete operation signatures are deferred to **Stage 2B-2C**, PRD02-FR-035 repeats it, and both peer ports are **async** with a generic "protocol response" return. A bare `bool` hung on a contract that does not exist would decide the four-way separation by accident, so `MOVE-REJECTION-TRANSPORT-SHAPE` is recorded as **BLOCKED-BY-TRANSPORT-SHAPE** and no bytes were invented. Association, cadence and cursor echo stay unfrozen with it; no free-text reason and no action/hint/nonce/digest/state duplication would ever cross the boundary. **(f) Sanction stays above.** Legality `False` → protocol/runtime interpretation → existing illegal-move/barrier error ownership → state/sanction consequence. The transport result carries no score, `Outcome`, `ProtocolPhase` or TAMPERED. **(g) The timeline is reconciled, not truncated.** Events 8 and 9 both survive and the count stays **15**: a timeline event is not automatically a peer family, and a log validation record is not automatically a wire message. Event 9's transmitted column, which read `(result)`, is corrected to local-only. **(h) Counts.** Derived peer-visible inventory **9 → 8** - Step-0, config negotiation, config lock, Commitment, Acknowledgement, Reveal, FinalNonceReveal, ResultAgreement - matrix **4 implemented / 0 ready / 4 blocked**. Conflict Register **11 → 12** with **C-12**, disambiguated from the unrelated PRD-review label of the same name (*result-hash self-reference*), which was not edited. No requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV or FIELD_MATRIX count changes, and no C-13. **(i) Out of scope.** `ResultAgreement`, `AUDIT-EXCHANGE-PAYLOAD`, Step-0 and the config families keep their own blockers and none was used to justify this shape. **Stage 4E is NOT COMPLETE.** |
| D41 *(4E-R11)* | **The peer layer is split into logical operations and semantic families: operations are request/response regardless of async I/O, their results are separate from every failure class, and that separation is what finally makes an exact `bool` game-legality result safe - while the audit payload stays blocked on one named log-artifact question** | **(a) Why this stage existed.** Two integration blockers - the move-rejection response and the audit-material exchange - were both waiting on a peer **operation** contract that had never been written. R11 wrote it as `API_BOUNDARIES.md` O1-O7. **(b) The conflation that caused the earlier block.** Stage 4E-R10-R3 left the move-rejection shape blocked partly because the peer ports are declared `async`, which I had treated as implying no request/response contract. That is a category error - `async` is an I/O execution property, not a message-shape property - and the committed `CONCURRENCY_MODEL.md` already settled it: outgoing peer calls are *"per request, async, bounded… **never fire-and-forget for state-changing calls**"*, inbound *"requests do not mutate directly"*, and *"two concurrent peer requests must never mutate the same turn state"*. **O1** therefore states it as contract: each peer operation is one request whose caller awaits exactly one operation-specific result or a typed owning-layer failure, and an operation result is **never** a peer semantic family. This was reconciliation, not invention. **(c) Why the bool is now safe.** **O2**: a success result is semantically distinct from transport, parse/schema, authentication and protocol phase/order/cursor failures, each of which already has its own identity in `ERROR_MODEL.md` and is raised by its owning layer - reinforced by the Gatekeeper rule that retries cover *"transport-class errors only; never integrity errors"*. **No universal `accepted` flag** may span layers, no lower-layer failure may be encoded as a semantic `False`, and no semantic rejection may masquerade as a transport failure. A bare bool is dangerous exactly in proportion to how many failure modes can collapse into it; here that number is zero because every other mode is already owned elsewhere. **(d) The move-rejection contract (O5).** The operation carrying a **Reveal** (logical (e), alias `receive_turn`) returns exactly one **`bool`**: `True` = the already-authenticated, protocol-valid revealed `PhysicalAction` was locally validated as game-legal for the currently expected turn and accepted for the local application path; `False` = the same prerequisites hold but it was validated game-illegal and rejected. It never means delivery, parse, signature, sender, phase, cursor, commitment or reveal-hash validity - those never reach it. Association is the awaited invocation, so **no `TurnCursor` echo** and no duplication of action, hint, nonce, digest or state; exactly one result per invocation that reaches the legality layer, and **none at all** if an earlier layer fails. **No free-text reason crosses the boundary** - `GameRulesPort`'s reason stays local diagnostic and log evidence, and no Python exception text is transported. Legality remains solely with `domain.rules`/`LocalTurnService`; `False` is a result, not a sanction. **`MOVE-REJECTION-TRANSPORT-SHAPE` is RESOLVED-PROJECT / READY-TO-IMPLEMENT.** **(e) Operations and aliases (O3, O4).** The PRD02-FR-033 operations (a)-(h) are routed to families without defining any blocked payload; the four reference tool names stay **PROJECT-CONTRACT / REFERENCE-COMPATIBILITY, not book-mandated**, and internal code never depends on an alias string. **No fifth validation operation** - the legality outcome is a *result*, not an operation. One stable group-level public ingress per team, with `SeriesLauncher` routing internally as operational routing only, never referee or shared state. **(f) Audit (O6).** Operation (f), alias `submit_audit`, **one submission per completed sub-game** matching the existing `log_<game_id>_g<NN>.json` artifact - whole-series batching is not assumed. The transport submits the finalized log representation and the receiver persists the same semantic document; **no `AuditEntry`, `AuditBundle`, `AuditEvidenceMessage` or `AuditProjection`** parallel schema is created. Success is ordinary operation completion, so there is **no audit semantic response, no verdict, no digest and no audit ACK**. **(g) What is still blocked, and why I did not unblock it myself.** `AUDIT-EXCHANGE-PAYLOAD` is narrowed to **`BLOCKED-BY-LOG-ARTIFACT-SHAPE`**: the natural payload is the finalized per-sub-game log document, but it is classified **LOCAL-ONLY** (*"the persistent log file structure is LOCAL-ONLY"*; D4/JDEC-007 records *"only the sealed payload + hash are interop"*), **and** `LOG_CONTRACT.md` still carries a REVIEW-REQUIRED item on whether ack/reveal are separate `entries[]` events or sub-objects. Promoting a LOCAL-ONLY document with unfrozen nesting to an interoperability payload is a reviewed decision, and editing that contract to unblock myself would be exactly the silent reclassification these stages exist to prevent - so `LOG_CONTRACT.md` was deliberately left untouched. Of the six candidate representations, a **filesystem path** is rejected outright as not peer-portable, an **artifact URL** is rejected for now because no stable fetch contract exists, and a **typed audit projection** is rejected as a second schema the log already owns. **(h) Binding boundary (O7).** An operation contract is not a FastMCP binding: no decorator, signature or JSON schema, with PRD02-FR-035 still governing concrete signatures. **(i) Unchanged.** Peer-visible families stay **8** with matrix **4 implemented / 0 ready / 4 blocked**; operation results and audit material are not families. No new port (20), error ID (20), timeline event (15), JDEC, NDEC, INV or Conflict-Register entry - **no C-13**, because no new source contradiction was found. 0 Python, 0 tests, 0 runtime. **Stage 4E is NOT COMPLETE.** |
| D42 *(4E-R11-R1)* | **The finalized per-sub-game log has one schema with a shared audit-disclosure core and a local remainder: JDEC-007's LOCAL-ONLY shorthand is narrowed rather than replaced, the sealed record's semantic members are persisted while its canonical bytes are not, and the `submit_audit` payload boundary is deterministic in both directions** | **(a) Why this decision exists.** Stage 4E-R11 left one blocker: the source requires each side to disclose enough of its log for the opponent to recompute every commitment, while JDEC-007's Stage-1D shorthand classified the persistent log **LOCAL-ONLY**. Read literally those cannot both hold. **(b) Amend in place, not replace.** The shorthand was over-broad, not wrong in spirit: what is genuinely local is **internal logger mechanics, artifact metadata and the locally-derived verification annotations**; what must be shared is the **audit-disclosure core** of the finalized document. Key spelling and nesting stay PROJECT-CONTRACT. **One schema throughout** - no `AuditBundle`, `AuditProjection`, `AuditEntry` or `AuditEvidenceMessage` - and **JDEC count stays 15**. **(c) A live contradiction resolved to complete the mandatory proof.** `FIELD_MATRIX` lists the per-turn sealed record as a **Required** log row; `LOG_CONTRACT` §B calls the hashed payload *"a distinct object from the persistent entry"* and the illustrative example omits `state`. These are about different objects: the **canonical byte serialization** is derived and never stored, while the **semantic members are persisted**. The distinction is load-bearing, not pedantic - a receiver cannot derive `state.self_pos` under partial observation, so on the other reading independent verification is impossible. Resolved with **no tenth field and no FIELD_MATRIX edit**. **(d) Completeness.** For every audited turn the receiver correlates `state`, `move`, `intent`, `hint`, `step`, `role`, `sub_game` from the sealed record, `nonce` from `audit.final_reveal[]`, and `H_commit` from `entries[].commit`, all under the frozen Stage 4E-R9 canonical mapping - no second representation of `PhysicalAction`, `SealedState`, `Intent`, `ActorRole`, `NonceValue` or `Sha256Digest`. The nonce stays withheld until the final-audit boundary (CRYPTO-002). **(e) Nesting closed.** The separate-event `entries[]` form JDEC-007 already chose is preserved, with `(sub_game, step, phase)` as the only association and chronological protocol order for replay - never lexicographic, never hash-order dependent. Nesting was rejected because it would duplicate turn identity into a container, not on aesthetics. **(f) The payload boundary is deterministic in both directions.** *DISCLOSURE-READY* carries **all and only** SHARED-AUDIT-INPUT. Both **LOCAL-DERIVED-AUDIT** (`entries[].verified`, `audit.result`, `audit.tampered_step`) and **LOCAL-ARTIFACT-METADATA** (`by_role`, `schema_version`) are **outside** it, and neither becomes optional wire content because it exists in a local artifact. The CLOSE-time guard caught this: my own R11-R1 wording said `by_role` *"may appear"* without naming the context, and the lifecycle sentence excluded only local-derived annotations. In an interoperability contract optionality is a decision, not a hedge. **(g) Verdicts are never transmitted.** `FinalAuditVerdict` remains local audit / log / replay vocabulary; a sender's claimed verdict has no standing and compatibility copies are ignored for verdict generation. **(h) Bytes and hashes.** Semantic equality of the disclosure core is required; **whole-log byte identity between peers is not** - the byte-identity requirement stays with the sealed record and the other canonical objects, untouched. **No `log_sha256`, `audit_sha256` or `artifact_sha256`**: the audit verifies individual commitments against `H_commit`. **(i) A narrowed over-broad claim.** *"No semantic fact is represented twice"* became *"no field is duplicated within a single event"*, because the revealed `move`/`hint` legitimately appear both as historical **Reveal evidence** and as **sealed-record audit input** - distinct evidentiary roles whose values are expected to agree, and comparing them is meaningful. **(j) Ownership.** `PeerServerPort` receives; audit ingress validates identity, structure and the LIVE conditions; `ArtifactStore` persists; the replay/audit verifier reconstructs and compares and is the sole source of the local verdict. No static artifact value performs LIVE checks. **(k) Result.** `AUDIT-EXCHANGE-PAYLOAD` is **RESOLVED-PROJECT / READY-TO-IMPLEMENT**, `MOVE-REJECTION-TRANSPORT-SHAPE` stays resolved, and `INTEROPERABILITY_BLOCKERS` reads **Blocking items: None** - which changes no *family* readiness. Inventory stays **8** with matrix **4/0/4**; no requirement, PRD, Appendix-E/F value, JDEC, NDEC, INV or Conflict-Register entry changes, and **no C-13**. 0 Python, 0 tests, 0 runtime. **Stage 4E is NOT COMPLETE.** |
| D43 *(4E-R12 family)* | **Three pre-match peer families are frozen together, and the route mattered as much as the destination.** **(a) Bootstrap.** Verifying the Step-0 `AuthProof` in `STEP0_NEGOTIATION` could not depend on an `AuthProfile` frozen two states later at `CONFIG_LOCKED` and authenticated by the very proof it was needed to verify. `AuthProfile`/`KeyId` are therefore **provisioned out of band before `BOOT`**, and incoming `auth_alg`/`key_id` are **compared, never used to select their own verifier** - the fix that defeats algorithm-confusion downgrade without putting the envelope inside its own authenticated bytes. Serialized spellings are exactly `"HMAC_SHA256"` / `"ED25519"`, proofs fixed-width lowercase hex (64 / 128); malformed is `E-PROTO-MALFORMED`, failing-but-well-formed is `E-AUTH-FAILURE`; plain unkeyed SHA-256 is not an `AuthProfile`. **(b) The Step-0 core was never defined.** Four documents authenticated `canonical(step0_core)` while none enumerated it. It is now **19 members** - own subtree plus game identity, `game_start` and the token cap - own-subtree being forced by event-1 ordering, not preference. **(c) A field that should not have existed.** `token_usage_locked` was simultaneously Optional and a mandatory core member, at cardinality 1 in a two-team document. Returning to the book resolved it: Ch 5 §5.5 joins token locking to Step-0 with `במקביל` (*in parallel*), Ch 9 §9.3.3 gives the declaration the role of fixing *everything that does not change*, and App E #54 puts actual consumption in the **result**. Removed; **FIELD_MATRIX recomputed to 74 = 15/39/9/11**. **(d) An overclaim of my own.** I then wrote that `result_sha256` satisfied the source's cryptographic token-locking requirement. It does not: it protects the *finally reported* totals and proves nothing about whether every call was metered. That obligation stays open as **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`**, owned by PRD-06, and **Stage 4 may not close on peer-family readiness alone**. **(e) Chronology.** Moving the cap into the core created a contradiction with R12's own "agreed at event 2" reasoning. **MODEL A** was adopted because the evidence required it - `PROTOCOL_TIMELINE.md` event 1 always listed the token cap under *Known before* and `DATA_FLOW.md` always placed it inside the core - giving the lifecycle **PRE-STEP0-AGREED / SERIES-WIDE / IMMUTABLE-AFTER-STEP0** while the Appendix-F status stays **NEGOTIABLE**: provenance and lifecycle are different axes. Equality-only at event 2, never counter-proposable; the locked config's copy must equal the authenticated declaration cap. **(f) Config lock.** Its `AuthProof` covers a **`ConfigLockContext`** `{game_id, game_uid, sub_game, config_sha256, profiles}`, because the App-B core is byte-identical across every sub-game and alone would bind no sub-game and none of the `PRD06-FR-048` frozen values; the local `CONFIG_LOCKED` transition remains a fourth layer with **no serialized form**, and no `ConfigLockAck` family exists. **(g) Result.** Step-0, Config negotiation and Config lock are **READY-TO-IMPLEMENT**; families stay **8 = 4/3/1** with `ResultAgreement` the sole blocked one; integration blockers **None**; 0 Python, 0 tests; no requirement, port, error ID, family or register ID added. **Stage 4E is NOT COMPLETE.** |
| D44 *(4E-R13 family)* | **The last blocked peer family closes, and it took three passes because the first two answers were each right about the wrong layer.** **(a) Shape was not the problem.** R13 froze the `ResultAgreement` shape, the mutual-audit precondition, identity, disagreement behaviour and the timestamp-proposer rule - then found that `RESULT_APPROVAL_CORE` held **per-team** values (`sub_games[].tokens`, `total_tokens`, `sub_games[].github_commit`) that no frozen contract transmitted before the digest. Both peers were required to build the core independently, so honest peers would have produced different bytes and `E-REPORT-DISAGREE` would have voided **every** match. It stopped rather than guessing among three readings. **(b) Participant-scoped, not combined.** The ruling: wherever a semantic is participant-owned the shared core carries **both** values, as `{group_a, group_b}` objects - a scalar whose meaning depends on who emitted the report cannot live in a shared core. Per-group rather than one combined total, because the token **cap** is per group and a combined count would destroy the association with the participant whose cap applies. `total_tokens.<g>` is **derived** from that participant's six sub-game values, so one fact has one representation. **(c) Fixing one hole opened another.** Replacing the digest-only request with a contribution-carrying one excluded `timestamp` - leaving the non-proposer no channel to learn the exact string the hashed core demands. `timestamp` now travels in the request beside the identity (**not** inside `ResultContribution`: a jointly approved core value is not participant-owned evidence), as **`UtcTimestamp`** with the lexical form pinned to 20 ASCII characters because the value is hashed and echoed verbatim. **(d) An audit correction worth keeping.** The proposer is the participant whose **`group_id` value** is byte-wise lower - **not `group_a`**. The live example puts `"MaRs-777"` in `group_a` and `"GROUP-XY"` in `group_b`, and `"GROUP-XY"` is the lower value, so naming the slot would have been wrong in the project's own example. `group_a`/`group_b` are slots, not an ordering. **(e) Cadence.** Exactly two semantic requests per series in deterministic order; the non-proposer can answer the first request immediately, the proposer retains that digest until its own core becomes derivable, and both gates require **both directions** and **equal digests**. Transport retries re-send the same immutable request. **(f) Result.** `RESULT-APPROVAL-CORE-JOINT-DERIVABILITY: RESOLVED-PROJECT`; **all eight families are implemented or ready, none blocked**; `ResultContribution`, `ResultContributionEntry`, `UtcTimestamp`, `GitCommitSha` and the `Sha256Digest` response are supporting values and operation results, **not a ninth family**; FIELD_MATRIX unchanged at **74 = 15/39/9/11** with the repaired **result (11)** prose; 0 Python, 0 tests; no requirement, port, error ID or register ID added. **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE` stays BLOCKED-BY-CONSTRUCTION and Stage 4E is NOT COMPLETE.** |
| D45 *(4E-R14 family)* | **The pregame semantic value layer is implemented, and the two pre-code gates that stopped the first attempt were both real.** **(a) Stopping produced the contract.** R14 wrote **0 files**: `Declaration` was named by a frozen peer-message shape but had never been defined, and four `InteropProfileSet` members were *concepts with cross-references* rather than value vocabularies - enough to prove the lock context binds them, not enough to construct a typed value. **(b) One type, three lifecycle moments.** `Declaration` is a single immutable value whose instances are the partial producer snapshot, the merged snapshot and the final snapshot; later moments construct new values. No `declaration_state`, no phase, no `sender_id` - and the 15 declaration rows split **12 subject + 3 envelope**, so the `AuthProof` travels beside the subject data and there is exactly one authoritative auth value. The one structurally frozen lifecycle rule is enforced: a partial snapshot cannot carry `game_end`. **(c) Hash membership is not a type argument.** R14-R1 retyped `ram_gb` to `Decimal` reasoning that it sits in the hashed Step-0 core. That is wrong: `Decimal` keeps a *fractional* quantity exact, an integral quantity is already exact, and widening it would change canonical bytes for nothing. Repaired to strict `int`, with the faulty rationale recorded so it is not repeated. **(d) `vram_gb` was typed nowhere** - the row said only `number | GB`, the structural sentence was silent, and both examples declare `"gpu": false` so neither prints a literal. It was **not** inferred from a neighbour; the supervising ruling froze `int | None` with its `gpu`-dependent presence as PROJECT-CONTRACT. **(e) Counts are load-bearing.** Ten closed profile types, 17 enum-member memberships, 16 unique serialized tokens - the gap of one is deliberate, because `LECTURER_ATTACHMENT_COMPATIBILITY` is a member of two distinct types and neither is renamed to make strings globally unique. Four types have exactly one v1 member, kept rather than elided so the lock context binds an explicit token instead of an implicit assumption. **(f) Implemented means the value layer.** 9 new modules, 313 new tests per repo, 100.00% statement and branch coverage, every file <=150 LOC, production byte-identical across repos, no `# pragma: no cover`. **No network, transport, key loading, HMAC/Ed25519 computation, serializer, `ResultAgreement` or token-accounting crypto was added**, so Step-0, config negotiation and config lock stay **READY** - a semantic type existing is not a protocol working. Matrix **8 = 4/4/0**; FIELD_MATRIX **74 = 15/39/9/11**; `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE` still BLOCKED-BY-CONSTRUCTION; **Stage 4E is NOT COMPLETE.** |
| D46 *(4E-R15)* | **The result semantic value layer is implemented, and the three invariants it enforces were chosen by one test: is everything being compared already inside this immutable object?** **(a) Six entries, 1..6, ascending.** A single tuple comparison rejects a short set, a seventh entry, a duplicate, a gap, an out-of-order tuple and an empty tuple - and it never sorts, deduplicates or repairs, because silently fixing a malformed contribution would hide exactly the defect the peer needs to see. **(b) One commit across all six.** A participant's played commit is fixed for the game, so six differing values inside one contribution are self-contradictory before any peer, artifact or network exists. Comparing that commit against the real `Declaration` is a different question with a different owner and stays LIVE. **(c) The declaration join.** `declaration_ref` must equal `f"declaration_{game_id}.json"` because both values sit in the same value and the Table-20 join is frozen; a reference naming another game is incoherent on its face. Path prefixes, alternate extensions and whitespace are refused, never trimmed. **(d) What was deliberately left out.** `ResultAgreement` carries **no** `result_sha256`: the common digest cannot exist until a peer holds the opponent's contribution, so it is the operation's *response* - the already-existing `Sha256Digest` - and no response class, ack or ninth family was created. No proposer, cadence, approval-core assembly, hashing, transport or reporting was built, so the family stays **operationally READY**: a semantic type existing is not a protocol working. **(e) The guards earned their keep.** Four Stage-4E-R7 architecture tests failed immediately because they encoded the pre-R15 world - `ResultAgreement` absent from the façade, two classes in the finalization module, an exact import set. They were updated to the new truth without being weakened, a positive guard was added that the façade exposes the family and hides its support values, and the file was split when the edit pushed it past 150 LOC. A guard that fails loudly on a legitimate change is doing its job. **(f) Result.** 96 new tests per repo, 1667 / 1662 totals, 100.00% statement and branch coverage, all files <=150 LOC, production byte-identical across repos; matrix **8 = 4/4/0**; FIELD_MATRIX **74 = 15/39/9/11**; `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE` still BLOCKED-BY-CONSTRUCTION; **Stage 4E is NOT COMPLETE.** |
| D47 *(4E-R16)* | **The peer application runtime is implemented, and four rulings shaped it.** **(a) Ports, because `app` may not import `protocol`.** The layer rule is absolute, so every byte, digest and keyed proof reaches the runtime through an `app.ports` Protocol implemented by a `protocol` adapter. That is not ceremony: it is why the four runtimes are synchronous, deterministic, free of canonicalization and key material, and testable with plain fakes - and a guard asserts the rule per module from the AST rather than by grepping prose. **(b) `hardware.vram_gb` is the sole conditional member.** The Step-0 inventory is **19** semantic members and never varies; the *serialized* core carries **18** present leaves for a CPU-only producer and **19** for one with a GPU. The key is **omitted, never `null`** - and could not be `null`, because the canonical layer refuses `None`, so emitting one would make a lawful CPU-only declaration impossible to authenticate at all. Stage 4E-R16 shipped this correctly and *under-proved* it; supervising review caught the unconditional count wording, and R16-FIX proved the production code right rather than changing it. A stage that discovers its own code was already correct still owes the proof. **(c) The framing was never actually fixed, and now is.** Every live statement wrote `context ‖ canonical(core)` and deferred to "a fixed, unambiguous framing agreed pre-match" that no document defined - so two conforming peers could have computed different bytes from the same core. CLOSE freezes it byte-exactly: `b"step0"` / `b"config"` immediately followed by the canonical object bytes, prefixes `b"step0{"` / `b"config{"`, **no separator and no length prefix**. It is unambiguous because neither label prefixes the other and a canonical object is self-delimiting from its `{`. Recorded as **PROJECT-CONTRACT completing JDEC-013**, explicitly **not** as lecturer-mandated, and no JDEC or NDEC was added. **(d) What the matrix does and does not say.** All eight peer families now have executable **local application** behaviour, so the application-runtime matrix is **8 / 0 / 0 / 8**. It does **not** say transport exists: no FastMCP, socket, tunnel, wire adapter, Gmail, GUI or artifact persistence was written, and the words network-ready, league-ready and counted-match-ready are all false today. **(e) Result.** 293 R16 tests, 1959 / 1954 totals, 100.00% statement and branch coverage, every file <=150 LOC, all 16 production files byte-identical across repos; registers unmoved and FIELD_MATRIX **74 = 15/39/9/11**; `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE` still BLOCKED-BY-CONSTRUCTION; **Stage 4E is NOT COMPLETE.** |
| D48 *(4E-R17-R1)* | **The transport prerequisites are frozen, and the stage is worth reading for why it began with a stop.** **(a) The stop was correct.** R17 was authorized to implement the adapter and instead changed **0 files**, because `fastmcp` was neither declared nor locked nor importable - the project had no runtime dependency whatsoever - and because `PRD02-FR-035` and **O7** deferred every signature, leaving three variants sharing `negotiate`, three sharing `receive_turn` and two sharing `submit_audit` with no frozen way to tell them apart. Guessing which payload keys happened to be present would have produced an adapter that two independent implementations could never agree on. **(b) The envelope.** One tool argument `request` carrying exactly `{kind, payload}`, both required, a closed nine-token lowercase vocabulary, no heartbeat and no alias; an unknown token **or a valid token sent to the wrong tool** is `E-PROTO-MALFORMED`, with no cross-tool redispatch. The framework enforces it - `additionalProperties: false` at both levels - rather than the prose asking politely. **(c) The Decimal finding, which justified the whole probe.** Measured against a real Streamable HTTP server, a `Decimal` parameter handed the JSON *number* `0.10` arrives as `Decimal('0.1')`. That is silent, it changes `config_sha256`, and it would make two honest peers refuse each other for a reason neither could see. The wire therefore carries **canonical decimal text**, reconstructed by `Decimal(text)` with no float in the path, and `config_sha256` was proved identical before and after. Note the subtlety a weaker test would have missed: `Decimal("0.10") == Decimal("0.1")` is **True** - only the canonical text, and therefore the digest, separates them. **(d) Pydantic is direct because we import it.** A `dataclass` and a `TypedDict` both **accept** an extra envelope member; only `ConfigDict(extra="forbid")` refuses it. Since production will call that API itself, the project owns the dependency rather than inheriting it through FastMCP - and adding the pin moved no resolved version. **(e) What this is not.** No transport was implemented; a test asserts no `src` module imports `fastmcp` or `pydantic`. The envelope is transport, not semantics: peer families remain **8**, no ack family was created, no error ID, requirement, port or `FIELD_MATRIX` row was added, and the **8/0/0/8** matrix still describes local application behaviour only. **Stage 4E is NOT COMPLETE.** |
| D49 *(4E-R17)* | **The local FastMCP peer transport is implemented and closed, and the result-agreement completion workflow is now production code rather than a set of unreached helpers.** **(a) Both stops were correct and neither is smoothed over.** The stage took eight passes and both stops are recorded because both were correct. **(1)** Original **R17** was authorized to implement the adapter and changed **0 files**, stopping on two genuine blockers: `fastmcp` was neither declared, locked nor importable, and the wire envelope shape was unfrozen. **(2) R17-R1** provisioned and froze the prerequisites - `fastmcp==3.4.6`, `pydantic==2.13.4` as a **direct** dependency, and the `request = {kind, payload}` envelope. **(3) R17-RESUME** implemented the local transport: 19 `transport.*` modules, four tools, nine kinds, the canonical decimal-text codec and two-process Streamable HTTP. **(4)** Supervising review found the result cadence under-proved, timeout/watchdog ownership unproved and the error inventory incomplete; **FIX** completed all three. **(5)** Review then found that a digest mismatch produced only a quiet not-agreed, that the test harness duplicated the digest algorithm, and that no `PeerTransportPort` abstraction existed; **FIX2** added `require_matching_digest`, a production-delegating harness and the real port/adapter separation. **(6)** The **first CLOSE hard gate then discovered that the new guard itself was still unwired**: `require_matching_digest`, `MutualAgreementGate` and `ResultDigester` each had exactly one hit under `src/` - their own definition - so the comparison was invoked only by a test. That CLOSE **stopped with 0 files changed** on `RESULT-DIGEST-DISAGREEMENT-INTEGRATION: BLOCKED-BY-UNWIRED-PRODUCTION-GUARD`. **(7) FIX3** introduced `app.result_exchange.ResultExchange`, the application owner that wires the `PeerTransportPort` response to the local production digest, to `require_matching_digest`, to direction verification and to `MutualAgreementGate`; the successful and mismatching two-process cadences now drive production and the harness computes nothing. **(8)** This CLOSE. **(b) What the second stop actually proved.** A guard with no caller is not a guard. `MutualAgreementGate` and `require_matching_digest` were both written, both tested and both unreachable from `src/`; the CLOSE gate that greps for a production caller is what caught it, and the lesson generalizes: *a test that calls the guard directly proves the guard works, not that anything uses it.* **(c) The resolution.** `app.result_exchange.ResultExchange` owns one agreement end to end - cadence, digest, comparison, verdict - depending on `PeerTransportPort` and `ResultDigestPort` and on **no** transport type. It computes the digest exactly once, through `result_core_runtime.assemble()`, so there is no second algorithm to drift. **(d) The two directions stay distinct.** `PeerOperations` (inbound, `transport.handlers`) and `PeerTransportPort` (outbound, `app.peer_transport`) are mirrors with disjoint method names and no shared base; `FastMcpPeerTransport` satisfies the port **structurally**, with no `cast` and no `type: ignore`, and `app` imports `transport` **zero** times. **(e) No register grew.** `PeerTransportPort` is the implementation of an already-frozen architecture, not a new entry: no new peer family, error ID, requirement, `FIELD_MATRIX` row, port-register ID, C/JDEC/NDEC/INV row, dependency or CI workflow was created. **(f) Scope, stated plainly.** Local FastMCP Streamable HTTP is implemented and verified between two independent OS processes. **Public transport is not implemented and not tested** - no tunnel, no externally reachable endpoint, no external reachability evidence, no reconnect evidence, no real opponent, no counted match. **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`** is carried forward untouched. **Stage 4E is NOT COMPLETE.** | The blocker was mechanical, not stylistic: a genuine peer disagreement would have produced a quiet not-agreed and blamed nobody, because the only code that could raise `E-REPORT-DISAGREE` for a digest mismatch was never reached from production. Wiring it required an owner, and the owner had to live in `app` - the comparison is an application decision, and the transport must not decide whether a digest agrees. Putting it behind `PeerTransportPort` is what lets that owner be tested against a fake with no framework present, which is also why the two-process harness could be reduced to fixtures and dependency injection with `0` occurrences of `hashlib` or a hand-rolled digest. Closing here rather than continuing is deliberate: local transport is proved, public transport is untouched, and conflating the two would be the exact overclaim this stage spent two stops avoiding. |
| D50 *(4E-R18-R1)* | **Stage 4 closes at the infrastructure/public-transport boundary, and the missing application runtimes are moved to Stage 5 rather than absorbed here.** **(a) What is closed.** Our group's provider-neutral public ingress is implemented and live-verified: ngrok 3.39.10 as the first concrete outer adapter, bounded structured discovery over the measured `/api/tunnels` shape, three non-interchangeable endpoint identities, the exact ten-check `PRD05-FR-021` readiness gate with a production caller that refuses counted play, declaration-before-authentication, `FR-015b` replacement and same-ingress recovery, `infra.settings`, `infra.clock`, and a persistent FastMCP session on `PeerClient`. **(b) What is not closed, stated plainly.** **Closed at the infrastructure/transport boundary, and nowhere further.** This close does **not** claim a runnable production agent: there is no composition root, no BOOT, no executable entrypoint, and five inbound families - `Commitment`, `Acknowledgement`, `Reveal`, `FinalNonceReveal`, `AuditDocument` - have **no production application runtime owner at all**, which Stage 4E-R18-R1-CR2 established mechanically and which carries into **Stage 5 - Production Application Runtime Integration**. **(c) The evidence correction.** **What the public evidence does and does not say.** The live proofs are valid **transport** evidence: a real payload crossed a real ngrok HTTPS route, reached the real FastMCP server and router, and returned the frozen response contract. For **Step-0**, **ConfigProposal** and **ResultAgreement** the far side was a real production runtime - `Step0Runtime` raised `E-PROTO-STALE` and `E-AUTH-FAILURE`, `ConfigNegotiationRuntime` raised `E-NET-CONVENTION-MISMATCH`, and `ResultExchange` produced and verified the digest for the exercised direction. For the **five families above the far side was a test double**, because no production runtime exists to be there. The earlier reports' wording implied production application handling for all nine; that is corrected here rather than preserved. `Reveal` specifically: the public wire/tool `bool` contract is **verified**; the production inbound turn runtime is **missing**. `LocalTurnService` remains valid and independently tested, and is not that runtime. **(d) Why the boundary is here.** CR2 showed that completing `PeerOperations` would require implementing turn ordering, commitment-reveal verification, legality dispatch, nonce finalisation and audit acceptance **inside transport** - which `DEPENDENCY_RULES.md` forbids and which would have been the fourth occurrence of this project's recurring defect, after the R17 unwired digest guard and the R18 unwired session lifecycle. Moving it to Stage 5 is a scope correction, not a waiver: the work stays mandatory before a runnable agent, real-opponent readiness or any counted match. **(e) No register grew.** No new source requirement, peer family, peer error ID, `FIELD_MATRIX` row, JDEC, NDEC, INV or port. The three `E-NET-*` readiness tokens remain **local** `PublicReadiness` reasons that never cross `ToolError`. | The alternative was to let a transport stage keep growing until it contained the whole application, which is how the boundary would have been lost quietly rather than decided. Closing here records exactly what is true - public ingress and public transport work, measured and repeatable - while refusing the two overclaims that were available: that the held session is used by production (nothing constructs it), and that all nine public families reached production runtimes (five reached test doubles). Both refusals cost nothing now and would have cost a great deal at the first real match. |
| D51 *(8A-1T)* | **The pinned KIT wire is implemented as a second envelope profile, and the process picks one before it boots.** **(a) Two registrations, never two schemas.** `negotiate`, `receive_turn`, `submit_audit` and `receive_control` exist under both profiles with the same four names and different argument shapes, and a process registers exactly one - selected by `--external-mode` out of band, before the server exists and before the client exists. There is no auto-detection, no key sniffing, no try-one-then-the-other and no downgrade after a failure: a schema carrying both families would ask a stranger to guess which one this run means, and a fallback would turn an integrity failure into a silent compatibility story. The same selection drives outbound arguments, so a peer cannot serve one wire and speak the other. **(b) The schemas were compared against the pinned peer's own, not against its source.** A running pinned server on its own FastMCP major (2.14.7, isolated venv - our `fastmcp==3.4.6` pin was not touched) published its four schemas; tool names, argument names, requiredness and the free-form message object are identical to ours. The single difference is `additionalProperties: false` at the *tool argument* level, from `strict_input_validation`: an unknown second **argument** is refused. The kit's extension seam is one level down, **inside** `message`, and stays open - which is what its own unknown-key row exercises. **(c) A KIT turn cannot become a `Reveal`, so none was invented.** That wire carries the sealed `commit` and the unsealed adjuncts in one message and discloses the action only in `submit_audit`. The commitment half maps exactly onto `Commitment` and reaches the unchanged `on_commitment`; an audit reveal **is** the JSON-native disclosure document `on_audit_disclosure` already took. No second state machine, no KIT branch in a game service, no fabricated action. **(d) The smell grid stays the peer's binary64.** Our physics is exact `Decimal`; converting the peer's cells would assert an equivalence that is `MODEL_FORM_MATCH` and **not** vector-exact, which is precisely the claim the scent audit refuses to publish. **(e) Sparring is blocked, and the blockers are named separately because they have different owners.** Against the pinned sparring peer the kit's own `doctor` reported **PEER LISTENING**, its real greeting was **accepted**, `receive_control` was accepted, and `receive_turn`/`submit_audit` were **refused `E-AUTH-FAILURE`** - the unchanged HMAC Step-0 gate, which was not bypassed to make a practice run green. The series then ended `SPAR-N09`: nothing drives our KIT outbound side yet. So **the wire is compatible**; sparring is blocked by (1) the auth profile, which the pinned message shape cannot satisfy in either direction, and (2) role/turn orchestration, which Stage 8A-2 owns. **(f) Scope, stated plainly.** No role alternation, no public ingress integration, no counted play, no strategy change, no scent arithmetic change. Nothing about a sparring run is league diversity evidence. | Naming the two blockers apart is the whole point: an auth gap and an orchestration gap look identical from a timed-out console and have completely different fixes. Collapsing them into "sparring failed" would have hidden that the bytes already work - and weakening HMAC to make the console look green would have bought a practice run at the price of the one guarantee the kit's own terms digest explicitly does not provide. |
| D52 *(8A-2R)* | **Role alternation is implemented at the group, not inside a role, and a full six-sub-game friendly now plays against the pinned peer.** **(a) The correction that started it.** Stage 8A-1T reported the pinned harness supported a fixed-role series; it does not. `--role` picks only the side it takes in **sub-game 1** and `netplay.py` calls `role_for(natural, n)` inside its per-sub-game loop, so roles alternate every sub-game and `serve` has no `--sub-games`. Stage 8A-2 stopped on that with **zero repository changes**, which is why this stage had a true starting point rather than a half-built one. Alternation is **reference/de-facto practice, not a book MUST** - the body of the book never states it - and it is implemented under the project's own existing `SeriesConvention.REFERENCE_ODD_EVEN_ALTERNATION`. **(b) Two role backends, one group.** `teams.<group>.mcp_endpoint` is group-level, so one stable MaRs-777 URL fronts a gateway that routes each sub-game to the Police or Thief **process**, in its own repository, on its own venv. Neither repository became a dual-role agent, neither imports the other (verified: `import mars777_thief` fails inside the police environment), no strategy was copied and `GROUP_CODE` did not move. The gateway owns the live sub-game, the convention, the route and a bounded handoff gate - and no board, legality, strategy, scent, digest, audit verdict or score. **(c) Handoff needed three states, not a boolean.** The peer greets for `n+1` while `n` is still draining its audits, so `ACTIVE(n)` / `SETTLING(n)` / `READY(n)` are all real; a next-game greeting waits, bounded, until it can be *assigned*, because answering `ok` for a greeting nobody will act on makes the opponent burn its whole connect budget on our politeness. Settlement is **signalled** by the backend that played it - HTTP silence cannot tell a peer that is thinking from a sub-game that is finished. **(d) The settlement defect, and its real cause.** The first live sub-game settled `CAPTURE` here and `timeout` there. Two causes: self-capture was being applied to the **police**, but rules 46/47 are the thief's and `BAR-004` explicitly lets the police stand on its own barrier - so a lawful placement was being read as a capture; and a terminal answer was not delivered before we stopped talking. Both fixed, and the second half of the same contract pinned with it: a side that owes nothing sends nothing, and a survival claim that already rode on the turn just sent is never repeated. **(e) The measured result.** Four processes, real FastMCP HTTP, isolated KIT venv, no patched kit: six alternating sub-games, one `game_id`, **every row agreeing on both sides**, the kit verifying our chain in all six and our crypto gate verifying its chain in all six, and the pinned `check_artifacts.py` passing every check over the peer's set. **(f) What it is still not.** `KIT_FRIENDLY_ONLY`, with `counted_capable` **derived** from the run class so nothing can set it: the source requires keyed Step-0 authentication and the pinned peer offers an unkeyed content agreement, and the sparring group is synthetic. HMAC is untouched, there is no fallback into friendly mode, and a friendly never reaches the counted runtime at all. **(g) The one thing not delivered.** Our **own** 14-file artifact set for a friendly: every writer is gated on the counted protocol - an authenticated Step-0 peer, a verified config lock, the strict sealed evidence and audit runtimes, and a mutual result agreement the pinned four-tool surface cannot perform at all. Producing them would have meant fabricating counted facts or building a second result engine; neither was done. | The alternative architecture - teaching one repository to play both sides - was available and is exactly what the two-repo isolation model exists to prevent. Putting alternation at the group level cost one gateway and one handoff value, and kept the property that makes the whole design auditable: the Police code cannot play as thief, because it does not contain a thief. |
| D53 *(8A-2F)* | **A KIT friendly persists development evidence, and the counted artifact contract is left exactly where it was.** **(a) The audit came first, and it decided the shape.** Each official writer was traced to its counted-only precondition: the declaration needs an authenticated Step-0 peer *and* a full peer subtree the pinned `identity` does not carry; the config needs `locked_evidence`, a **keyed** lock proof cross-checked against the config and model digests; the log needs the strict eight-member sealed evidence and audit runtimes; and the result needs `is_agreed` and hard-codes `"mutual_agreement": true`. A friendly satisfies **none** of the four, so it produces none of the four. **(b) The number fourteen was not worth a lie.** Forcing a fourteen-file mirror would have meant fabricating precisely the facts those files exist to record - a Step-0 that never authenticated, a lock that never happened, an agreement the pinned four-tool wire has **no operation to perform**. So the friendly writes its own bundle instead: one series document plus one per sub-game, under `friendly_*` names, through a store that **cannot** emit a counted filename, with `keyed_step0_authentication: ABSENT` and `mutual_result_agreement: ABSENT` stated out loud rather than left to inference. **(c) No second result engine, and no invented number.** Scores are `outcome_line`'s and totals are `cumulative_of`'s - the counted path's own authorities. A **group** total under alternation is a figure no contract fixes, so none is published: each row records the side we played and a reader derives what the pairing agrees. **(d) Two backends, one series.** Each role backend writes a contribution of settled facts and immutable references - never a board, a position, a barrier set or a nonce - and a collector merges them, refusing contributions that disagree on identity, because that would be two series rather than one. **(e) Persisting changes nothing about worth.** Writing files is the operation that most looks like promotion, so each promotion it must not cause is pinned separately: counted readiness still refuses on `STEP0_AUTHENTICATED`, no mail sender exists anywhere in `src`, no diversity or league field is emitted, and `"mutual_agreement"` appears in exactly one file in the whole tree - the counted result document a friendly cannot produce. **(f) The 8A-2R status correction.** That stage is `GAMEPLAY_INTEROP: PASS` / `FULL_STAGE: PARTIAL`, and its gateway module is recorded in the workflow log as **not** RED-first: the tests were written afterward as characterization tests, they are real, and no later stage may claim red-before-green for them. | The tempting move was to write fourteen files with three fields quietly softened, and it would have looked like the strongest possible result. It would also have put a false `mutual_agreement: true` into an artifact family whose entire purpose is to be the record two teams are judged on. An evidence bundle that says what is missing is worth more than an official-looking set that says something untrue. |
| D54 *(8A-2G)* | **JDEC-005's `game_id` alphabet is amended to `[A-Za-z0-9-]`, and the two remaining counted blockers are classified rather than papered over.** **(a) The provenance question decided it.** `NAMING_AND_IDENTITY.md` records that the book *names* `game_id` and `game_uid` (Ch 9 p.95) and does **not** fix their format - "neither is claimed lecturer-specified beyond the names themselves" - so the format is PROJECT-CONTRACT and this is the project amending its own decision, not weakening a source rule. **(b) It had to be amended.** `group_id` is SOURCE-EXPLICIT (App E #45: 8 characters, no spaces) and ours is `MaRs-777`; the kit derives `game_id` as `"-vs-".join(sorted(pair))`, so our own group code sits inside it verbatim. The lowercase rule refused a filename for a game we had actually played - found during the 8A-2F live run, by the development writer hitting a real derived id. **(c) Nothing was folded and nothing was loosened.** An identifier is refused or returned exactly as given: case-folding a filename would silently make two peers hash and compare different games. Separators, dot segments, absolute and drive prefixes, whitespace, control characters, NUL and empty are all still refused, and the traversal set is now pinned explicitly rather than implied by the alphabet. `game_uid`, terms, commitment bytes and result consensus are untouched. **(d) One validator, not two.** 8A-2F had added a second development-only id check purely because the counted one could not express a kit id; with the format corrected that reason is gone, so the development bundle now shares `require_game_id` and only its *filenames* stay separate. **(e) Result agreement: `PARTNER_EXTENSION_REQUIRED`.** `is_agreed` needs four facts - our digest, the peer's digest, our request sent, the peer's request handled - and in strict mode the peer's digest comes back as the **return value** of `receive_control(kind=result_agreement)`. The pinned wire cannot carry that: its control vocabulary is a closed four words, every tool answers `{"ok": true}` so no digest can ride the response, and the pinned driver never calls `receive_control` at all. The seam itself is already input-driven - `accept_peer_request` plus `_verify` close the gate the moment a peer digest arrives by any means - so what is missing is transport, not logic. **(f) Counted auth is documented, not built.** The exact non-secret contract is now in the handoff: HMAC-SHA256, a declared non-secret `key_id`, `b"step0"` followed immediately by the canonical JSON of the sender's own five-part Step-0 core, exchanged with the pre-game greeting, out-of-band key. It rides the pinned `negotiate` beside `terms`, so it cannot invalidate `terms_signature` and needs no schema change - with the caveat that an **unmodified** kit peer drops unknown keys and so can neither read nor produce it. Both sides must opt in. | The lazy fix was to lowercase the group code on the way into a filename. That would have produced a `game_id` our own peer could not reproduce, in the one field whose entire job is to make sure two teams' four files describe the same game - and it would have been invisible until a real pairing diffed two reports the next morning. |
| D55 *(8B-P)* | **The public path is wired, and the two unwired components that made it necessary are named.** **(a) The finding came first.** `KitGroupGateway` and `NgrokPublicIngress` both existed, both were tested, and **neither had a caller anywhere in `src/`** - the exact shape D48 recorded as this project's recurring defect. `KitPublicLauncher` is the seam, and it is thin on purpose: what a public endpoint *is* stays with the existing policy, and the launcher owns only the order and the release. **(b) The framework guard earned its keep again.** The first cut put `FastMCP` and `Client` imports in two root-level lifecycle files, and `test_only_the_transport_package_imports_the_framework_stack` refused them. The right answer was not to widen the allowlist: serving an ASGI app and holding an MCP session are transport mechanics, so they moved to `transport/kit_serving.py` and `transport/kit_admin_client.py` and the four inward layers stayed portable. **(c) One endpoint, discovered every run.** The banner prints the group URL and nothing else - no private backend address, no admin port, no key, no token - and closing forgets the endpoint, because a remembered hostname is a route to somebody else's tunnel. **(d) Proved live, through the real HTTPS URL**: exactly the four pinned tools with the right argument names including `submit_audit` → `payload`; `g01` routed to the police backend, settlement signalled, `g02` routed to the thief backend, the URL identical throughout; and the **real** counted-readiness authority still refusing while the tunnel was up, failing on d/e/f/g/h/i - every partner- and auth-dependent check. **(e) Overhead measured, then left alone.** 6.4 ms direct, 14.2 ms through the gateway, so ~7.8 ms for the hop against a 180 s turn budget; decode 3.1 us, encode 0.3 us, ~80 MB per gateway process. Optimising 0.004% of a turn would be premature. **(f) What this is not.** A loopback caller is not a student: this is a **PUBLIC GROUP-ENDPOINT PROOF**, not a real friendly. Outbound reachability, peer identity and cross-group interoperability remain unproven because there is no partner to prove them with. | The temptation was to add the two files to the framework allowlist and move on - a one-line change that would have passed. It would also have started the slide the guard exists to prevent, one honest exception at a time, until `app` imports FastMCP and nothing inward is portable any more. Moving the mechanics cost twenty minutes and kept the boundary exactly where it was. |
