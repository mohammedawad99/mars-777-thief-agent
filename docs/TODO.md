# TODO - group MaRs-777 (THIEF)

> **Status: DRAFT.**
> **Purpose:** Track outstanding foundation and project tasks.
> **Authoritative source:** book v3.0.0.
> **Note:** No requirement is approved merely because this file exists.

## Done
- [x] Stage 0A - environment / tooling / Git / GitHub preflight audit.
- [x] Stage 0B - local repository foundation.
- [x] Stage 0B.1 - final evidence audit (exact future-commit validation).
- [x] Stage 0C - private GitHub repository created under mohammedawad99; **SSH** origin configured; reviewed initial commit pushed to origin/main.
- [x] Stage 0C - first GitHub Actions CI run green (ubuntu-latest + windows-latest).
- [x] Stage 1-SYNC - reviewed COMMON Stage-1 specification baseline **synchronized** from Police locked commit `691280dc…` into `docs/spec/` (documentation only; adopted, not re-extracted here). See `SOURCES.md`, `DECISIONS.md` D15.

- [x] Stage 2A - architecture freeze: 21 architecture docs + 7 PRD blueprints; 91/91 requirements architecture-mapped; red-team blocking findings resolved.
- [x] Stage 2A-R - read-only lecturer reference audit (`rmisegal/Game-P2P-Cop-Chase` @ `960499fd`); reference classified NON-BINDING; book wins on keyed Step-0 auth.
- [x] Stage 2A-R2 - attachment/chatbot reconciliation (AE-01…AE-04, secondary provenance); compatibility profiles; **JDEC-014** result→declaration reference; field matrix **77 → 75** (result 13 → 11). Zero chatbot questions pending.
- [x] Stage 2A-CLOSE - stale-baseline sweep + `result_sha256` audit; architecture/compatibility baseline committed and pushed; CI green (Ubuntu + Windows).
- [x] Stage 2B - PRD-01…04 authored in full (reviewed).
- [x] Stage 2C - PRD-05…07 authored in full (reviewed); 91/91 requirements have exactly one primary owner.
- [x] Stage 2-CLOSE - **PASS**; CLOSE-F1 (series convention = negotiated metadata, **not** a declaration field) and CLOSE-F2 (declared MCP endpoint = stable group-level ingress) resolved with **no artifact-contract change** (matrix stays **75**); all 7 PRDs **APPROVED — PHASE 2 LOCKED**; implementation **NOT STARTED**.

### Phase 3 — Deterministic Core Implementation (started)
- [x] Stage 3A - deterministic domain **foundation** (tests-first): immutable `GridConfig` (project grid minimum enforced here), immutable `Position`, immutable policy-free `Board` geometry with blocked cells, `Move` = N/S/E/W/STAY, stable `MOVE_ORDER`, destination calculation, bounds/blocked legality, deterministic `legal_moves`, typed `apply_move` failure. Role-neutral, no opponent truth, no I/O. **Supervising review PASS.**
- [x] Stage 3A-CLOSE - final audits, narrow tracking update, commit + push + CI.
- [x] Stage 3B - deterministic **game semantics** (tests-first): barrier placement, the three capture routes, terminal/survival evaluation, role-keyed scoring and bounded scent physics. **Supervising review PASS.**
- [x] Stage 3B-FIX1 - supervising correction: **JDEC-015** terminal threshold admissibility (`survival_threshold <= max_moves`) + radial scent-kernel contract hardened; `UnspecifiedTerminalError` removed.
- [x] Stage 3B-FIX2 - supervising ruling: **C-10** scent state bound vs additive update resolved as the saturating recurrence `min(0.9, max(0, (1-rho)*tau + delta))`.
- [x] Stage 3B-CLOSE - final audits, tracking finalization, commit + push + CI.
- [x] Stage 3C - local application / turn orchestration **foundation** (tests-first): `LocalTruth` (board, own position, completed steps), typed `MoveAction`/`BarrierAction`, role-specific `LocalTurnService` (police move **or** barrier; thief move only), atomic local effect application, local step accounting and max-moves exhaustion. **Supervising review PASS.**
- [x] Stage 3C-FIX1 - state-ownership correction: removed the duplicated `barriers_placed` counter from `LocalTruth`; barrier usage now has **one** authoritative representation (the public board plus the validated `BarrierQuota`), so no local count can drift.
- [x] Stage 3C-CLOSE - final audits, PRD-02 status alignment, commit + push + CI.

### Phase 4 — Protocol Foundation (started)
- [x] Stage 4A - local protocol **state machine** foundation (tests-first): the frozen 18-phase graph (15 normal + 3 fault) with **31** legal directed edges, exhaustively verified over all 324 ordered pairs; `ProtocolMachine` owns **only** the current phase; normal bootstrap via `ProtocolMachine.start()` at BOOT. **Supervising review PASS.**
- [x] Stage 4A-FIX1 - **TECHNICAL_LOSS lifecycle correction**: the frozen table made "technical loss" an entry condition of SUBGAME_COMPLETE and told the phase to "proceed per series rules", while R5 names only TAMPERED and FAILED as never returning to play - yet it listed no successor. One edge added, `TECHNICAL_LOSS -> SUBGAME_COMPLETE`, recorded in `STATE_MACHINE.md` §4 as an implementation-discovered architecture correction. TAMPERED and FAILED stay absorbing.
- [x] Stage 4A-CLOSE - final graph/ownership audit, bootstrap guard, tracking, commit + push + CI.
- [x] Stage 4B - protocol event / **transition evidence** foundation (tests-first): every successful `advance()` now returns one immutable `TransitionResult` carrying a `TransitionEvidence` of exactly `source_phase` + `target_phase`, per R7 and `PROTOCOL_TIMELINE.md` event 10 (transmitted "phase transition", persisted "—", hashed "—"). No transition-signal vocabulary is frozen anywhere, so **no event enum was invented**; there is no evidence-less transition path and no compatibility shim. **Supervising review PASS.**
- [x] Stage 4B-FIX1 - supervising corrections: (a) `TransitionEvidence` is now **valid by construction** - the pair must be an edge of the single `_ALLOWED` graph, so a record of a step R1/R5 forbid (e.g. `COMMIT_SENT -> REVEAL`, `TAMPERED -> SUBGAME_COMPLETE`) cannot be built; 31 legal pairs constructible, 293 illegal pairs and all 18 self-loops rejected. The replay corruption boundary moved forward to the constructor and negative replay coverage was **extended** (duplicated record, out-of-position record) rather than weakened. (b) A reported "153-line" file was reconciled as a **cross-repo semantic-diff count**, not physical LOC; the repository-wide audit proves max **150** and **0** files over the limit.
- [x] Stage 4B-CLOSE - final evidence/graph invariant audit, repository-wide LOC proof, tracking finalization, commit + push + CI.
- [x] Stage 4C - local **orchestrator / protocol guard** foundation (tests-first): `LocalOrchestrator` composes the `ProtocolMachine` and owns the one live fact `STATE_OWNERSHIP.md` assigns it - the **current sub-game index** - plus a read-only `SeriesConfig` projection. It delegates every phase question to `ProtocolMachine.advance()`, passes the emitted evidence through unchanged, and decides exactly one branch itself (`SUBGAME_COMPLETE -> READY / SERIES_COMPLETE`) because the cursor truthfully determines it - no `more_subgames` boolean is accepted. Ownership conflicts resolved without touching frozen architecture: `MODULE_BOUNDARIES.md`'s "turn cursor" yields to `STATE_OWNERSHIP.md` (the D17 precedent), and role alternation stays a PRD-05/`SeriesLauncher` convention (PRD02-FR-011), never derived from odd/even index. **Supervising review PASS.**
- [x] Stage 4C-FIX1 - supervising correction: the counted series is **`num_games` = 6, FIXED**, not a floor. Stage 4C had implemented `>= 6` after following stale wording in `CONFIG_CONTRACT.md` and the C-05 impact cell ("6 or the agreed higher"); Appendix F defines FIXED as "binding, unchangeable; **deviation disqualifies**" and names `num_games` in its FIXED-14 list, so 7 is as illegal as 5. The error was caught **before commit**: `MIN_NUM_GAMES` became `FIXED_NUM_GAMES`, `SeriesConfig` now requires exact equality, and the two stale sentences were corrected to match the already-locked App F / C-05 semantics - no new JDEC, conflict, requirement or register entry. The bootstrap audit confirmed `start()` is the only runtime path and no production caller constructs a `LocalOrchestrator` directly.
- [x] Stage 4C-CLOSE - final FIXED-series/cursor/ownership audit, duplicate-literal and bootstrap audit, package-surface alignment, tracking finalization, commit + push + CI.
- [ ] Stage 4D - application **port contracts** foundation - **ATTEMPTED, BLOCKED BEFORE CODE** (`PORT-DEPENDENCY-BLOCKER`). Zero repository changes were made. Three internal contradictions prevented a truthful implementation: `API_BOUNDARIES.md` fixes **no Python signatures** (confirmed by a repository-wide search finding no `def`, `Protocol` or return annotation in any architecture or PRD document); the `app.ports` row allowed **stdlib typing only** while the same architecture defines ports over `Board`/`Move`/`Outcome`/`ScoreLine`; and `PLAN.md` still claimed 18 ports where the frozen inventory is 20. No signature, payload type or method name was guessed.
- [x] Stage 4D-R1 - application port **architecture reconciliation** (documentation only): `app.ports` may now reference immutable `domain` value types and nothing else outward; `API_BOUNDARIES.md` records that any future Python signature is **PROJECT-CONTRACT** (never source- or reference-mandated) and that a port row is not an automatic Protocol mandate; the port count is corrected to **20**. See **D22**. No requirement, PRD, register or Appendix-F value changed. **CLOSED** at Stage 4D-R1-CLOSE (committed + pushed; CI green Ubuntu + Windows).

## In progress
**Phase 2 — PRD and architecture — is fully complete.** **Phases 3 and 4 are under
way:** the deterministic game-rule layer (3A/3B), the local turn-execution step
(3C), the local protocol phase machine (4A), its transition evidence (4B) and
the local series orchestrator (4C) exist and are tested. The phase machine
enforces order only — the phases named COMMIT_SENT, ACKNOWLEDGED, REVEAL and
FINAL_AUDIT carry no cryptography, message bodies or transport, and the machine
never applies a local effect. Transition evidence is **structurally valid, not
authenticated**: it supports ordered **phase-path** replay only, never
game-state, movement, barrier, score, scent, commitment, nonce, network-message,
official-artifact or complete-game replay. The orchestrator implements its
**cursor/guard slice only** — a counted series is exactly 6 sub-games, the
cursor advances exactly five times and no seventh sub-game is representable.
**Recording the per-sub-game and cumulative score is also `app.orchestrator`
state per `STATE_OWNERSHIP.md`, and remains pending** a later stage, because it
needs truthful terminal facts that do not exist yet. **Not implemented:** score
recording, turn-service integration, application ports, FastMCP, networking,
cryptography, logger/replay persistence, JSON artifacts, strategy, belief, GUI
and reporting. PRD-01 and PRD-02 remain **IN PROGRESS**; the next stage is
tracked once, under Pending.
## Pending
- [ ] Branch protection / rulesets - **blocked**: unavailable on the current GitHub
      plan for private repos (Stage 0D). Needs Pro upgrade, org, or public-at-submission.
- [ ] Example-simulator review (non-binding).
- [x] PRD-01 game logic - authored and locked; implementation **IN PROGRESS** (Stage 3A foundation only; barriers, capture, terminal/survival, scoring and scent still pending).
- [x] PRD-02 local FastMCP - authored and locked; implementation **IN PROGRESS** (Stage 3C local turn foundation only; state machine, orchestrator, ports, FastMCP and runtime composition still pending).
- [x] PRD-03 baseline strategy (**THIEF** role-specific) - authored and locked; implementation not started.
- [x] PRD-04 language & scent - authored and locked; implementation not started.
- [x] PRD-05 public network - authored and locked; implementation not started.
- [x] PRD-06 security & cryptography - authored and locked; implementation not started.
- [x] PRD-07 reporting, GUI, replay - authored and locked; implementation not started.
- [x] **Phase 3 — Deterministic Core Implementation** — **STARTED** (Stage 3A closed; the phase itself is **not** complete).
- [x] **Stage 3B — Deterministic Game Semantics** — **CLOSED** (barriers, capture, terminal/survival, scoring, bounded scent physics).
- [x] **Stage 3C — Local Application / Turn Orchestration Foundation** — **CLOSED.**
- [x] **Stage 4A — Local Protocol State Machine Foundation** — **CLOSED.**
- [x] **Stage 4B — Protocol Event / Transition Evidence Foundation** — **CLOSED** (deterministic per-transition evidence, valid by construction against the single frozen graph; phase-path replay only). No event enum was invented: no transition-signal vocabulary is frozen in any source or architecture document.
- [x] **Stage 4C — Local Orchestrator / Protocol Guard Foundation** — **CLOSED** (sub-game cursor, the one cursor-owned branch, evidence pass-through; `num_games` = 6 FIXED). Its **score-recording** half remains open below.
- [ ] **Orchestrator score recording** — **NOT STARTED; blocked on truthful terminal facts.** `STATE_OWNERSHIP.md` assigns the recorded per-sub-game and cumulative score to `app.orchestrator` (computed by `domain.scoring`, anti-duplication rule 3). Stage 4C deliberately did not implement it: the input is a sub-game `Outcome`, which needs verified public facts the local agent does not yet have.
- [ ] Stage 4E - protocol semantic message contracts foundation - **ATTEMPTED, BLOCKED BEFORE CODE**; zero repository changes. Two blockers: `MESSAGE-PHASE-DEPENDENCY-BLOCKER` (the one authoritative `ProtocolPhase` lives in `app.state_machine`, which `protocol.messages` may not import) and `MESSAGE-ENVELOPE-CURSOR-BLOCKER` (PRD02-FR-044's `(sub_game, step, phase)` has no defined pre-turn `step`, no Step-0 `sub_game` meaning and no `phase` semantics). The audit also found the peer-visible inventory is **10**, not the **9** previously planned: Event 8 "Move validation" transmits accept/reject and had been omitted. Nothing was guessed.
- [x] Stage 4E-R1 - semantic message architecture / turn-cursor **reconciliation** (documentation only): `app.peer_messages` added as an application contract module (a semantic value the Turn Executor consumes cannot live in `protocol`, because `app` may never import `protocol`); `protocol.messages` becomes the wire ⇄ semantic mapper; **PRD02-FR-044 narrowed** to turn-scoped messages carrying `(sub_game, step)`, with the phase check staying receiver-side per FR-062/STATE-003; peer-visible family inventory corrected to **10**. Both Stage-4E blockers are resolved. See **D23**.
- [x] Stage 4E-R1-FIX1 - turn-cursor consistency + sub-game-control identity (documentation only): three current contracts still defined the cursor as `(sub_game, step, phase)` — PRD-02 §8, `CONCURRENCY_MODEL.md` guard 2 and `SECURITY_ARCHITECTURE.md` T3 — and were corrected to `(sub_game, step)` with receiver-side phase admissibility. `step <= max_moves` was reclassified **contextual** (locked per-sub-game config), leaving only `step >= 1` structural; sub-game-control identity is explicit semantic context rather than "implied by artifact `g<NN>`"; and `app.peer_messages` is worded as values application control flow consumes **and produces**. See **D23a**.
- [ ] Stage 4F - protocol value representation contracts foundation - **ATTEMPTED, BLOCKED BEFORE CODE** (`VALUE-MODULE-HOME-BLOCKER`); zero repository changes. Nine representations were audited: seven were genuinely unresolved and deferred rather than guessed, and the two that were representation-ready (SHA-256 digest, final-audit verdict) had no module home reachable by their consumers. Supervising review then caught two further defects in the report itself: the digest was called ready while its case policy was called unresolved, and `RESULT_CONTRACT.md` was found to define `mutual_agreement` as both a bool and an object.
- [x] Stage 4F-R1 - shared protocol value architecture / representation **reconciliation** (documentation only): `app.protocol_values` added as an application contract module for shared protocol semantic primitives, with narrow type-reference access for `protocol.commitment` and `protocol.config_lock` only — `infra` needs nothing, because its logger/artifacts/reporter are mappers, so `DEPENDENCY_RULES.md` is unchanged. Digest representation resolved as canonical **lowercase 64-hex PROJECT-CONTRACT** (hex is project-locked, length entailed, case was free — `.hexdigest()` is reference-only), with uppercase rejected rather than normalized. Final-audit verdict confirmed as the closed pair `Verified OK` / `TAMPERED`. The `mutual_agreement` scalar/object contradiction corrected in favour of the bool defined by the field table, scoring rule and example. See **D24**. **CLOSED** at Stage 4F-R1-CLOSE (committed + pushed; CI green Ubuntu + Windows), together with **Stage 4F-R1-FIX1** (runtime-use dependency for the two protocol producers; `app.protocol_values` allowed the pure stdlib value-definition primitives; readiness accounting corrected to 2/6/1). See **D24a**.
- [x] Stage 4F — **RESUME implemented and CLOSED** at Stage 4F-CLOSE (committed + pushed; CI green Ubuntu + Windows). Tests-first in both repos: RED was `ModuleNotFoundError: app.protocol_values`, then 55 targeted tests green. **Stage 4F-RESUME-FIX1** followed on supervising review with a second RED of 6 failures — `InvalidDigestError` was not a `ValueError`, it was missing from the exhaustive app surface, and a malformed digest was therefore not catchable generically — ending at 58 targeted tests. Delivered exactly **two semantic values** in `app.protocol_values`: `Sha256Digest`, validating the **PROJECT-CONTRACT** canonical lowercase 64-hex representation with uppercase rejected and never normalised, performing **no hashing**; and `FinalAuditVerdict`, exactly `Verified OK` / `TAMPERED`. `InvalidDigestError(ValueError)` is a **supporting error contract, not a third semantic value**, publicly exported alongside them. See **D25**.
- [x] **Stage-4E resume preflight debt: re-audit `app.peer_messages` permitted stdlib machinery** — **PAID at Stage 4E-R2.** The row allowed only "stdlib typing" and could not name `app.protocol_values`. It now allows the pure stdlib value-definition primitives (`typing`, `dataclasses`) it actually needs, immutable `domain` value types, and `app.protocol_values` at **runtime use**. `enum` was **not** granted: no peer message defines a vocabulary of its own. See **D26**.
- [x] **Stage 4E-R2 — Peer Message Runtime Contract Readiness Reconciliation** — **CLOSED** at Stage 4E-R2-CLOSE (committed + pushed; CI green Ubuntu + Windows), together with **Stage 4E-R2-FIX1** (NDEC-006 corrected to the separate-field result model plus an explicit record-shape-is-not-message-shape scope limit; the `TurnCursor`/`Commitment` static-construction contract decided as built-in `ValueError` with **0** supporting error types) and **Stage 4E-R2-FIX2** (the same withdrawn nested form propagated out of **PRD06-FR-142**, **PRD07-FR-085** and **PRD07-FR-190**, and `app.peer_messages` narrowly widened to read the two globally-FIXED sub-game constants). Documentation/design only; no Python. The `app.peer_messages` capability and `app.protocol_values` dependency debts are paid, acyclicity is proved structurally rather than by convention, and all ten peer-visible families were re-audited against the ten readiness criteria. Result: **1 READY**, 9 blocked. `TurnCursor` is **TURN-CURSOR-READY** and belongs in `app.peer_messages`, not `app.protocol_values` — no outer protocol implementation needs it, and it stays a projection of `app.orchestrator`'s sub-game index and `domain.truth`, never a second owner. See **D26**.
- [x] **Stage 4E RESUME (selected subset: `TurnCursor` + `Commitment` only)** — **DELIVERED**; see the slice-close entry below.
- [ ] **Family-6 (Acknowledgement) preflight: freeze the acking-role question** — **BLOCKS Family 6; NOT STARTED.** Two of its three sub-questions are now resolved — PRD06-FR-082 binds receipt of a *specific* `H_commit` for a *specific* `(sub_game, step)`, so the commit digest **is** carried and `ack_of_step` **is** the cursor's step rather than a second field. What is not frozen is whether the acking-role identity (`LOG_CONTRACT.md` §C `by_role`, Required, SOURCE-SEMANTIC) is a **transmitted** semantic field or a log-side attribution derived from who sent the message. Freeze that, then Family 6 is `TurnCursor` + `Sha256Digest`.
- [ ] **Family-7 (Reveal) preflight: freeze how a barrier placement is revealed** — **BLOCKS Family 7; NOT STARTED.** PRD-01 makes a legal action "a move in `move_set` **or** a police barrier placement" with exactly one action per turn (PRD01-FR-010), and BAR-001/BAR-002/PRD01-FR-035 make every placement and its **exact location** openly and truthfully declarable. But PROTOCOL_TIMELINE event 7 and `LOG_CONTRACT.md` §D describe the reveal as `move` + `hint` only, and **no contract says how the placement cell reaches the peer**. Until that is frozen, a `Reveal` of `(cursor, Move, hint)` would be structurally unable to express a legal police turn.
- [x] **Family-14 preflight (part 1): `mutual_agreement` object-form residue in NDEC-006** — **CORRECTED at Stage 4E-R2-FIX1.** NDEC-006 now states the procedure (build core → compute `result_sha256` → exchange → compare → set the flag) and the record shape (`result_sha256` and `mutual_agreement` as **separate** fields), with an explicit scope limit recording that it does **not** freeze Event-14 message multiplicity. No replacement nested object; NDEC count still **7**; result fields still **11**. See **D26a**.
- [x] **Family-14 preflight (part 2): the object form in three PRD requirements** — **CORRECTED at Stage 4E-R2-FIX2.** **PRD06-FR-142** now excludes "`result_sha256` itself, the `mutual_agreement` flag"; **PRD07-FR-085** now stores the digest as a **separate top-level result field** beside the separate top-level `mutual_agreement` bool, agreeing with **PRD07-FR-080** in its own table; **PRD07-FR-190** now requires both reports to carry the same `result_sha256` and record `mutual_agreement = true` as a separate field once the digest comparison establishes equality. All three keep their ID, modality, ownership, ordering and provenance; no requirement was added, deleted or renumbered. **Zero current project contracts now assert the nested form** — it survives only as historical annotation and as external attachment evidence, both deliberately left intact. See **D26b**.
- [ ] **Family-14 preflight (part 3): Event-14 message flow** — **BLOCKS Family 14; NOT STARTED.** Independent of the data shape: `RESULT_CONTRACT.md` sets `mutual_agreement` **after** the exchange, so no single message can truthfully carry both its own `result_sha256` and an agreement verdict. Freeze the message multiplicity (one message / request-response / offer-confirm / digest echo), the disagreement signal (`false` vs absence + `E-REPORT-DISAGREE` / C-09) and message-layer identity together. **Not decided at Stage 4E-R2-FIX1 by instruction.**
- [x] **Stage-4E static-construction error contract for `TurnCursor` and `Commitment`** — **DECIDED at Stage 4E-R2-FIX1; DESIGN ONLY, no Python.** Both raise **built-in `ValueError`** for every malformed static construction; **0 supporting error types**. Precedent: `FinalAuditVerdict` — a semantic value in the sibling contract module — already raises native `ValueError`, and a test asserts exactly that; `InvalidDigestError` is itself a `ValueError` **subclass**, so one `except ValueError` uniformly catches malformed construction across `app.protocol_values` and `app.peer_messages`. `TypeError`-for-type / `ValueError`-for-range was rejected (no project value raises `TypeError`; it makes `bool` ambiguous); a narrow module-local subclass was rejected as unnecessary public API **and** is a strictly non-breaking later addition if a future family needs named diagnostics. No `ProtocolMessageError`, no `E-PROTO-STALE`, no `IllegalTransitionError`, no `DomainError` reuse. See **D26a**.
- [x] **`TurnCursor` fixed-constant dependency** — **RESOLVED at Stage 4E-R2-FIX2.** The `app.peer_messages` row permitted immutable `domain` value **types**, which names classes and does not authorize the two module-level `Final[int]` constants `FIRST_SUB_GAME` / `FIXED_NUM_GAMES` that the frozen bound `FIRST_SUB_GAME <= sub_game <= FIXED_NUM_GAMES` requires. One narrow widening now names exactly those two, read-only. A literal `6`, a copied constant and a `SeriesConfig`-derived bound were all rejected with reasons. Mechanically verified: `FIXED_NUM_GAMES: Final[int] = 6` in `domain.config_model` is the **only** `= 6` series length in the source tree, and `app.orchestrator` already imports `FIRST_SUB_GAME` from that module at runtime — so no new layer edge. See **D26b**.
- [ ] **Protocol-stage preflight debt: `protocol.messages` → `app.peer_messages` says "type references only"** — **MANDATORY BEFORE `protocol.messages` is implemented; NOT STARTED.** A wire⇄semantic mapper constructs semantic values, so annotation-only is the same defect corrected twice already (`app.ports` wording copied to `app.protocol_values` at 4F-R1-FIX1, and to `app.peer_messages` at 4E-R2). Not corrected at Stage 4E-R2 because `protocol.messages` is not resuming — the identical reason Stage 4F-R1-FIX1 gave for leaving `app.peer_messages` alone.
- [x] **Stage 4E — RESUME: TurnCursor + Commitment Foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-RESUME-CLOSE. **This closes one slice, not Stage 4E: nine of the ten peer-visible families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Tests-first in both repositories, with the RED observed twice — first `ModuleNotFoundError: No module named '<pkg>.app.peer_messages'`, then, after the module existed, an `AttributeError` because the exhaustive app surface did not yet export `TurnCursor` — closing at **73** targeted tests. Delivered exactly two types in `app.peer_messages` and **0** supporting error classes. `TurnCursor` is a frozen, slotted `(sub_game, step)` projection with **no phase**: strict `int` (`bool` refused), `FIRST_SUB_GAME <= sub_game <= FIXED_NUM_GAMES` read from the one authoritative `domain.config_model` contract, `step >= 1` with **no** context-free ceiling because `max_moves` is per-sub-game locked config and stays LIVE. `Commitment` is exactly `cursor: TurnCursor` + `h_commit: Sha256Digest` — event 5's "`H_commit` only" plus the independently-required cursor — using **exact type identity** (subclasses refused), **no coercion** (a valid 64-hex `str` is refused rather than wrapped), **no hashing** and no privacy-leaking field. Malformed static construction raises the built-in `ValueError`. See **D27**.
- [x] **Stage 4E-R3 — Acknowledgement + Reveal Contract Reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R3-CLOSE. Documentation/contract only; **0 Python**. **This closes one reconciliation, not Stage 4E: eight families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Re-audited both families from the **authoritative PDF** rather than from our derived docs. **Acknowledgement is now READY-TO-IMPLEMENT** as exactly `cursor: TurnCursor` + `h_commit: Sha256Digest`: Ch 5 §5.3.2 (p.51) says only that *the opponent confirms it received the commitment and is locked onto it*, and **`by_role` is LOCAL-LOG-ATTRIBUTION, not transmitted content** — Figure 6 (p.52) carries the acknowledging party as the message's direction, and the local writer can always derive it (one opponent process per ARCH-001/002, role immutable from `CONFIG_LOCKED` per PRD06-FR-048, and it knows whether it sent or received). Because role is never transmitted there is nothing to forge. **Reveal remains BLOCKED**, but its *source* ambiguity is resolved: Ch 5 p.51 defines the sealed `Move` as *"the physical action; the chosen action (movement, **barrier placement**, etc.)"*, and Ch 3 p.37/38 require every placement **and its exact location** to be declared truthfully and never hidden — so a barrier travels in the ordinary `move` action slot, bound by the same `H_commit`, and **no eleventh family is needed**. See **D28**.
- [x] **Reveal blocker 1: no shared action semantic type in a reachable home** — **DESIGN FROZEN at Stage 4E-R4; implementation NOT STARTED.** The home is confirmed **domain-owned**: the physical action is game-domain semantics (PRD-01 owns the legal action space), it exists before any transport, and `domain` is the one layer reachable by all three consumers — `app.turn_service` (deps `domain`), `app.peer_messages` (deps allow immutable `domain` value types) and the future `protocol.commitment` (deps `canonical, domain, app.protocol_values`) — while `domain` imports none of them, so the graph stays acyclic. R4 freezes it as a **new** `domain.actions` module (not `domain.rules`, which is already 118 LOC and would break the 150-line rule) holding `MoveAction(move: Move)`, `BarrierAction(target: Position)`, the alias `PhysicalAction`, and the `ActionKind` tag vocabulary moved down from `app.turn_service` so exactly one tag authority exists. Structural validation only, raising `InvalidPhysicalActionError(DomainError)`. What remains is Python, in one atomic slice with the `turn_service` migration.
- [x] **Reveal blocker 2: the sealed-record `move` representation is not frozen** — **RESOLVED at Stage 4E-R4.** `NDEC-001` is amended in place (NDEC count still **7**) to freeze the action value as a **tagged, structurally-exclusive object**: `{"kind":"MOVE","value":"<move_set token>"}` and `{"kind":"BARRIER","value":[row,col]}`. The movement form reuses the FIXED `move_set` vocabulary verbatim and the barrier form carries the **exact placed cell** as the `[row,col]` array already locked by JDEC-006/JDEC-012 — no new coordinate convention was invented. A barrier-carrying sealed record is therefore now recomputable byte-identically, so **the byte-identity blocker is gone**. The shape is **PROJECT-CONTRACT**, not source-prescribed (Ch 5 p.53's `move: str` is EXAMPLE-ONLY), and is carried pre-match by the existing NDEC-001 echo exactly as NDEC-002 carries `state` — no new negotiation subsystem and no `action_codec` identifier.
- [x] **Stage 4E — RESUME 2: Acknowledgement Foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-RESUME2-CLOSE. **This closes one slice, not Stage 4E: eight of the ten peer-visible families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Delivered exactly one new type, `Acknowledgement(cursor: TurnCursor, h_commit: Sha256Digest)` — frozen/slotted, exact type identity (subclasses of both composed values refused), **no coercion** (a valid 64-hex `str` raises rather than being wrapped), built-in `ValueError`, **0** supporting error classes and **0** new dependencies. It carries no `by_role`, no `ack_of_step`, no `accepted`/`ok`, no `message_id`, no timestamp and no phase; it neither computes nor verifies `H_commit`. It shares its component types with `Commitment` and is deliberately **not** an alias, subclass or parent of it — class identity alone discriminates the two families, so no message-kind enum exists. `Commitment` and `TurnCursor` are byte-unchanged. **Process note, recorded rather than sanitized:** the first implementation attempt was interrupted and left an unclean worktree, so its preflight violated the stage's own STOP-on-drift rule and its RED was *reconstructed* by restoring production to HEAD around a test file that attempt had already written. Supervising review correctly refused that as TDD provenance. **Stage 4E-RESUME2-FIX1 replayed the whole slice from the exact clean committed parents** — clean baseline 721/716, only `tests/app/test_acknowledgement.py` created, production SHA-confirmed equal to HEAD at the moment of a genuine `ImportError` RED, production written only afterwards, 39 Ack tests GREEN, then the committed `test_peer_messages.py` observed failing **solely** because `Acknowledgement` was still listed as a blocked family, then the one approved regression-maintenance line, 73 GREEN, 112 targeted. The FIX1 replay is the authoritative provenance. See **D29**.
- [x] **Stage 4E-R4 — Shared Physical Action + Canonical Move Representation Reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R4-CLOSE. Documentation / architecture / contract only; **0 Python**. **This closes one reconciliation, not Stage 4E: eight families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Re-verified from the **authoritative PDF**, not from our derived docs: Ch 5 §5.3.1 (p.51) defines the sealed `Move` as *"the physical action; the chosen action (movement, **barrier placement**, etc.)"*, §5.3.2 has Reveal send *"the action (Move) and the verbal sentence"* with the nonce withheld, Ch 3 §3.4 (p.37) gives **one single action per turn** — move one orthogonal cell **or stay** — and lets the police place a barrier only *"in a turn in which he forgoes movement"*, within one step of himself, with a duty to declare **the exact location** truthfully, and Ch 5 p.50 confirms the sealed record is *richer than the four shown fields*. Two decisions frozen: (a) the **semantic home** — a new domain-owned `domain.actions` (see Reveal blocker 1); (b) the **canonical representation** — a tagged object under the unchanged sealed key `move` (see Reveal blocker 2). The sealed field set stays **exactly eight**; no ninth field, no `action`/`barrier_target`/`action_kind` top-level key. `domain.rules.Move` stays the five-token movement vocabulary only. **Reveal is reclassified `BLOCKED-BY-VALUE-REPRESENTATION` → `BLOCKED-BY-FUTURE-SEMANTIC-TYPE`**: its design is frozen and it now waits only on the shared action value existing in Python. No JDEC/NDEC/INV/Conflict entry was created. See **D30**.
- [x] **Stage 4E-R5 — Shared physical-action foundation + `LocalTurnService` migration (one atomic slice)** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R5-CLOSE. **This closes one slice, not Stage 4E: seven families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Tests-first from the exact clean committed parents (baseline 760/755, 100%): the **mandatory impact scan ran before any edit** and derived the scope rather than inheriting R4's desk prediction — it proved `app/__init__.py` needed **no** change, and it surfaced two constraints the desk audit had missed, namely that `tests/app/test_turn_boundaries.py` pins the `from <pkg>.app.turn_service import … MoveAction` path through a subprocess probe, and that the same file forbids the words `commitment`/`nonce`/… anywhere in `turn_service` source. RED was genuine (`ModuleNotFoundError: No module named '<pkg>.domain.actions'`) with `app/turn_service.py`, `domain/__init__.py` and `app/__init__.py` all SHA-confirmed equal to HEAD and `domain/actions.py` absent. Delivered `domain/actions.py` (**byte-identical across repos**, sha256 `c78776a3…`) holding `ActionKind`, `MoveAction(move)`, `BarrierAction(target)`, `PhysicalAction` and `InvalidPhysicalActionError(DomainError)` — frozen, slotted, exact composed types, **no coercion**, `kind` a derived property and never a stored field, and **no LIVE check**: a `BarrierAction` accepts an off-board `Position` structurally because bounds, role, adjacency, occupancy and quota are LIVE. `app.turn_service` now **consumes** those types: exactly one `class ActionKind`/`MoveAction`/`BarrierAction` exists per package, the app-level names are identity-equal re-exports (`turn_service.MoveAction is domain.actions.MoveAction`), and `LocalAction is PhysicalAction` rather than a second union. `Move.STAY` stays an ordinary movement, `domain.rules.Move` stays exactly N/S/E/W/STAY, `app/peer_messages.py` has **zero diff**, and no canonicalization helper was added. Malformed *construction* now raises `InvalidPhysicalActionError`; the LIVE `InvalidActionError`/`UnsupportedActionError`/`ActionsExhaustedError` keep their prior meanings. **800 / 795 tests, 100% coverage.** See **D31**.
- [x] **Reveal readiness** — **READY-TO-IMPLEMENT** as of Stage 4E-R5-CLOSE. Every prerequisite is now satisfied: R4 froze the shape (`cursor`, `action`, `hint`), `TurnCursor` exists, `PhysicalAction` now exists, `app.peer_messages` is already permitted to consume immutable `domain` value types, `hint` is structurally a `str` with `hint_max_words` staying LIVE locked-config validation, and no source-payload, canonical-representation or dependency-home blocker remains. The one open question from the R5 report — the `ValueError` vs `DomainError` asymmetry — is **resolved by layer ownership** rather than by changing either contract: constructing a malformed *domain value* is `InvalidPhysicalActionError(DomainError)`, while handing a future `Reveal` a component whose exact type is neither `MoveAction` nor `BarrierAction` is a *message-composition* failure and stays the built-in `ValueError`. Reveal will therefore **never construct, wrap or coerce** a `PhysicalAction`, and never catch and translate `InvalidPhysicalActionError`; because `PhysicalAction` is a union alias its structural check is exactly `type(action) in (MoveAction, BarrierAction)` — not `type(action) is PhysicalAction` — with subclass rejection.
- [x] **Stage 4E — RESUME 3: Reveal Foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-RESUME3-CLOSE. **This closes one slice, not Stage 4E: seven families remain BLOCKED and Stage 4E as a whole is NOT COMPLETE.** Tests-first from the exact clean committed parents (baseline 800/795, 100%), with the impact + LOC scan run **before** any edit. RED was genuine — `ImportError: cannot import name 'Reveal'` — with `app/peer_messages.py`, `app/__init__.py`, `domain/actions.py` **and** the historical `tests/app/test_peer_messages.py` all SHA-confirmed equal to HEAD. Delivered `Reveal(cursor: TurnCursor, action: PhysicalAction, hint: str)` — frozen, slotted, exact three-field order, **0** supporting error classes and **0** new dependencies. Because `PhysicalAction` is a *union alias*, the runtime rule is exact membership of `(MoveAction, BarrierAction)`, never `type(x) is PhysicalAction`; subclasses of both are refused and nothing is coerced — a bare `Move`, a `Position`, a `"N"` token and a canonical `{"kind": …}` mapping all raise. `hint` is structurally just a `str`: `""`, `"   "` and an 800-word string are **accepted**, because `hint_max_words` is locked config and stays LIVE. Absent: `move`, `nonce`, `state`, `intent`, `role`, `by_role`, `sealed_record`, `phase`, `h_commit`, `accepted`/`ok`, `message_id`, `timestamp` and the rest of the 21-name sweep — the internal member is `action`, and the sealed key `move` is **not** renamed. No canonicalization, no hashing, no live validation. **858 / 853 tests, 100% coverage.** See **D32**.
- [x] **Peer-message module capacity** — **COMPLIANT BUT AT CEILING.** `app/peer_messages.py` is exactly **150 / 150** LOC. Reveal did not fit at the parent's 132 LOC, so roughly 20 lines were reclaimed by **prose-only** compression of four docstrings; that this changed no behaviour was *proved*, not asserted, by stripping docstrings from both revisions and comparing ASTs — `TurnCursor`, `Commitment`, `Acknowledgement` and `_require_int` are **executable-AST identical** to the parent, with `Reveal` the only added name. Supervising review accepted this. **Consequence:** no further semantic family may be added to this module until organization is reconciled; the docstrings must not be compressed again, and no base-class/mixin/registry or module split may be improvised. That decision belongs to Stage 4E-R6.
- [x] **Stage 4E-R6 — Remaining turn-protocol readiness + peer-message module organization reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R6-CLOSE, together with its two supervising corrections **R6-FIX1** (the false "peers disagreeing on nonce case/length cannot recompute identical bytes" rationale removed; CSPRNG/secrecy separated from representation; the series-wide batch corrected to **per sub-game**; and the full `NonceValue`/`NonceRevealEntry`/`FinalNonceReveal` inventory frozen, since R6 had labelled the family READY while deferring its types) and **R6-FIX2** (the contradiction where NDEC-001 admitted an alternative nonce encoding that `NonceValue`'s `[0-9a-f]{32}` contract would have rejected — current **v1 supports exactly one profile**, both peers echo it before `CONFIG_LOCKED`, and a mismatch refuses counted play as a LIVE check). Documentation / architecture / contract only; **0 Python**. Re-derived from the **authoritative PDF**, not from our derived docs. **(1) Module organization — decided by measurement.** `app/peer_messages.py` is at 150/150 with `TurnCursor` 27, `Commitment` 25, `Acknowledgement` 28, `Reveal` 31, the private int guard 8, a 16-line docstring and 4 imports. Simply relocating all four into one new module lands at **~145** and hits the same wall one family later, so that option was **rejected on arithmetic**. Frozen instead: `app.turn_cursor` (~54) + `app.peer_turn_messages` (~107, room for one more turn family) + `app.peer_final_messages` (new) + **`app.peer_messages` as a pure façade** (~36) re-exporting **the same class objects**. `TurnCursor` is extracted because *both* message modules need it and neither should import the other — ownership evidence, not a capacity workaround. No `PeerMessage` base class, no `MessageKind` enum, no registry, factory, generic payload dict or reflection. The public paths `from <pkg>.app.peer_messages import …` and `from <pkg>.app import …` must keep working with identity-equal classes, because three test modules import that path directly and one imports the module object to assert blocked families are absent. **(2) The three families, re-audited together.** Figure 6 (p.52) draws exactly **four** arrows — Commit, Acknowledge, Reveal, Final Reveal: all Nonces — captioned as the four stages Commit→Acknowledge→Reveal→**Audit**. There is **no move-validation arrow and no audit-verdict arrow**, which reframes two of the three blockers. See **D33**.
- [x] **#8 Move validation — blocker reclassified** — `BLOCKED-BY-VALUE-REPRESENTATION` → **`BLOCKED-BY-PAYLOAD-SHAPE`**, with evidence in both directions. **Resolved:** the value-representation half is gone — R4 froze the canonical action encoding, R5 built `PhysicalAction`, and `Reveal` already carries the action, so nothing about *representing a move* still blocks this family. **Still blocked:** whether validation is a **transmitted peer message at all**. App E #14 genuinely requires the opponent to reject an illegal move (*"no diagonal moves — sanction: move rejected by the opponent → loss"*) and Ch 6.5 requires the local algorithm to reject any illegal move its own model proposes, so rejection is real; but no source fixes whether it travels as a payload or surfaces through the sanction path, and the only "accept/reject" text in the book is the minimal FastMCP server's `{"accepted": is_valid, …}` (p.28) — **REFERENCE-EXAMPLE** about signature checking, the same snippet Stage 4E-R3 refused to promote for the acknowledgement. This is the same category as #14, whose flow NDEC-006 explicitly declines to freeze.
- [x] **#11 Final nonce reveal — READY-TO-IMPLEMENT** — `BLOCKED-BY-VALUE-REPRESENTATION` → **`READY-TO-IMPLEMENT`**, with the full type contract frozen at Stage 4E-R6-FIX1 (R6 had labelled it READY while leaving the type inventory to the implementing slice — corrected here). **Source.** Ch 5 §5.4 (p.55): each agent submits his full log *"including the nonce reveals of **all his steps**"*, and Figure 6 draws *"Final Reveal: all Nonces"* in **both** directions — so it is a **batched** message per side over that side's **own** steps, never per turn and never carrying the opponent's material. **Batch boundary — SUB-GAME-SCOPED, PROJECT-CONTRACT.** The source term is genuinely ambiguous: Appendix F names the counted unit *משחקון* (sub-game, `num_games` = 6 FIXED) while §5.4 says *בתום המשחק* ("at the end of the game"). The project boundary is settled by its own **already-locked artifact**: the log is `log_<game_id>_g<NN>.json`, **one file per sub-game** with a top-level `sub_game` and its own `final_reveal` array and `audit.result` (INV-02, JDEC-004, JDEC-007). Per-sub-game scoring and outcomes need the audit to conclude at `SUBGAME_COMPLETE`, and §5.3–5.4 plus Figure 6 describe one game's commit→ack→reveal→audit cycle. So: **one `FinalNonceReveal` per peer per sub-game.** Recorded as a project interpretation, not as source law. **Representation (rationale corrected).** The nonce default stays `[0-9a-f]{32}` but **explicitly not because recomputation requires it** — a receiver rebuilds the record from the *exact revealed string*, so any stable form recomputes identically. It is kept for parser strictness, NFC-stability under NDEC-003, and a fixed 16-byte width; it is **PROJECT-CONTRACT**, with `secrets.token_hex(16)` remaining REFERENCE-EXAMPLE. *(Corrected at Stage 4E-R6-FIX2: current **v1 supports exactly one** nonce representation, so NEGOTIATED-PRE-MATCH here means both peers **echo that same required profile** before `CONFIG_LOCKED` — it does **not** mean v1 may select among codecs. An echoed profile that differs is a **profile mismatch that refuses counted play**, which is a LIVE negotiation check, not an `InvalidNonceError`, not tampering and not a score sanction. FIX1 had implied an alternative could already be accepted, which `NonceValue`'s `[0-9a-f]{32}` contract would have rejected — the two could not both be true.)* **NDEC-001 amended in place; NDEC count still 7.** **Generation vs representation.** CSPRNG production (CRYPTO-010) and secrecy-until-final-reveal are **producer/runtime** duties — a value built from a *received* string cannot prove either — so neither is a structural constructor check. **Frozen future types.** `NonceValue(value: str)` in **`app.protocol_values`** (the Stage-4F home for reusable protocol semantic values), frozen/slotted, exact `str`, no coercion or normalisation, malformed → `InvalidNonceError(ValueError)` mirroring `InvalidDigestError`; then `NonceRevealEntry(cursor: TurnCursor, nonce: NonceValue)` and `FinalNonceReveal(entries: tuple[NonceRevealEntry, ...])` in `app.peer_final_messages`, both frozen/slotted with exact-type composition and built-in `ValueError` on wrong composition. `entries` is an **exact `tuple`** — no list, dict, iterable or generator coercion — and every member an exact `NonceRevealEntry`. **`entries == ()` is structurally accepted**: completeness against the steps actually played, uniqueness, ordering and cross-checking every entry's `sub_game` are **LIVE**, because a value must never query game state to be constructed; any canonical/wire ordering belongs to the future mapper. Association is `TurnCursor` alone — no `role`/`by_role` (a side reveals only its own nonces and the receiver knows the direction, exactly the R3 result), no repeated `h_commit` (the receiver already holds it from event 5), no repeated `action`/`hint` (already revealed at event 7), no `game_id`, `phase` or verdict.
- [x] **#12 Final audit — blocker reclassified** — `BLOCKED-BY-ASSOCIATION-SHAPE` → **`BLOCKED-BY-PAYLOAD-SHAPE`**. The association question is **answered**: Ch 5 §5.4 has each side rebuild the opponent's record from *State, Move, Intent, Nonce* and compare against the declared commitment, and `LOG_CONTRACT.md` §E already freezes what the audit *records* — `entries[].verified` (bool), `audit.result` (`Verified OK`/`TAMPERED`) and the optional `audit.tampered_step`. What is **not** frozen is whether any verdict is **transmitted**: Figure 6 shows no audit arrow, Ch 2.2.1 makes independent verification the entire reason there is no central server, and Ch 7 §7.5 + Figure 10 place the recomputation in the **Replay Viewer** over the persisted log. A future stage may well conclude this family is **not transmitted at all**; that conclusion is not forced here.
- [x] **Stage 4E-R7 — peer-message module reorganization (pure migration, no new family)** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R7-CLOSE. Tests-first from the exact clean committed parents (baseline 858/853, 100%), with the impact **and metadata** audit run before any edit: it found **no** contract, test or runtime anywhere depending on `__module__`, `__qualname__`, `repr`, pickle or source location for the four values, so the defining-module metadata was allowed to follow the new ownership rather than being forged. RED was genuine (`ImportError: cannot import name 'peer_final_messages'`) with all five production files SHA-confirmed equal to HEAD and the three target modules absent. Final layout: `app/turn_cursor.py` (**51 LOC** — `TurnCursor` + its private `_require_int`), `app/peer_turn_messages.py` (**108** — `Commitment`, `Acknowledgement`, `Reveal`), `app/peer_final_messages.py` (**10** — boundary only, **zero** semantic classes), and `app/peer_messages.py` reduced from **150 → 20 LOC** as a pure façade with identity-equal `X as X` re-exports and an `__all__` of exactly the four implemented names. Proven by AST rather than grep: **one definition per class** in its owning module, **zero** class definitions in the façade and in the finalization boundary, and **executable-AST equivalence** for all five moved definitions against the committed parent. `peer_messages.X is <defining module>.X` and `app.X is <defining module>.X` hold for all four. `app/__init__.py`, `protocol_values.py`, `turn_service.py`, `domain/actions.py` and `domain/rules.py` are **byte-unchanged**, and the three committed behaviour test modules (73 + 39 + 58) passed **with zero edits** — which is the migration's real acceptance test. Import graph acyclic: `turn_cursor` (no sibling imports) ← `peer_turn_messages` ← façade. **881 / 876 tests, 100% coverage.** The capacity blocker is genuinely removed, not relocated: the façade has ~130 lines of headroom and `peer_turn_messages` ~42. **Process defect recorded, not smoothed over:** the first migration helper sliced the class blocks by *hardcoded line numbers*; an assertion caught an off-by-one **after** `peer_messages.py` had already been overwritten. The partial work was discarded — `peer_messages.py` restored from HEAD and its original hash `23cf3b03…` re-verified, the partial modules deleted — and the migration redone with **AST-derived source ranges**, each extracted block re-parsed and proved AST-identical to the original definition *before* being written. The line-slicing attempt was never an accepted approach. See **D34**.
- [x] **Stage 4E-R8 — Final nonce reveal foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R8-CLOSE. Tests-first from the exact clean committed parents (baseline 881/876, 100%), with the impact, LOC and public-surface audits run before any edit. Both dedicated test files were written first and the four production files were still byte-identical to HEAD when RED was observed as a genuine `ImportError` for the absent values. Implemented together, and nothing else: `InvalidNonceError(ValueError)` + `NonceValue` in `app.protocol_values`, and `NonceRevealEntry(cursor, nonce)` + `FinalNonceReveal(entries: tuple[NonceRevealEntry, ...])` in `app.peer_final_messages`, re-exported through the stable `peer_messages` façade (the entry stays off it) and the app surface as identity-equal objects. **`NonceValue` validates representation only** — exactly `[0-9a-f]{32}`, no coercion and no normalisation, uppercase/whitespace/`0x`/separators/Unicode lookalikes and `str` subclasses all refused — so `"0" * 32` is deliberately valid: entropy, freshness, CSPRNG provenance and secrecy are producer and protocol duties a constructor handed a *received* string can never verify. The batch is structural only: an empty tuple, duplicate cursors, out-of-order steps and mixed sub-games all construct, because completeness, uniqueness, ordering and same-sub-game agreement are LIVE. No `role`/`by_role`, no repeated `h_commit`, action or hint; no `sub_game` field (association is each entry's cursor). **No nonce generator, hashing, canonicalization, commitment verification or `FinalAudit`.** The two historical tests were each run unchanged first and maintained only after their expected stale failures. 983/978 tests, 100% coverage. **`FinalNonceReveal` is the fourth implemented family: 10 families = 4 implemented / 0 ready / 6 blocked, and Stage 4E as a whole is NOT COMPLETE.** See **D35**.
- [x] **Stage 4E-R9 — Canonical commitment codec + recompute foundation** — **STOPPED `BLOCKED-BEFORE-CODE`, and the stop was correct.** The stage opened with a read-only dependency audit of all eight sealed members before any test file existed. Five were exact and implementation-ready — `move`, `hint`, `step`, `sub_game`, `nonce` — and three were not: **`role`** had four spellings across the source and the locked artifacts (`Cop`/`Thief` in Figure 6, `police`/`thief` in `LOG_CONTRACT.md`, `POLICE`/`THIEF` in the repository constants, `cop`/`thief` in the PRD-01 score keys) with no contract naming the sealed one or the mapping; **`state`** had a frozen JSON shape but no Python semantic value, and `LOG_CONTRACT.md` still contradicted its own lock by marking it REVIEW-REQUIRED twice; **`intent`** had a source-explicit `truth`/`lie` vocabulary but no Python representation. **Nothing was written**: no partial codec, no action-only mapper, no canonical-JSON utility, no `protocol/` package, no invented semantic type — 0 code, 0 tests, 0 docs diff. A canonical-JSON helper was technically writable, but its mandatory known-answer digest vectors need all eight members, so it would have shipped with tests proving only `codec_output == codec_output`. This entry is preserved as a **blocked** result; it is not rewritten as though the stage had been ready. Reconciled by **4E-R9-R1**; the codec itself resumes after **4E-R9-R2**.
- [x] **Stage 4E-R9-R1 — Sealed commitment semantic prerequisites reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R9-R1-CLOSE. Documentation / contract / architecture only; **0 Python, 0 tests**. Eliminates exactly the three R9 blockers and two directly-related ambiguities, taking the sealed record from **5/8 to 8/8 contract-ready** (contract-ready, *not* implemented). **`role`:** the canonical sealed vocabulary is exactly `"police"` / `"thief"` as **PROJECT-CONTRACT** — the source only ever labels the sides *Cop* / *Thief* in Figure 6, which is explanatory terminology and not a byte string — with the repository constants mapped **explicitly** (`"POLICE"` → `"police"`, `"THIEF"` → `"thief"`), no `lower()`, case-folding, alias or synonym, and an unrecognised runtime role refused rather than guessed. Three vocabularies are now deliberately separate and recorded as such: source `Cop`/`Thief`, sealed `police`/`thief`, and the PRD-01 score keys `cop`/`thief` — **the `cop` score key is not the sealed role for police**, and PRD-01 was not re-spelled for cosmetic uniformity. **`intent`:** frozen to the **SOURCE-REQUIRED** `truth` / `lie`, both words printed in Ch 5 p.51; no bool, no aliases, never inferred from `hint`, which remains a separate sealed member that must always travel with it. **`state`:** the JDEC-012 / NDEC-002 / PRD06-FR-068 own-known shape, with `barriers` **sorted by `(row, col)` and duplicate-free in the semantic value itself** so no mapper ever sorts, and the builder required to refuse `state.step != cursor.step` or `state.role != role` **before hashing** — a composition error, never TAMPERED. Full future contracts frozen for **`ActorRole`**, **`Intent`** and **`SealedState`** in a legal dependency-safe home, `app.sealed_record_values`, chosen on **boundary** grounds (`app.protocol_values` is stdlib-pure and cannot hold a `domain` `Position`; `domain` may not import `app`; defining them in `protocol.commitment` would force an outward edge from `app.turn_service`). Also closed: the stale `LOG_CONTRACT.md` REVIEW-REQUIRED text, the `[row,col]` citation (**JDEC-012**, not JDEC-006), **`ensure_ascii=False`** propagated into the central canonical contract instead of living only in PRD06-FR-005, and the three-layer split that keeps `E-HASH-MISMATCH` / TAMPERED **above** the pure comparison primitive — a digest that differs is a `bool`, not an exception. **No register ID was created**; JDEC 15 / NDEC 7 / INV 15 / C-01…C-10 unchanged. See **D36**.
- [x] **Stage 4E-R9-R2 — Sealed record semantic values foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R9-R2-CLOSE. Tests-first from the exact clean committed parents (baseline 983/978, 100%), with the impact/LOC, module-boundary and public-surface audits all run before any production edit. Three test files were written first; the `SealedState` design first measured **169 LOC**, so **while still tests-first** it was split by responsibility into `test_sealed_state.py` and `test_sealed_state_barriers.py` — a split, never a weakened assertion. RED was a genuine `ModuleNotFoundError: No module named '<pkg>.app.sealed_record_values'` observed while `app/__init__.py`, `app/protocol_values.py` and `domain/board.py` were all SHA-proven `== HEAD` and the module was absent. Implemented together in the R9-R1-frozen home `app.sealed_record_values`: **`ActorRole(StrEnum)`** exactly `POLICE = "police"` / `THIEF = "thief"`, **`Intent(StrEnum)`** exactly `TRUTH = "truth"` / `LIE = "lie"`, and **`SealedState`** frozen and slotted over `config_sha256: Sha256Digest`, `self_pos: Position`, `barriers: tuple[Position, ...]`, `step: int`, `role: ActorRole`. Two members each, no aliases, no `_missing_`, no normalisation, no runtime-mapping helper and no hint inference: the three vocabularies stay separate, so `"POLICE"` (runtime), `"Cop"` (the book's display word) and `"cop"` (a PRD-01 score key) are all **refused** as roles. `SealedState` checks exact composed types with no coercion, requires `step >= 1` with `bool` rejected by exact type, and requires barriers **already sorted by `(row, col)` and duplicate-free** — proved non-transforming by `state.barriers is` the supplied tuple. Board bounds, quotas, legality and the builder's `state.step == cursor.step` / `state.role == role` invariants all stay with their owners; **no opponent truth is representable**; and nothing serializes, canonicalizes or hashes. Every committed historical test passed **unchanged** — **0 test modifications**. 1116/1111 tests, 100% coverage. The eight sealed members remain contract-ready and the original R9 codec scope is now **implementation-unblocked but not started**. Peer families unchanged at **4 implemented / 0 ready / 6 blocked**; these three are internal prerequisites, not peer-message families. See **D37**.
- [x] **Stage 4E-R9-RESUME — Canonical commitment codec + recompute foundation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R9-RESUME-CLOSE. Resumes the scope the original **Stage 4E-R9 correctly stopped** on (`BLOCKED-BEFORE-CODE`, 5/8 sealed members ready), after **4E-R9-R1** reconciled `role`/`intent`/`state`, `ensure_ascii` and mismatch ownership and **4E-R9-R2** implemented `ActorRole`, `Intent` and `SealedState` — 8/8 contract-ready with every prerequisite in code before a byte was written. Tests-first from clean parents (baseline 1116/1111, 100%). **Before any production existed**, the three supervisor known-answer vectors were independently reconstructed by a one-off verifier importing only `json`, `hashlib` and `unicodedata`, importing no project code and writing no repository file; all three canonical strings and SHA-256 values matched, so the oracle was external and no expected value was ever adjusted afterwards. Five test files were written first; two exceeded 150 LOC after formatting and were **split by responsibility while still tests-first** into seven, with no assertion weakened. RED was a genuine `ModuleNotFoundError: No module named '<pkg>.protocol'` with the package absent and all thirteen committed semantic/application/domain files SHA-proven `== HEAD`. Implemented in the architecture-frozen homes: **`protocol.canonical`** (NFC `canonical_text`; `canonical_json_bytes` over a deliberately narrow `str`/`int`/`list`/`dict` domain that refuses `None`/`bool`/`float`/`bytes`/tuples/sets/generators/subclasses and non-NFC strings, serialized `sort_keys=True`, `separators=(",",":")`, `ensure_ascii=False`, UTF-8, no trailing newline) and **`protocol.commitment`** (explicit `PhysicalAction` and `SealedState` mappings, the exact eight-field builder, unkeyed SHA-256 into the existing `Sha256Digest`, and a plain `bool` comparison). The builder **refuses `state.step != cursor.step` and `state.role != role` before hashing** — a local composition defect, never TAMPERED. **One** `compute_commitment` primitive serves both the initial commit and the later recomputation from the revealed nonce, so no two paths can drift. All three vectors match exactly, including NFC (`"e\u0301"` and `"é"` give identical bytes and digest) and literal Hebrew UTF-8 with no `\u05` escapes, **identically in both repositories**. No `FinalAudit`, TAMPERED, technical loss, peer-message construction, game legality, wire mapper, FastMCP, transport or nonce generation. Every committed test passed **unchanged** — 0 modifications. 1258/1253 tests, 100% coverage. `MODULE_BOUNDARIES.md`'s `protocol.commitment` dependency list was reconciled to include `app.turn_cursor`, which the frozen builder input always required. Peer families unchanged at **4 implemented / 0 ready / 6 blocked**. See **D38**.
- [x] **Stage 4E-R10 — Final audit + move validation readiness reconciliation** — **STOPPED `INVENTORY-CONTRADICTION`, BEFORE ANY DOCUMENTATION CHANGE; 0 FILES CHANGED.** The stage was asked to freeze two payloads and could freeze neither. Its two findings both stand: (a) the source **does not specify a transmitted `FinalAudit` verdict payload** — Ch 5 §5.4 exchanges **logs** and makes each side's recomputation local, Ch 7 §7.5 + Figure 10 put `Verified OK`/`TAMPERED` in the **Replay Viewer** over a persisted file, Figure 6 (p.52) draws no audit-verdict arrow, and App E / PRD06-FR-100 make the mutual audit a precondition to agreeing the shared result; and (b) **`MoveValidation` remains `BLOCKED-BY-PAYLOAD-SHAPE`** — opponent rejection of an illegal move is source-entailed (App E #14) but no vocabulary, association, reason or cadence is source-fixed. Rather than change the ten-family inventory unilaterally, the stage stopped and asked for review. **This entry is preserved as a stop, not rewritten as a PASS.** Resolved by **4E-R10-R1**.
- [x] **Stage 4E-R10-R1 — Final-audit inventory + audit-material exchange reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R10-R1-CLOSE. Documentation / architecture only; **0 Python, 0 tests, 0 runtime**. The supervised resolution of R10's stop, and it turns on separating three things that had been conflated into one. **(1) `FinalNonceReveal`** is an implemented peer family owning the batched nonce disclosure — unchanged and *not* expanded. **(2) The audit material / full-log disclosure** is a **SOURCE-REQUIRED** end-of-game obligation: the sealed record has eight members, Reveal discloses the action and hint and `FinalNonceReveal` the nonces, so the peer still lacks the disclosed `state` and `intent` it needs to rebuild the record — Ch 5 §5.4 closes that with each side submitting its **full log**. That obligation is **not** an `app.peer_messages` family; its interchange shape is newly recorded as **`AUDIT-EXCHANGE-PAYLOAD: BLOCKED-BY-INTEROPERABILITY-SHAPE`**, an artifact/transport blocker, with raw bytes / whole JSON log / sealed-record tuple / partial material / artifact URL / filesystem path all deliberately left undecided and `submit_audit` left a reference convention. **(3) `FinalAuditVerdict`** stays exactly `"Verified OK"` / `"TAMPERED"` as the **local** audit/log/replay vocabulary. Ruling: **there is no peer-visible `FinalAudit` verdict family and none is planned** — stated precisely as *the source does not **require** one and the project declines to invent it*, never as a prohibition. Surviving untouched: `ProtocolPhase.FINAL_AUDIT` (a phase is not a family), timeline **event 12** and its **15**-event count, and the TAMPERED sanction. Timeline event 12 now distinguishes **transmitted** (audit material) / **local** (recomputation, comparison, verdict) / **persisted** (audit evidence). Wording corrected on the downstream link: a **completed mutual audit is a precondition to** `ResultAgreement`, which is *not* the transport of a verdict and stays `BLOCKED-BY-PAYLOAD-SHAPE`. The derived peer-visible inventory is corrected **10 → 9**, matrix **4 implemented / 0 ready / 5 blocked**, recorded as official **Conflict-Register C-11** (explicitly distinct from the unrelated PRD-review label of the same name). `MoveValidation` deliberately not solved. 1258/1253 tests, 100% coverage, all three supervisor vectors unchanged. See **D39**.
- [x] **Stage 4E-R10-R2 — MoveValidation existence + payload reconciliation** — **STOPPED `BLOCKED-BY-EXISTENCE-EVIDENCE` AT THE EXISTENCE GATE; 0 FILES CHANGED.** The stage was told to answer existence *before* payload, and it could not answer existence. App E #14 states the **sanction** — *"no diagonal moves; sanction: **rejection of the move by the opponent** and loss"* — so a peer-facing rejection is source-entailed, but no passage says whether it travels as **(a)** a standalone message, **(b)** the response of the same call that received the move, or **(c)** a local refusal surfacing through the sanction path. Figure 6 draws no move-validation arrow; Ch 6 §6.4-6.5 reserve legality to the **local algorithm**, whose "reject" is of *its own model's* proposal; and the only response-shaped artifact — the FastMCP `receive_move` return `{"accepted": is_valid, …}` — computes `is_valid = verify_signature(...)`, i.e. **signature authentication, not game legality**. Deliberately **not** declared a second inventory contradiction: unlike `FinalAudit`, the source positively places a rejection act *on the opponent*, so there was no positive evidence the peer does nothing. **This entry is preserved as a stop, not rewritten as a PASS.** Resolved by **4E-R10-R3**.
- [x] **Stage 4E-R10-R3 — Move-rejection inventory + transport-response reconciliation** — **CLOSED / COMMITTED / CI-GREEN at Stage 4E-R10-R3-CLOSE — but PARTIAL: the inventory correction is complete and the transport response shape remains blocked.** Documentation / architecture only; **0 Python, 0 tests, 0 runtime**. Supervising review made the **PROJECT-CONTRACT** architecture decision the source leaves open: the peer-facing rejection is **not** a standalone `app.peer_messages` family but belongs at the **transport / port response boundary** of the operation that receives the turn — and the source is **not** claimed to forbid a distinct message, only never to require one. `app.peer_messages` must never contain `MoveValidation`, `ValidationAck`, `ValidationResultMessage`, `MoveAcceptedMessage` or `MoveRejectedMessage`. The obligation relocates to contracts that already exist rather than disappearing: **`GameRulesPort`** already decides legality and *"never raises for legality — returns a verdict"*, and **`E-PROTO-ILLEGAL-MOVE`** already owns the opponent-illegal-move outcome with *"received message + validator verdict"* as evidence — **no new port, semantic concept or error ID**. Newly frozen and load-bearing: **four acceptances that must never collapse into one flag** — delivery/parsing, authentication, protocol phase/cursor/order, and **game legality** — of which App E #14 concerns only the last while the FastMCP example's `accepted` is the second. Transport never re-derives legality, applies a move, mutates `LocalTruth`, computes scoring or chooses technical loss. Timeline **events 8 and 9 survive**, reconciled rather than deleted, with the count still **15**: a timeline event is not automatically a family and a log validation record is not automatically a wire message. Inventory corrected **9 → 8**, matrix **4 implemented / 0 ready / 4 blocked**, recorded as official **Conflict-Register C-12** (distinct from the unrelated PRD-review label of the same name, *result-hash self-reference*, which was not edited). **Intentionally unresolved:** `MOVE-REJECTION-TRANSPORT-SHAPE: BLOCKED-BY-TRANSPORT-SHAPE` — `API_BOUNDARIES.md` and PRD02-FR-035 defer concrete operation signatures to Stage 2B-2C and both peer ports are **async**, so a bare game-legality `bool` remains a documented **candidate, not frozen**; freezing it now would decide the four-way acceptance separation by accident. 1258/1253 tests, 100% coverage, all three supervisor vectors unchanged. See **D40**.
- [x] **Stage 4E-R11 — Peer operation + transport contract reconciliation** — **CLOSED / COMMITTED / CI-GREEN at Stage 4E-R11-CLOSE — but PARTIAL: the operation contract is frozen and one of the two integration blockers is resolved, the other narrowed.** Documentation / architecture only; **0 Python, 0 tests, 0 runtime**. Two blockers were waiting on the same missing thing, so the stage froze the peer **operation** layer as `API_BOUNDARIES.md` **O1-O7**. **O1** — `async` is an I/O property, **not** a message shape: peer operations are logically **request → await exactly one operation-specific result**, or a typed owning-layer failure, and state-changing calls are never fire-and-forget. This was reconciliation rather than invention: `CONCURRENCY_MODEL.md` already said peer calls are *"per request, async, bounded… never fire-and-forget for state-changing calls"*. An operation result is **never** a peer semantic family. **O2** — a success result is distinct from transport, parse/schema, authentication and protocol phase/order/cursor failures, each keeping its own error identity (`E-TRANSPORT`, `E-PROTO-MALFORMED`, `E-AUTH-FAILURE`, `E-PROTO-STALE`); **no universal `accepted` flag**, no lower-layer failure encoded as a semantic `False`. **O3** — the PRD02-FR-033 logical operations (a)-(h) are routed to families, with the reference names `negotiate` / `receive_turn` / `submit_audit` / `receive_control` kept only as **PROJECT-CONTRACT / REFERENCE-COMPATIBILITY aliases, not book-mandated**; internal code never depends on an alias string, and **no fifth validation operation** exists. **O4** — one stable group-level public ingress per team, with `SeriesLauncher` routing internally to the role-specific backend as **operational routing only**; no separate public Police/Thief endpoints. **O5** — **`MOVE-REJECTION-TRANSPORT-SHAPE` is RESOLVED-PROJECT and READY-TO-IMPLEMENT**: the operation carrying a Reveal returns exactly one **`bool`** whose only meaning is game legality *after* authentication and protocol validation — never delivery, parse, signature, sender, phase, cursor, commitment or reveal-hash validity, which never reach it. Correlation is the awaited invocation, so **no `TurnCursor` echo** and no duplication of action, hint, nonce, digest or state; **no free-text reason crosses the boundary**; legality stays with `domain.rules`/`LocalTurnService` via `GameRulesPort`. **O6** — audit submission is logical operation (f), alias `submit_audit`, **one submission per completed sub-game** following the existing `log_<game_id>_g<NN>.json` artifact; the transport submits the finalized log representation and **no second audit schema** (`AuditEntry`/`AuditBundle`/`AuditEvidenceMessage`) is created; success is ordinary operation completion, so **no audit semantic response, no verdict, no digest and no audit ACK**. **O7** — an operation contract is not a FastMCP binding: no decorator, signature or JSON schema, with PRD02-FR-035 still governing concrete signatures. **Intentionally unresolved:** `AUDIT-EXCHANGE-PAYLOAD` is narrowed from open-ended to **`BLOCKED-BY-LOG-ARTIFACT-SHAPE`** — the natural payload is the finalized per-sub-game log document, but it is classified **LOCAL-ONLY** (D4/JDEC-007) and `LOG_CONTRACT.md` still carries a REVIEW-REQUIRED item on ack/reveal nesting, so promoting it to an interoperability payload is a reviewed decision. Inventory unchanged at **8**, matrix **4/0/4**, registers unchanged, **no C-13**. 1258/1253 tests, 100% coverage, vectors A/B/C unchanged. See **D41**.
- [x] **Stage 4E-R11-R1 — Log artifact interoperability + audit exchange reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R11-R1-CLOSE. Documentation / contract only; **0 Python, 0 tests, 0 runtime**. Resolves the last integration blocker. **JDEC-007 is amended in place, not replaced**: the Stage-1D shorthand *"persistent log = LOCAL-ONLY"* was over-broad — read literally it forbids the disclosure Ch 5 §5.4 requires — so it is narrowed to keep **internal logger mechanics, artifact metadata and locally-derived verification annotations LOCAL** while the finalized per-sub-game log's **audit-disclosure core** is **SHARED / INTEROPERABLE at final audit**. Key spelling and nesting stay PROJECT-CONTRACT; **JDEC count stays 15, no JDEC-016**. **One schema, two lifecycle states** (documentation labels, not types): *DISCLOSURE-READY* carries **all and only** the SHARED-AUDIT-INPUT fields and is what `submit_audit` conveys; *AUDITED-LOCAL* is the receiver's own copy after recomputation. **Sealed-record clarification**: `FIELD_MATRIX`'s Required per-turn sealed-record row is authoritative — the *semantic members* are persisted, while §B's *"distinct object"* refers to the **canonical byte serialization**, which is never stored. This mattered because a receiver cannot derive `state.self_pos` under partial observation, so without it independent verification would be impossible; resolved with **no tenth field and no FIELD_MATRIX edit**. **Completeness proved**: `state`, `move`, `intent`, `hint`, `step`, `role`, `sub_game` from the per-turn sealed record, `nonce` from `audit.final_reveal[]`, `H_commit` from `entries[].commit` — all under the frozen Stage 4E-R9 canonical mapping, no second representation. **Ack/reveal nesting closed** in favour of the separate-event `entries[]` form JDEC-007 had already chosen, with `(sub_game, step, phase)` association and chronological replay order; the REVIEW-REQUIRED count for that question is **0**. **`submit_audit` carries the exact JSON-native audit-disclosure core** — never a filesystem path, URL, base64, pickle, Python repr or `Path`. Semantic equality is required; **whole-log byte identity is not**, and **no `log_sha256`/`audit_sha256`/`artifact_sha256`** was added. Verdicts stay local: `entries[].verified`, `audit.result`, `audit.tampered_step` and `FinalAuditVerdict` are never transmitted or trusted. **CLOSE-time payload-core guard**: the guard found and corrected a real ambiguity — `by_role` had read *"may appear"* and the lifecycle text excluded only local-derived annotations — so the boundary is now deterministic, with **both** LOCAL-DERIVED-AUDIT and LOCAL-ARTIFACT-METADATA (`by_role`, `schema_version`) explicitly **outside** the payload and no version field added or implied. The over-broad *"no semantic fact appears twice"* claim was also narrowed to *"no field is duplicated within a single event"*, preserving the deliberate distinction between historical **Reveal evidence** and **sealed-record audit input** whose values are expected to agree. **Both integration blockers are now RESOLVED-PROJECT and `INTEROPERABILITY_BLOCKERS` reads Blocking items: None.** Inventory unchanged at **8**, matrix **4/0/4**, registers unchanged, no C-13. 1258/1253 tests, 100% coverage, vectors A/B/C unchanged. See **D42**.
- [x] **Stage 4E-R12 — Step-0 + config negotiation + config lock readiness reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R12-CLOSE across six stages (R12 · R12-FIX · R12-R1 · R12-R2 · R12-R3 · CLOSE), **0 Python and 0 tests**, **20 paths per repo**. **Step-0, Config negotiation and Config lock are all READY-TO-IMPLEMENT**; families **8 = 4 implemented / 3 ready / 1 blocked**, with `ResultAgreement` the sole blocked family. Resolved the bootstrap auth-profile paradox, froze `AuthProfile`/`KeyId`/`AuthProof` and their encodings, defined the previously undefined `step0_core` (**19 members**), derived `NegotiatedConfig` (35 core members; FIXED 15 / MINIMUM 9 / NEGOTIABLE 9), and made the config `AuthProof` cover a `ConfigLockContext` so it binds the sub-game and the frozen profile set. Removed `token_usage_locked` as a misplaced runtime datum (App E #54 / Ch 9 §9.3.3 put actual consumption in the **result**), recomputing **FIELD_MATRIX to 74 = 15/39/9/11**. Corrected an own overclaim: `result_sha256` protects the **reported** totals only, so **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`** remains a mandatory unresolved runtime/security obligation. Closed the token-cap chronology with MODEL A — **PRE-STEP0-AGREED / SERIES-WIDE / IMMUTABLE-AFTER-STEP0**, source status still NEGOTIABLE. Integration blockers remain **None**; no new requirement, register ID or peer family.
- [x] **Stage 4E-R13 — ResultAgreement payload + result artifact reconciliation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R13-CLOSE across four stages (R13 · R13-R1 · R13-R2 · CLOSE), **0 Python and 0 tests**, **14 paths per repo**. **ResultAgreement is READY-TO-IMPLEMENT and every peer family is now implemented or ready — 8 = 4 implemented / 4 ready / 0 blocked.** R13 froze the message shape but stopped PARTIAL on a deeper defect: the approval core held **per-team** values no contract transmitted, so honest peers could never match digests. R13-R1 made the three members **participant-scoped `{group_a, group_b}`** objects, derived `total_tokens` from the six sub-game values, and added `ResultContribution` plus the `Sha256Digest` operation response. R13-R2 closed the timestamp hole R13-R1 opened — `timestamp` now travels in the request as **`UtcTimestamp`**, with a **deterministic two-request order** and the proposer identified by **byte-wise lower `group_id` value, not the `group_a` slot**. `RESULT-APPROVAL-CORE-JOINT-DERIVABILITY: RESOLVED-PROJECT`. FIELD_MATRIX stays **74 = 15/39/9/11** with the repaired **result (11)** prose; integration blockers remain **None**; no new family, requirement or register ID.
- [x] **Stage 4E-R14 — Pregame peer semantic types implementation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R14-CLOSE across five stages (R14 · R14-R1 · R14-R1-FIX · R14-R1-FIX2 · R14-R2), **35 paths per repo**. R14 itself stopped **BLOCKED-BEFORE-CODE with 0 files**: no `Declaration` type existed and four `InteropProfileSet` vocabularies were unfrozen. R14-R1 froze the Declaration lifecycle and every profile vocabulary; R14-R1-FIX repaired a `ram_gb` `Decimal` regression, the profile count, the shared-primitive module ownership and a stale `Declaration` module line, then stopped on the `vram_gb` type; R14-R1-FIX2 froze `vram_gb = int | None`. **R14-R2 implemented the pregame semantic VALUE layer** — 9 new modules, façade re-exports, **313 new tests per repo**, **100.00% statement and branch coverage**, every production file **≤150 LOC**, production byte-identical across repos. **No FastMCP, transport, network, key loading, HMAC/Ed25519 computation, `ResultAgreement`, Gmail or token-accounting crypto was added**, so Step-0, config negotiation and config lock remain **READY**, not operationally implemented. Matrix stays **8 = 4/4/0**; FIELD_MATRIX **74 = 15/39/9/11**.
- [x] **Stage 4E-R15 — ResultAgreement semantic types implementation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R15-CLOSE, **18 paths per repo**, single pass. Implements the **semantic value layer**: `ParticipantGitCommits`, `ParticipantTokenUsage`, `ResultContributionEntry` and `ResultContribution` in `app.result_values`; **`ResultAgreement`**`(game_id, game_uid, declaration_ref, timestamp, contribution)` in `app.peer_final_messages`; identity-preserving façade export. Reuses `GitCommitSha`, `UtcTimestamp` and `Sha256Digest` rather than redefining them. Enforces the six-entry **1…6 ascending** rule, the **game-fixed commit** invariant and the `declaration_ref == declaration_<game_id>.json` join. **96 new tests per repo**; totals **1667 / 1662** at **100.00% statement and branch coverage**; all files ≤150 LOC. **Not implemented:** operation runtime, timestamp proposer, two-request cadence, `RESULT_APPROVAL_CORE` assembly, `result_sha256`, digest-response handling, `mutual_agreement` transition, FastMCP, transport, reporting, Gmail, token crypto. **`ResultAgreement` stays operationally READY**; matrix **8 = 4/4/0**; FIELD_MATRIX **74 = 15/39/9/11**.
- [x] **Stage 4E-R16 — Peer application protocol runtime implementation** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R16-CLOSE, **52 paths per repo** (16 production, 27 tests, 3 contract reconciliations, 6 tracking), across three stages: R16 implemented broadly, supervising review found the conditional-VRAM branch under-proved and one count assertion unconditional, and **R16-FIX proved production already correct** while repairing the proof and the wording. **Step-0** projects its **19-member inventory**, constructs and verifies its keyed proof, validates participant ownership and the equality-only token cap, merges immutably and gates on both directions. **Config negotiation** implements the deterministic byte-wise-lower proposer, the bounded cadence, LIVE validation and the agreed config/profile state. **Config lock** implements the **35-member** projection, `config_sha256`, authenticated evidence and `CONFIG_LOCKED` gating. **ResultAgreement** implements approval-core assembly, contribution merge, derived `total_tokens`, `result_sha256`, the timestamp proposer, the two-request cadence, `Sha256Digest` responses and both completion gates. **Auth** is concrete HMAC-SHA256 with an injected `ED25519` path and **no fallback**. `app` never imports `protocol` — everything crosses through `app.ports`. `hardware.vram_gb` is the sole conditional member: **18** present leaves CPU-only, **19** with a GPU, omitted and never `null`. `CANONICALIZATION_CONTRACT.md` now freezes the framing byte-exactly as `b"step0"`/`b"config"` + canonical bytes, **no separator**. **293** R16 tests; totals **1959 / 1954** at **100.00% statement and branch coverage**; all files ≤150 LOC. **Not implemented:** FastMCP, transport, tunnel, Gmail, reporting, GUI, artifact persistence, token crypto. **Application-runtime matrix 8 / 0 / 0 / 8; transport OUTSTANDING**; FIELD_MATRIX **74 = 15/39/9/11**. See **D46**.
- [x] **Stage 4E-R17-R1 — FastMCP transport prerequisites** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R17-R1-CLOSE, **16 paths per repo** (2 dependency, 2 contracts, 6 probes, 6 tracking), **0 production**. The original **Stage 4E-R17 stopped BLOCKED-BEFORE-CODE with 0 files changed**, on two blockers supervising review accepted as real: `FASTMCP-DEPENDENCY: BLOCKED-BY-MISSING-DECLARED-DEPENDENCY` (the project had no runtime dependency at all) and `TRANSPORT-OPERATION-DISCRIMINATOR: BLOCKED-BY-UNFROZEN-WIRE-SHAPE` (three variants share `negotiate`, three share `receive_turn`, two share `submit_audit`, with every signature deferred by `PRD02-FR-035` and **O7**). R17-R1 provisioned **`fastmcp==3.4.6`**, audited the installed API mechanically, and froze the wire contract: one `request` argument carrying exactly `{kind, payload}`, a **closed nine-token `kind` vocabulary**, no heartbeat, and `E-PROTO-MALFORMED` for an unknown token or a valid token on the wrong tool. **The Decimal probe is the finding that mattered:** a JSON *number* `0.10` arrives as `Decimal('0.1')` — lossy — while the JSON *string* `"0.10"` arrives exactly, so the wire rule is canonical decimal **text** with `Decimal(text)` reconstruction and no float path; `config_sha256` was proved identical before and after a real FastMCP HTTP round trip. `ToolError` identity is recovered exactly (`str(exc) == "E-PROTO-STALE"`). **R17-R1-FIX** made **`pydantic==2.13.4`** direct, because only a Pydantic model refuses an extra envelope member — a `dataclass` and a `TypedDict` accept one. **44 probes**; totals **2003 / 1998** at **100.00% coverage**. **No transport was implemented.** Transport: **prerequisites CLOSED, implementation NOT STARTED**; matrix **8/0/0/8** (local application behaviour, *not* transport); FIELD_MATRIX **74 = 15/39/9/11**. See **D47**.
- [x] **Stage 4E-R17 — FastMCP + peer transport adapter integration** — **CLOSED / COMMITTED / CI-GREEN** at Stage 4E-R17-RESUME-CLOSE-RETRY, **53 paths per repo** (23 production, 23 tests, 1 architecture, 6 tracking; 0 dependency, 0 CI, 0 deletion). The stage took eight passes and both stops are recorded because both were correct. **(1)** Original **R17** was authorized to implement the adapter and changed **0 files**, stopping on two genuine blockers: `fastmcp` was neither declared, locked nor importable, and the wire envelope shape was unfrozen. **(2) R17-R1** provisioned and froze the prerequisites - `fastmcp==3.4.6`, `pydantic==2.13.4` as a **direct** dependency, and the `request = {kind, payload}` envelope. **(3) R17-RESUME** implemented the local transport: 19 `transport.*` modules, four tools, nine kinds, the canonical decimal-text codec and two-process Streamable HTTP. **(4)** Supervising review found the result cadence under-proved, timeout/watchdog ownership unproved and the error inventory incomplete; **FIX** completed all three. **(5)** Review then found that a digest mismatch produced only a quiet not-agreed, that the test harness duplicated the digest algorithm, and that no `PeerTransportPort` abstraction existed; **FIX2** added `require_matching_digest`, a production-delegating harness and the real port/adapter separation. **(6)** The **first CLOSE hard gate then discovered that the new guard itself was still unwired**: `require_matching_digest`, `MutualAgreementGate` and `ResultDigester` each had exactly one hit under `src/` - their own definition - so the comparison was invoked only by a test. That CLOSE **stopped with 0 files changed** on `RESULT-DIGEST-DISAGREEMENT-INTEGRATION: BLOCKED-BY-UNWIRED-PRODUCTION-GUARD`. **(7) FIX3** introduced `app.result_exchange.ResultExchange`, the application owner that wires the `PeerTransportPort` response to the local production digest, to `require_matching_digest`, to direction verification and to `MutualAgreementGate`; the successful and mismatching two-process cadences now drive production and the harness computes nothing. **(8)** This CLOSE. The shipped surface: four public tools (`negotiate`, `receive_turn`, `submit_audit`, `receive_control`) carrying nine kinds over one `request = {kind, payload}` argument with `additionalProperties: false` at both levels; **no heartbeat, no fifth tool**. `Decimal("0.10")` crosses as canonical **text** and returns lossless; `config_sha256` is byte-preserved. Post-lock deadlines come from the locked `network_and_league.response_timeout_sec` (17/45/120 proved to reach the FastMCP invocation, never a hardcoded 30); the bootstrap window comes from the frozen negotiation window; `app.peer_supervision.Watchdog` is application-level, injected-clock only, and **assigns no sanction**. All seven peer identities round-trip through `ToolError`, with `E-TRANSPORT` and `E-TIMEOUT-WATCHDOG` kept as separate local categories. `Reveal` returns `False` **only** for a game-illegal action after lower-layer success. **142** R17 unit/adapter and **25** process-integration tests; totals 2171 (Police) / 2166 (Thief) at 100.00% statement **and** branch coverage. Local FastMCP Streamable HTTP is implemented and verified between two independent OS processes. **Public transport is not implemented and not tested** - no tunnel, no externally reachable endpoint, no external reachability evidence, no reconnect evidence, no real opponent, no counted match. **`TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`** is carried forward untouched. **Stage 4E is NOT COMPLETE.**
- [ ] **Stage 4E-R18 — Public network + tunnel integration** — **NEXT AUTHORIZED; NOT STARTED.** Expose the **same** stable FastMCP ingress through the approved public network/tunnel path: a tunnel process, a stable externally reachable endpoint, external reachability validation, all four tools through the public ingress, failure/reconnect evidence, public endpoint evidence, and preparation for real-opponent interoperability. It must **not** redesign the protocol, the envelope, the kinds or the error model. Token-accounting cryptographic evidence remains a separate mandatory later Stage-4 security slice.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**. **Stage 4 must not be declared complete once transport lands** — this obligation needs its own explicit design and implementation stage.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**. R16 aggregates and mutually agrees *reported* token totals and proves nothing about metering. **Stage 4 must not be declared complete once transport lands** — this obligation needs its own explicit design and implementation stage.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**. Reported result token totals are what the peers agree was reported, not proof every call was metered. **Stage 4 must not be declared complete once the protocol runtime lands** — this obligation needs its own explicit design + implementation/verification stage.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**. **Stage 4 must not be declared complete merely because the peer-family semantic values are implemented** — this obligation needs its own explicit design + implementation/verification stage.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**. Agreed result token totals are what the peers report, not proof that every call was metered. **Stage 4 must not be declared complete once the peer families are implemented** — this obligation needs its own explicit design + implementation/verification stage.
- [ ] **Mandatory pre-Stage-4-close security slice — `TOKEN-ACCOUNTING-CRYPTO-EVIDENCE: BLOCKED-BY-CONSTRUCTION`.** Ch 5 §5.5 requires actual LLM consumption to be **monitored** and **cryptographically locked**; the construction is SOURCE-UNSPECIFIED and **not yet frozen**, and `result_sha256` is **not** claimed to satisfy it. **Stage 4 must not be declared complete merely because every peer-family contract reaches READY/IMPLEMENTED** — this obligation needs its own explicit design + implementation/verification stage.
- [ ] Collaborator (Rawey7) access - pending explicit instruction.

_Phases 1 and 2 are specification and requirements only; all seven PRDs remain
APPROVED — PHASE 2 LOCKED. Phase 3 implementation has begun with the Stage-3A
domain foundation (grid config, coordinates, board geometry, move vocabulary,
movement legality). No JSON schema, protocol, networking, cryptography,
strategy, GUI or reporting code has been implemented._
