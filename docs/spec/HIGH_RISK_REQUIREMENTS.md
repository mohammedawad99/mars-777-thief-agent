# High-Risk Requirement Audit — group MaRs-777

**Status: REVIEWED — Stage 1B supervising review PASS. No gate implemented (gates
are proposed only, for later PRDs/CI). Approved baseline (input to Stage 1C).**

Requirements where a small compliance mistake could cause technical loss,
invalid match, score loss, disqualification, inability to verify, inability to
report, or submission rejection. Each row lists the catalog IDs, source, failure
mode, prevention strategy, and the planned future automated/manual gate (not
built yet). Ordered roughly by severity of consequence.

| # | Requirement IDs | Source | Failure mode | Prevention strategy | Future automated gate | Future manual gate |
|---|---|---|---|---|---|---|
| H-01 | ARCH-001, ARCH-002, ARCH-003 | PDF p.31,142–143 | Any shared memory/variable/module between police & thief → **immediate DQ for info leakage**; also invalidates local dev results. | Two repos, two venvs, two processes, no shared importable live-state module; separate `config/` trees. | CI check: import-graph has no cross-agent module; process-isolation smoke test. | Reviewer inspects launch scripts & module boundaries. |
| H-02 | GAME-001, JSON-004, GAME-002 | PDF p.34,128,144 | Config not byte-identical / a MINIMUM lowered → **symmetry break, game DQ**. | Canonical JSON + `config_sha256` exchange before play; validator enforcing Appendix F floors. | Pre-game hash-equality gate; config-vs-F floor validator. | Reviewer diffs both signed configs. |
| H-03 | CRYPTO-001, CRYPTO-002, CRYPTO-003, CRYPTO-008, CRYPTO-009, REPLAY-002 | PDF p.50–55,72–75 | Non-canonical/ambiguous hashing or wrong reveal order → hash mismatch at audit → **score 0**, or a real tamper undetected. | One shared canonical serializer (sorted keys, fixed separators, UTF-8); nonce hidden until final audit; cross-impl byte-identity tests. | Byte-identity test between peers; tamper-injection e2e replay test. | Reviewer runs Replay Viewer on a known-good and a tampered log. |
| H-04 | LEAGUE-002, REPORT-001, JSON-001, JSON-002 | PDF p.94–95,147 | One side fails to send, sends free text, or reports conflict → **0 to both**; non-JSON → rejected → 0. | Both agents auto-send; strict JSON attachment; result reconciliation with opponent before send. | Schema validation gate; dual-report reconciliation test; attachment-type check. | Reviewer inspects both teams' sent result JSONs. |
| H-05 | SEC-003, SEC-004, SEC-005, SEC-006, SEC-002 | PDF p.121–123,135,148 | Pushing `credentials.json`/`token.json`/keys → **severe security failure & project failure**; a leaked secret is permanent in history. | `.gitignore` from first commit; send-only `gmail.send` scope; secret-scan; rotate on any leak. | CI secret-scan on push; staged-diff secret scan; scope assertion. | Reviewer audits history + OAuth scope. |
| H-06 | CRYPTO-004, CRYPTO-005, BAR-001, BAR-002 | PDF p.37–38,144–145 | False capture claim or hidden/lied barrier → detected in log audit → **immediate DQ, no appeal**. | Truthful capture protocol; declare every barrier + exact cell; audit both sides' logs. | Log-audit cross-check of claims vs commits. | Reviewer replays capture and barrier events. |
| H-07 | REPLAY-001 | PDF p.72,145 | No Replay Viewer → cannot pass audit → **blocks submission**. | Build the viewer early (PRD-07) with per-step SHA-256 verify + Verified OK/TAMPERED. | Presence + functional e2e test in CI. | Reviewer runs the viewer and checks screenshots. |
| H-08 | GIT-003, CRYPTO-006 | PDF p.56,150 | Wrong/missing per-game commit hash → examiner cannot reproduce the exact game → integrity/repro failure. | Step-0 records the exact commit hash each game; also in result JSON `github_commit`; email per game. | Field-presence + git-rev existence check. | Reviewer checks out the recorded commit. |
| H-09 | NET-001, NET-003, NET-002, SEC-001, REPORT-003 | PDF p.29,89–91,95,144–146 | No public tunnel → cannot compete; unthrottled sends → 429 → **account blocked, reporting paralysed**. | Tunnel for league; Gatekeeper trio (Quota Manager, Token Bucket, DOS detector); honor 429 backoff. | Public-reachability test; rate-limit burst test; 429-backoff test. | Reviewer verifies tunnel + limiter behavior. |
| H-10 | STATE-002, STATE-003, STATE-004, STATE-005 | PDF p.78–83,143 | Illegal transition / no deadline / no watchdog → **deadlock → technical loss**; silent freeze loses documentation. | Strict transition table; per-request expiry; heartbeat watchdog with controlled shutdown + persistence. | FSM illegal-transition test; timeout-injection; heartbeat-loss test. | Reviewer forces opponent disconnect and checks graceful TECHNICAL_LOSS. |
| H-11 | LEAGUE-001, LEAGUE-003, LEAGUE-004, LEAGUE-005 | PDF p.86,147–149 | Fewer than min games / false game-count → **no passing grade / total DQ**; extra counted games vs same team invalid. | Track counted games; truthful start-of-game declaration; one counted game per opponent. | Game-count & opponent-uniqueness checks vs result set. | Reviewer cross-checks declarations vs lecturer records. |
| H-12 | GAME-003, GAME-004, GAME-005, GAME-006, GAME-009 | PDF p.37–38,58 | Illegal move (diagonal), mis-scored terminal, or LLM-decided move → **technical loss / wrong score**. | Deterministic legality + scoring code; LLM never decides moves; unit tests on every terminal case. | Move-legality + scoring unit suite. | Reviewer replays edge cases (trap, barrier-capture). |
| H-13 | SCENT-001, SCENT-002, SCENT-003 | PDF p.43,47,145 | Scent-model mismatch between peers → **game voided**. | Exchange full emission/decay model + numeric example; verify identical interpretation; crypto-lock. | Model-hash equality + formula unit test. | Reviewer checks the signed scent-model hash. |
| H-14 | SUB-001..005, DOC-001, DOC-002, GIT-001, GIT-002 | PDF p.96,114,134,136 | Missing tag, missing README component, wrong links (2 vs 4), altered Moodle form, missing per-member submission, self-scoring the game → **submission rejected / no grade**. | Appendix C Table 6 gate before tagging; annotated tag; correct link counts; per-member Moodle submit; self-score = code quality only. | Submission-checklist CI gate; link-count assertions. | Reviewer walks the full Table 6 checklist. |
| H-15 | LLM-002, LLM-003, LLM-004 | PDF p.146,152,67 | Numeric-position protocol or over-length hints → **DQ of game character**. | Free natural-language hints only; enforce word limit on template and LLM; ban numeric-coord protocol. | Hint-content + word-count audit. | Reviewer samples hint transcripts. |

## Explicitly examined risk areas (as required)

- **Peer separation** → H-01. **Public-network requirement** → H-09. **Negotiated configuration** → H-02. **Legal movement validation** → H-12. **Time limits** → H-10 (deadline/watchdog; Appendix F timeouts). **Commit → Ack → Reveal** → H-03/H-06. **Canonical data/hash integrity** → H-03. **Replay/tamper verification** → H-03/H-07. **Exact game commit** → H-08. **League game counts/opponent rules** → H-11. **Reporting** → H-04/H-09. **GitHub/tag/submission** → H-14. **Secrets** → H-05.

No gate is implemented in Stage 1A. Gates are proposed for later PRDs/CI only
after the specification is reviewed and approved.
